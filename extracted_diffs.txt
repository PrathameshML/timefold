@@ -11453,7 +11453,14 @@
                     
                     List<String> required = requiredSkillsMap.getOrDefault(emp.getPosition(), List.of());
                     Set<String> empSkills = emp.getSkills() != null ? emp.getSkills() : Set.of();
-                    long missing = required.stream().filter(s -> !empSkills.contains(s)).count();
+                    long missing = required.stream().filter(reqSkill -> {
+                        if (reqSkill == null) return false;
+                        String normalizedReq = reqSkill.trim().toLowerCase();
+                        return empSkills.stream()
+                                .filter(s -> s != null)
+                                .map(s -> s.trim().toLowerCase())
+                                .noneMatch(normalizedReq::equals);
+                    }).count();
                     entity.setMissingSkillCount(missing);
                     
                     entity.setSkills(emp.getSkills());

===DIFF===

@@ -10909,7 +10909,7 @@
                             Joiners.equal(Scheduler.EmployeeAssignment::getEmployeeId),
                             Joiners.equal(Scheduler.EmployeeAssignment::getDate))
                     .filter((a1, a2) -> a1.getShift() != null && a2.getShift() != null)
-                    .penalizeLong(penalty)
+                    .penalizeLong(penalty, (a1, a2) -> 10000L)
                     .asConstraint("noOverlappingShifts");
         }
 
@@ -10927,7 +10927,7 @@
                         }
                         return fails;
                     })
-                    .penalizeLong(penalty)
+                    .penalizeLong(penalty, a -> 10000L)
                     .asConstraint("unavailableTimeslot_ratingMismatch");
         }
 
@@ -10944,7 +10944,7 @@
                                     Scheduler.ShiftSchedule.RoleLimit::getRoleName))
                     .filter((date, position, count, roleLimit) -> count < roleLimit.getMaxWorkers())
                     .penalizeLong(penalty,
-                            (date, position, count, roleLimit) -> (long) (roleLimit.getMaxWorkers() - count))
+                            (date, position, count, roleLimit) -> (long) (roleLimit.getMaxWorkers() - count) * 20000L)
                     .asConstraint("everyShiftPlanned");
         }
 
@@ -10963,7 +10963,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> a.getShiftDurationHours() > maxHours)
-                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - maxHours) * 60))
+             
<truncated 885 bytes>
10984,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> a.getShiftDurationHours() > threshold)
-                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - threshold) * 100))
+                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - threshold) * 200L))
                     .asConstraint("overtimeThreshold");
         }
 
@@ -10999,7 +10999,7 @@
                     })
                     .penalizeLong(penalty, a -> {
                         long workBeforeBreak = java.time.Duration.between(a.getShiftStartTimeObj(), a.getBreakStartTime()).toMinutes();
-                        return (long) (workBeforeBreak - (maxHours * 60));
+                        return (long) (workBeforeBreak - (maxHours * 60)) * 100L;
                     })
                     .asConstraint("breakAfterHours");
         }
@@ -11026,7 +11026,7 @@
                                 totalGaps += (daysBetween - 1);
                             }
                         }
-                        return totalGaps * 10L;
+                        return totalGaps * 50L;
                     })
                     .asConstraint("consecutiveShifts");
         }
@@ -11036,7 +11036,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> !a.isPermanentEmployee())
-                    .penalizeLong(penalty, a -> a.isPrioritizePermanent() ? 50L : 0L)
+                    .penalizeLong(penalty, a -> a.isPrioritizePermanent() ? 100L : 0L)
                     .asConstraint("permanentPriority");
         }
 

===DIFF===

@@ -10909,7 +10909,7 @@
                             Joiners.equal(Scheduler.EmployeeAssignment::getEmployeeId),
                             Joiners.equal(Scheduler.EmployeeAssignment::getDate))
                     .filter((a1, a2) -> a1.getShift() != null && a2.getShift() != null)
-                    .penalizeLong(penalty)
+                    .penalizeLong(penalty, (a1, a2) -> 10000L)
                     .asConstraint("noOverlappingShifts");
         }
 
@@ -10927,7 +10927,7 @@
                         }
                         return fails;
                     })
-                    .penalizeLong(penalty)
+                    .penalizeLong(penalty, a -> 10000L)
                     .asConstraint("unavailableTimeslot_ratingMismatch");
         }
 
@@ -10944,7 +10944,7 @@
                                     Scheduler.ShiftSchedule.RoleLimit::getRoleName))
                     .filter((date, position, count, roleLimit) -> count < roleLimit.getMaxWorkers())
                     .penalizeLong(penalty,
-                            (date, position, count, roleLimit) -> (long) (roleLimit.getMaxWorkers() - count))
+                            (date, position, count, roleLimit) -> (long) ((roleLimit.getMaxWorkers() - count) * 10000L))
                     .asConstraint("everyShiftPlanned");
         }
 
@@ -10963,7 +10963,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> a.getShiftDurationHours() > maxHours)
-                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - maxHours) * 60))
+           
<truncated 891 bytes>
984,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> a.getShiftDurationHours() > threshold)
-                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - threshold) * 100))
+                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - threshold) * 200L))
                     .asConstraint("overtimeThreshold");
         }
 
@@ -10999,7 +10999,7 @@
                     })
                     .penalizeLong(penalty, a -> {
                         long workBeforeBreak = java.time.Duration.between(a.getShiftStartTimeObj(), a.getBreakStartTime()).toMinutes();
-                        return (long) (workBeforeBreak - (maxHours * 60));
+                        return (long) ((workBeforeBreak - (maxHours * 60)) * 100L);
                     })
                     .asConstraint("breakAfterHours");
         }
@@ -11026,7 +11026,7 @@
                                 totalGaps += (daysBetween - 1);
                             }
                         }
-                        return totalGaps * 10L;
+                        return totalGaps * 50L;
                     })
                     .asConstraint("consecutiveShifts");
         }
@@ -11036,7 +11036,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> !a.isPermanentEmployee())
-                    .penalizeLong(penalty, a -> a.isPrioritizePermanent() ? 50L : 0L)
+                    .penalizeLong(penalty, a -> a.isPrioritizePermanent() ? 100L : 0L)
                     .asConstraint("permanentPriority");
         }
 

===DIFF===

@@ -10909,7 +10909,7 @@
                             Joiners.equal(Scheduler.EmployeeAssignment::getEmployeeId),
                             Joiners.equal(Scheduler.EmployeeAssignment::getDate))
                     .filter((a1, a2) -> a1.getShift() != null && a2.getShift() != null)
-                    .penalizeLong(penalty, (a1, a2) -> 10000L)
+                    .penalizeLong(penalty, (a1, a2) -> 3000L)
                     .asConstraint("noOverlappingShifts");
         }
 
@@ -10927,7 +10927,7 @@
                         }
                         return fails;
                     })
-                    .penalizeLong(penalty, a -> 10000L)
+                    .penalizeLong(penalty, a -> 3000L)
                     .asConstraint("unavailableTimeslot_ratingMismatch");
         }
 
@@ -10944,7 +10944,7 @@
                                     Scheduler.ShiftSchedule.RoleLimit::getRoleName))
                     .filter((date, position, count, roleLimit) -> count < roleLimit.getMaxWorkers())
                     .penalizeLong(penalty,
-                            (date, position, count, roleLimit) -> (long) ((roleLimit.getMaxWorkers() - count) * 10000L))
+                            (date, position, count, roleLimit) -> (long) ((roleLimit.getMaxWorkers() - count) * 3000L))
                     .asConstraint("everyShiftPlanned");
         }
 

===DIFF===

@@ -10898,7 +10898,7 @@
                         double actualPct = (matchedCount * 100.0) / required.size();
                         return actualPct < matchPercentage;
                     })
-                    .penalizeLong(penalty, a -> a.getMissingSkillCount() * 5000L)
+                    .penalizeLong(penalty, a -> a.getMissingSkillCount() * 2000L)
                     .asConstraint("skillMatch");
         }
 
@@ -10908,7 +10908,7 @@
                             Joiners.equal(Scheduler.EmployeeAssignment::getEmployeeId),
                             Joiners.equal(Scheduler.EmployeeAssignment::getDate))
                     .filter((a1, a2) -> a1.getShift() != null && a2.getShift() != null)
-                    .penalizeLong(penalty, (a1, a2) -> 10000L)
+                    .penalizeLong(penalty, (a1, a2) -> 3000L)
                     .asConstraint("noOverlappingShifts");
         }
 
@@ -10926,7 +10926,7 @@
                         }
                         return fails;
                     })
-                    .penalizeLong(penalty, a -> 10000L)
+                    .penalizeLong(penalty, a -> 3000L)
                     .asConstraint("unavailableTimeslot_ratingMismatch");
         }
 
@@ -10943,7 +10943,7 @@
                                     Scheduler.ShiftSchedule.RoleLimit::getRoleName))
                     .filter((date, position, count, roleLimit) -> count < roleLimit.getMaxWorkers())
                     .penalizeLong(penalty,
-                            (date, position, count, roleLimit) -> (long) ((roleLimit.getMaxWorkers() - count) * 10000L))
+                            (date, position, count, roleLimit) -> (long) ((roleLimit.getMaxWorkers() - count) * 3000L))
                     .asConstraint("everyShiftPlanned");
         }
 
@@ -11071,7 +11071,7 @@
                 factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .rewardLong(HardMediumSoftLongScore.ONE_SOFT, 
-                            a -> (long) (a.getPerformanceRating() * a.getShiftDurationHours() * 10))
+                            a -> (long) (a.getPerformanceRating() * a.getShiftDurationHours() * 5))
                     .asConstraint("maximizeRating")
             );
 

===DIFF===

@@ -11510,7 +11510,15 @@
                         pinnedEntity.setShiftStartStr(startTime);
                         pinnedEntity.setShiftStartTimeObj(startLocalTime);
                         pinnedEntity.setShiftEndStr(endTime);
+                        
+                        // Propagate real employee fields to avoid triggering false positive constraints
                         pinnedEntity.setHourlyWage(emp.getHourlyWage());
+                        pinnedEntity.setPerformanceRating(emp.getPerformanceRating());
+                        pinnedEntity.setSkills(emp.getSkills());
+                        pinnedEntity.setEmployeeType(emp.getEmployeeType());
+                        pinnedEntity.setPermanentEmployee("Permanent".equalsIgnoreCase(emp.getEmployeeType()));
+                        pinnedEntity.setPrioritizePermanent(prioritizePermanent);
+                        
                         pinnedEntity.setShiftDurationHours(shiftDurationHours);
                         pinnedEntity.setIsoWeekNum(d.get(java.time.temporal.WeekFields.ISO.weekOfYear()));
                         pinnedEntity.setLocalDateObj(d);

===DIFF===

@@ -11240,6 +11240,20 @@
                 constraintConfigs = getDefaultConstraintConfigs();
             }
             
+            // Allow dynamic overrides from JSON payload for testing
+            @SuppressWarnings("unchecked")
+            List<Map<String, Object>> overrideConfigs = (List<Map<String, Object>>) input.get("config");
+            if (overrideConfigs != null) {
+                for (Map<String, Object> override : overrideConfigs) {
+                    int cId = (Integer) override.get("constraintId");
+                    String severity = (String) override.get("severity");
+                    constraintConfigs.stream()
+                        .filter(c -> c.getConstraintId() == cId)
+                        .findFirst()
+                        .ifPresent(c -> c.setSeverity(severity));
+                }
+            }
+            
             int breakAfterHours = 4; // Default fallback
             ConstraintConfig breakConfig = constraintConfigs.stream()
                     .filter(c -> c.getConstraintId() == 9)

===DIFF===

@@ -11570,6 +11570,21 @@
             problem.setRequestedShiftName(shiftName);
             problem.setPrioritizePermanent(prioritizePermanent);
 
+            // Configure Local Search with enhanced move selectors
+            ai.timefold.solver.core.config.localsearch.LocalSearchPhaseConfig localSearchConfig = 
+                new ai.timefold.solver.core.config.localsearch.LocalSearchPhaseConfig();
+            localSearchConfig.setMoveSelectorConfig(
+                new ai.timefold.solver.core.config.heuristic.selector.move.composite.UnionMoveSelectorConfig()
+                    .withMoveSelectorConfigList(java.util.List.of(
+                        new ai.timefold.solver.core.config.heuristic.selector.move.generic.ChangeMoveSelectorConfig(),
+                        new ai.timefold.solver.core.config.heuristic.selector.move.generic.SwapMoveSelectorConfig(),
+                        new ai.timefold.solver.core.config.heuristic.selector.move.generic.PillarSwapMoveSelectorConfig()
+                    ))
+            );
+            localSearchConfig.setTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
+                    .withSpentLimit(Duration.ofSeconds(15))
+                    .withUnimprovedSpentLimit(Duration.ofSeconds(3)));
+
             SolverConfig solverConfig = new SolverConfig()
                     .withSolutionClass(ShiftScheduleV2.class)
                     .withEntityClasses(Scheduler.EmployeeAssignment.class)
@@ -11576,6 +11576,7 @@
-                    .withTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
-                            .withSpentLimit(Duration.ofSeconds(15))
-                            .withUnimprovedSpentLimit(Duration.ofSeconds(3)));
+                    .withPhaseConfigList(java.util.List.of(
+                        new ai.timefold.solver.core.config.constructionheuristic.ConstructionHeuristicPhaseConfig(),
+                        localSearchConfig
+                    ));
 
             SolverFactory<ShiftScheduleV2> solverFactory = SolverFactory.create(solverConfig);
             Solver<ShiftScheduleV2> solver = solverFactory.buildSolver();

===DIFF===

@@ -11573,14 +11573,14 @@
             // Configure Local Search with enhanced move selectors
             ai.timefold.solver.core.config.localsearch.LocalSearchPhaseConfig localSearchConfig = 
                 new ai.timefold.solver.core.config.localsearch.LocalSearchPhaseConfig();
-            localSearchConfig.setMoveSelectorConfig(
-                new ai.timefold.solver.core.config.heuristic.selector.move.composite.UnionMoveSelectorConfig()
-                    .withMoveSelectorConfigList(java.util.List.of(
-                        new ai.timefold.solver.core.config.heuristic.selector.move.generic.ChangeMoveSelectorConfig(),
-                        new ai.timefold.solver.core.config.heuristic.selector.move.generic.SwapMoveSelectorConfig(),
-                        new ai.timefold.solver.core.config.heuristic.selector.move.generic.PillarSwapMoveSelectorConfig()
-                    ))
-            );
+            ai.timefold.solver.core.config.heuristic.selector.move.composite.UnionMoveSelectorConfig unionMoveConfig =
+                new ai.timefold.solver.core.config.heuristic.selector.move.composite.UnionMoveSelectorConfig();
+            unionMoveConfig.setMoveSelectorList(java.util.List.of(
+                new ai.timefold.solver.core.config.heuristic.selector.move.generic.ChangeMoveSelectorConfig(),
+                new ai.timefold.solver.core.config.heuristic.selector.move.generic.SwapMoveSelectorConfig(),
+                new ai.timefold.solver.core.config.heuristic.selector.move.generic.PillarSwapMoveSelectorConfig()
+            ));
+            localSearchConfig.setMoveSelectorConfig(unionMoveConfig);
             localSearchConfig.setTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
                     .withSpentLimit(Duration.ofSeconds(15))
                     .withUnimprovedSpentLimit(Duration.ofSeconds(3)));
@@ -11587,11 +11587,12 @@
+            java.util.List<ai.timefold.solver.core.config.phase.PhaseConfig> phases = new java.util.ArrayList<>();
+            phases.add(new ai.timefold.solver.core.config.constructionheuristic.ConstructionHeuristicPhaseConfig());
+            phases.add(localSearchConfig);
+
             SolverConfig solverConfig = new SolverConfig()
                     .withSolutionClass(ShiftScheduleV2.class)
                     .withEntityClasses(Scheduler.EmployeeAssignment.class)
-                    .withConstraintProviderClass(ShiftConstraintsV2.class)
-                    .withPhaseConfigList(java.util.List.of(
-                        new ai.timefold.solver.core.config.constructionheuristic.ConstructionHeuristicPhaseConfig(),
-                        localSearchConfig
-                    ));
+                    .withConstraintProviderClass(ShiftConstraintsV2.class);
+            solverConfig.setPhaseConfigList(phases);
 
             SolverFactory<ShiftScheduleV2> solverFactory = SolverFactory.create(solverConfig);
             Solver<ShiftScheduleV2> solver = solverFactory.buildSolver();

===DIFF===

@@ -11570,30 +11570,13 @@
             problem.setRequestedShiftName(shiftName);
             problem.setPrioritizePermanent(prioritizePermanent);
 
-            // Configure Local Search with enhanced move selectors
-            ai.timefold.solver.core.config.localsearch.LocalSearchPhaseConfig localSearchConfig = 
-                new ai.timefold.solver.core.config.localsearch.LocalSearchPhaseConfig();
-            ai.timefold.solver.core.config.heuristic.selector.move.composite.UnionMoveSelectorConfig unionMoveConfig =
-                new ai.timefold.solver.core.config.heuristic.selector.move.composite.UnionMoveSelectorConfig();
-            unionMoveConfig.setMoveSelectorList(java.util.List.of(
-                new ai.timefold.solver.core.config.heuristic.selector.move.generic.ChangeMoveSelectorConfig(),
-                new ai.timefold.solver.core.config.heuristic.selector.move.generic.SwapMoveSelectorConfig(),
-                new ai.timefold.solver.core.config.heuristic.selector.move.generic.PillarSwapMoveSelectorConfig()
-            ));
-            localSearchConfig.setMoveSelectorConfig(unionMoveConfig);
-            localSearchConfig.setTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
-                    .withSpentLimit(Duration.ofSeconds(15))
-                    .withUnimprovedSpentLimit(Duration.ofSeconds(3)));
-
-            java.util.List<ai.timefold.solver.core.config.phase.PhaseConfig> phases = new java.util.ArrayList<>();
-            phases.add(new ai.timefold.solver.core.config.constructionheuristic.ConstructionHeuristicPhaseConfig());
-            phases.add(localSearchConfig);
-
             SolverConfig solverConfig = new SolverConfig()
                     .withSolutionClass(ShiftScheduleV2.class)
                     .withEntityClasses(Scheduler.EmployeeAssignment.class)
-                    .withConstraintProviderClass(ShiftConstraintsV2.class);
-            solverConfig.setPhaseConfigList(phases);
+                    .withConstraintProviderClass(ShiftConstraintsV2.class)
+                    .withTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
+                            .withSpentLimit(Duration.ofSeconds(15))
+                            .withUnimprovedSpentLimit(Duration.ofSeconds(3)));
 
             SolverFactory<ShiftScheduleV2> solverFactory = SolverFactory.create(solverConfig);
             Solver<ShiftScheduleV2> solver = solverFactory.buildSolver();

===DIFF===

@@ -120,9 +120,7 @@
     await runBenchmark(10, 10);
     
     console.log("\n--- Starting Benchmarks ---");
-    results.push(await runBenchmark(25, 50));
-    results.push(await runBenchmark(50, 100));
-    results.push(await runBenchmark(100, 200));
+    results.push(await runBenchmark(10, 20));
     
     console.log(`\n===============================================`);
     console.log(`BENCHMARK SUMMARY`);

===DIFF===

@@ -97,6 +97,14 @@
         console.log(`  Perfect Workers ($10/hr) Assigned: ${goodAssigned}`);
         console.log(`  Sub-optimal Workers ($50/hr) Assigned: ${badAssigned}`);
         
+        const configs = [
+            { empCount: 25, shiftCount: 50 },
+            { empCount: 50, shiftCount: 100 },
+            { empCount: 100, shiftCount: 200 },
+            { empCount: 250, shiftCount: 500 },
+            { empCount: 500, shiftCount: 1000 }
+        ];
+
         return {
             empCount,
             shiftCount,

===DIFF===

@@ -97,13 +97,7 @@
         console.log(`  Perfect Workers ($10/hr) Assigned: ${goodAssigned}`);
         console.log(`  Sub-optimal Workers ($50/hr) Assigned: ${badAssigned}`);
         
-        const configs = [
-            { empCount: 25, shiftCount: 50 },
-            { empCount: 50, shiftCount: 100 },
-            { empCount: 100, shiftCount: 200 },
-            { empCount: 250, shiftCount: 500 },
-            { empCount: 500, shiftCount: 1000 }
-        ];
+
 
         return {
             empCount,
@@ -130,6 +130,8 @@
     results.push(await runBenchmark(25, 50));
     results.push(await runBenchmark(50, 100));
     results.push(await runBenchmark(100, 200));
+    results.push(await runBenchmark(250, 500));
+    results.push(await runBenchmark(500, 1000));
     
     console.log(`\n===============================================`);
     console.log(`BENCHMARK SUMMARY`);

===DIFF===

@@ -1,4 +1,3 @@
-request->
 {
   "shift_name": "Morning",
   "start_date": "2026-01-19",

===DIFF===

@@ -61,69 +61,9 @@
     { "employee_id": "147", "name": "Amit Shenoy", "rate": 9.0, "unit": "hour", "rating": 4, "role": "Helper", "gender": "Male", "employeeType": "Permanent" },
     { "employee_id": "148", "name": "Radha Bhat", "rate": 8.0, "unit": "hour", "rating": 2, "role": "Helper", "gender": "Female", "employeeType": "Contract" },
     { "employee_id": "149", "name": "Vikram Kumar", "rate": 9.5, "unit": "hour", "rating": 3, "role": "Helper", "gender": "Male", "employeeType": "Permanent" },
-    { "employee_id": "150", "name": "Anita Sharma", "rate": 8.5, "unit": "hour", "rating": 4, "role": "Helper", "gender": "Female", "employeeType": "Contract" },
-    { "employee_id": "151", "name": "Suresh Singh", "rate": 15.0, "unit": "hour", "rating": 4, "role": "CNC Operator", "gender": "Male", "employeeType": "Permanent" },
-    { "employee_id": "152", "name": "Neha Patel", "rate": 14.5, "unit": "hour", "rating": 3, "role": "CNC Operator", "gender": "Female", "employeeType": "Contract" },
-    { "employee_id": "153", "name": "Deepak Verma", "rate": 16.0, "unit": "hour", "rating": 5, "role": "CNC Operator", "gender": "Male", "employeeType": "Permanent" },
-    { "employee_id": "154", "name": "Pooja Gupta", "rate": 15.5, "unit": "hour", "rating": 4, "role": "CNC Operator", "gender": "Female", "employeeType": "Contract" },
-    { "employee_id": "155", "name": "Ravi Desai", "rate": 26.5, "unit": "hour", "rating": 5, "role": "Supervisor", "gender": "Male", "employeeType": "Permanent" },
-    { "employee_id": "156", "name": "Meena Joshi", "rate": 8.0, "unit": "hour", "rating": 2, "role": "Helper", "gender": "Female", "employeeType": "Contract" },
-    { "employee_id": "157", "name": "Sunil Yadav", "rate": 8.5, "unit": "hour", "rating": 3, "role": "Helper", "gender": "Male", "employeeTy
<truncated 6099 bytes>
employeeType": "Contract" },
-    { "employee_id": "195", "name": "Rajesh Kumar", "rate": 25.0, "unit": "hour", "rating": 4, "role": "Supervisor", "gender": "Male", "employeeType": "Permanent" },
-    { "employee_id": "196", "name": "Kavita Sharma", "rate": 8.5, "unit": "hour", "rating": 3, "role": "Helper", "gender": "Female", "employeeType": "Contract" },
-    { "employee_id": "197", "name": "Amit Singh", "rate": 9.0, "unit": "hour", "rating": 4, "role": "Helper", "gender": "Male", "employeeType": "Permanent" },
-    { "employee_id": "198", "name": "Divya Patel", "rate": 8.0, "unit": "hour", "rating": 2, "role": "Helper", "gender": "Female", "employeeType": "Contract" },
-    { "employee_id": "199", "name": "Vikram Verma", "rate": 9.5, "unit": "hour", "rating": 3, "role": "Helper", "gender": "Male", "employeeType": "Permanent" },
-    { "employee_id": "200", "name": "Smita Gupta", "rate": 8.5, "unit": "hour", "rating": 4, "role": "Helper", "gender": "Female", "employeeType": "Contract" }
+    { "employee_id": "150", "name": "Anita Sharma", "rate": 8.5, "unit": "hour", "rating": 4, "role": "Helper", "gender": "Female", "employeeType": "Contract" }
   ]
 }
-
-
-
-
-
-
-
-
-
-responce->
 {
     "status": "success",
     "version": "v2",
@@ -131,11 +131,11 @@
     "shift_time": "06:00 - 14:00",
     "shift_duration_hours": 8.0,
     "total_working_days": 5,
-    "solver_score": "0hard/0medium/4376soft",
-    "solver_time_seconds": 60.038,
+    "solver_score": "0hard/0medium/3868soft",
+    "solver_time_seconds": 60.001,
     "new_assignments_made": 110,
-    "entities_planned": 500,
-    "total_possible_assignments": 500,
+    "entities_planned": 250,
+    "total_possible_assignments": 250,
     "skipped_count": 0,
     "override_used": false,
     "ot_threshold_hours": 8.0,

===DIFF===

@@ -161,41 +161,1282 @@
             },
             {
                 "skills": [],
-                "employeeName": "Pooja Reddy",
-                "role": "Helper",
-                "employeeType": "Permanent",
-                "gender": "Female",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 5,
-                "regular_hours": 8.0,
-                "employeeId": "112",
-                "ot_hours": 0,
-                "wage": 9.5
-            },
-            {
-                "skills": [],
-                "employeeName": "Meena Iyer",
-                "role": "Helper",
-                "employeeType": "Permanent",
-                "gender": "Female",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 4,
-                "regular_hours": 8.0,
-                "employeeId": "114",
-                "ot_hours": 0,
-                "wage": 8.0
+                "employeeName": "Meena Iyer",
+                "role": "Helper",
+                "employeeType": "Permanent",
+                "gender": "Female",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+         
<truncated 42802 bytes>

+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 3,
+                "regular_hours": 8.0,
+                "employeeId": "109",
+                "ot_hours": 0,
+                "wage": 8.5
+            }
+        ],
+        "2026-01-22": [
+            {
+                "skills": [],
+                "employeeName": "Sunil Yadav",
+                "role": "CNC Operator",
+                "employeeType": "Permanent",
+                "gender": "Male",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 4,
+                "regular_hours": 8.0,
+                "employeeId": "111",
+                "ot_hours": 0,
+                "wage": 13.5
+            },
+            {
+                "skills": [],
+                "employeeName": "Pooja Reddy",
+                "role": "Helper",
+                "employeeType": "Permanent",
+                "gender": "Female",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 5,
+                "regular_hours": 8.0,
+                "employeeId": "112",
+                "ot_hours": 0,
+                "wage": 9.5
             },
             {
                 "skills": [],

===DIFF===

@@ -1478,442 +1478,480 @@
             },
             {
                 "skills": [],
-                "employeeName": "Smita Patil",
-                "role": "Helper",
-                "employeeType": "Permanent",
-                "gender": "Female",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 4,
-                "regular_hours": 8.0,
-                "employeeId": "122",
-                "ot_hours": 0,
-                "wage": 9.0
-            },
-            {
-                "skills": [],
-                "employeeName": "Ajay Dev",
-                "role": "CNC Operator",
-                "employeeType": "Contract",
-                "gender": "Male",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 5,
-                "regular_hours": 8.0,
-                "employeeId": "123",
-                "ot_hours": 0,
-                "wage": 15.5
-            },
-            {
-                "skills": [],
-                "employeeName": "Geeta Phogat",
-                "role": "Helper",
-                "employeeType": "Permanent",
-                "gender": "Female",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-  
<truncated 28686 bytes>
          "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 4,
+                "regular_hours": 8.0,
+                "employeeId": "121",
+                "ot_hours": 0,
+                "wage": 14.0
+            },
+            {
+                "skills": [],
+                "employeeName": "Smita Patil",
+                "role": "Helper",
+                "employeeType": "Permanent",
+                "gender": "Female",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 4,
+                "regular_hours": 8.0,
+                "employeeId": "122",
+                "ot_hours": 0,
+                "wage": 9.0
+            },
+            {
+                "skills": [],
+                "employeeName": "Geeta Phogat",
+                "role": "Helper",
+                "employeeType": "Permanent",
+                "gender": "Female",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 3,
+                "regular_hours": 8.0,
+                "employeeId": "124",
+                "ot_hours": 0,
+                "wage": 8.0
             },
             {
                 "skills": [],

===DIFF===

@@ -1993,6 +1993,25 @@
             },
             {
                 "skills": [],
+                "employeeName": "Anjali Yadav",
+                "role": "CNC Operator",
+                "employeeType": "Permanent",
+                "gender": "Female",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 4,
+                "regular_hours": 8.0,
+                "employeeId": "134",
+                "ot_hours": 0,
+                "wage": 15.5
+            },
+            {
+                "skills": [],
                 "employeeName": "Suraj Rao",
                 "role": "Supervisor",
                 "employeeType": "Permanent",

===DIFF===

@@ -2050,824 +2050,155 @@
             },
             {
                 "skills": [],
-                "employeeName": "Ritu Das",
-                "role": "Helper",
-                "employeeType": "Contract",
-                "gender": "Female",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 4,
-                "regular_hours": 8.0,
-                "employeeId": "138",
-                "ot_hours": 0,
-                "wage": 9.0
-            },
-            {
-                "skills": [],
-                "employeeName": "Kiran Bose",
-                "role": "CNC Operator",
-                "employeeType": "Permanent",
-                "gender": "Male",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 4,
-                "regular_hours": 8.0,
-                "employeeId": "143",
-                "ot_hours": 0,
-                "wage": 15.0
-            },
-            {
-                "skills": [],
-                "employeeName": "Priya Kapoor",
-                "role": "Helper",
-                "employeeType": "Contract",
-                "gender": "Female",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-    
<truncated 30722 bytes>
       "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 4,
+                "regular_hours": 8.0,
+                "employeeId": "147",
+                "ot_hours": 0,
+                "wage": 9.0
+            },
+            {
+                "skills": [],
+                "employeeName": "Radha Kumari",
+                "role": "Helper",
+                "employeeType": "Permanent",
+                "gender": "Female",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 4,
+                "regular_hours": 8.0,
+                "employeeId": "104",
+                "ot_hours": 0,
+                "wage": 9.0
+            },
+            {
+                "skills": [],
+                "employeeName": "Vikram Singh",
+                "role": "Supervisor",
+                "employeeType": "Permanent",
+                "gender": "Male",
+                "break": {
+                    "breakSlot": "10:00 - 10:30",
+                    "breakStart": "10:00",
+                    "breakType": "MANDATORY",
+                    "breakDuration": 30,
+                    "breakEnd": "10:30"
+                },
+                "rating": 5,
+                "regular_hours": 8.0,
+                "employeeId": "105",
+                "ot_hours": 0,
+                "wage": 25.0
             },
             {
                 "skills": [],

===DIFF===

@@ -2221,654 +2221,25 @@
             },
             {
                 "skills": [],
-                "employeeName": "Sunil Yadav",
-                "role": "Helper",
-                "employeeType": "Permanent",
-                "gender": "Male",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 3,
-                "regular_hours": 8.0,
-                "employeeId": "157",
-                "ot_hours": 0,
-                "wage": 8.5
-            },
-            {
-                "skills": [],
-                "employeeName": "Kavita Rao",
-                "role": "Helper",
-                "employeeType": "Contract",
-                "gender": "Female",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-                    "breakEnd": "10:30"
-                },
-                "rating": 4,
-                "regular_hours": 8.0,
-                "employeeId": "158",
-                "ot_hours": 0,
-                "wage": 9.0
-            },
-            {
-                "skills": [],
-                "employeeName": "Ramesh Patil",
-                "role": "CNC Operator",
-                "employeeType": "Permanent",
-                "gender": "Male",
-                "break": {
-                    "breakSlot": "10:00 - 10:30",
-                    "breakStart": "10:00",
-                    "breakType": "MANDATORY",
-                    "breakDuration": 30,
-    
<truncated 26140 bytes>
erlappingShifts) has 0 matches:\n        0: constraint (overtimeThreshold) has 0 matches:\n        0: constraint (skillMatch) has 0 matches:\n        0: constraint (unavailableTimeslot_ratingMismatch) has 0 matches:\n        16960soft: constraint (maximizeRating) has 110 matches:\n            80soft: justified with ([com.scheduler.ShiftApp$Scheduler$EmployeeAssignment@7004c32a])\n            80soft: justified with ([com.scheduler.ShiftApp$Scheduler$EmployeeAssignment@53bd5c24])\n            ...\n    Indictments (top 5 of 192):\n        -108soft: indicted with (com.scheduler.ShiftApp$Scheduler$EmployeeAssignment@661d1158) has 3 matches:\n            -208soft: constraint (wageOptimization)\n            -100soft: constraint (permanentPriority)\n            ...\n        -104soft: indicted with (com.scheduler.ShiftApp$Scheduler$EmployeeAssignment@216ff09e) has 3 matches:\n            -204soft: constraint (wageOptimization)\n            -100soft: constraint (permanentPriority)\n            ...\n        -104soft: indicted with (com.scheduler.ShiftApp$Scheduler$EmployeeAssignment@3b447bcd) has 3 matches:\n            -204soft: constraint (wageOptimization)\n            -100soft: constraint (permanentPriority)\n            ...\n        -104soft: indicted with (com.scheduler.ShiftApp$Scheduler$EmployeeAssignment@1b6e5b79) has 3 matches:\n            -204soft: constraint (wageOptimization)\n            -100soft: constraint (permanentPriority)\n            ...\n        -84soft: indicted with (com.scheduler.ShiftApp$Scheduler$EmployeeAssignment@53bd5c24) has 3 matches:\n            -100soft: constraint (permanentPriority)\n            -64soft: constraint (wageOptimization)\n            ...\n        ...\n",
     "message": "Successfully assigned shifts (V2 with dynamic constraints)!"
 }

===DIFF===

@@ -10878,7 +10878,7 @@
                 case 7 -> buildMaxWeeklyHours(factory, config);
                 case 8 -> buildOvertimeThreshold(factory, config);
                 case 9 -> buildBreakConstraint(factory, config);
-                case 10 -> buildConsecutiveShifts(factory, config);
+                case 10 -> null; // buildConsecutiveShifts(factory, config);
                 case 11 -> buildPermanentPriority(factory, config);
                 default -> null;
             };

===DIFF===

@@ -10878,7 +10878,7 @@
                 case 7 -> buildMaxWeeklyHours(factory, config);
                 case 8 -> buildOvertimeThreshold(factory, config);
                 case 9 -> buildBreakConstraint(factory, config);
-                case 10 -> null; // buildConsecutiveShifts(factory, config);
+                case 10 -> buildConsecutiveShifts(factory, config);
                 case 11 -> buildPermanentPriority(factory, config);
                 default -> null;
             };

===DIFF===

@@ -10878,7 +10878,7 @@
                 case 7 -> buildMaxWeeklyHours(factory, config);
                 case 8 -> buildOvertimeThreshold(factory, config);
                 case 9 -> buildBreakConstraint(factory, config);
-                case 10 -> buildConsecutiveShifts(factory, config);
+                case 10 -> null; // buildConsecutiveShifts(factory, config);
                 case 11 -> buildPermanentPriority(factory, config);
                 default -> null;
             };

===DIFF===

@@ -6381,16 +6381,8 @@
 
         Scheduler.ShiftConstraints.setConfiguration(roleLimits, ratingRequirements);
 
-        Scheduler.ShiftSchedule problem = new Scheduler.ShiftSchedule(
-                planningEntities,
-                possibleShifts,
-                roleLimits,
-                ratingRequirements
-        );
-        problem.setRequestedShiftName(shiftName);
-        problem.setPrioritizePermanent(prioritizePermanent);
-
         SolverConfig solverConfig = new SolverConfig()
+                .withEnvironmentMode(ai.timefold.solver.core.config.solver.EnvironmentMode.FULL_ASSERT)
                 .withSolutionClass(Scheduler.ShiftSchedule.class)
                 .withEntityClasses(Scheduler.EmployeeAssignment.class)
                 .withConstraintProviderClass(Scheduler.ShiftConstraints.class)

===DIFF===

@@ -6182,20 +6182,12 @@
 
         Scheduler.ShiftConstraints.setConfiguration(roleLimits, ratingRequirements);
 
-        Scheduler.ShiftSchedule problem = new Scheduler.ShiftSchedule(
-                planningEntities,
-                possibleShifts,
-                roleLimits,
-                ratingRequirements
-        );
-        problem.setRequestedShiftName(shiftName);
-        problem.setPrioritizePermanent(prioritizePermanent);
-
         SolverConfig solverConfig = new SolverConfig()
+                .withEnvironmentMode(ai.timefold.solver.core.config.solver.EnvironmentMode.FULL_ASSERT)
                 .withSolutionClass(Scheduler.ShiftSchedule.class)
                 .withEntityClasses(Scheduler.EmployeeAssignment.class)
                 .withConstraintProviderClass(Scheduler.ShiftConstraints.class)
-                .withTerminationSpentLimit(Duration.ofSeconds(3));
+                .withTerminationSpentLimit(Duration.ofSeconds(30));
 
         SolverFactory<Scheduler.ShiftSchedule> solverFactory = SolverFactory.create(solverConfig);
         Solver<Scheduler.ShiftSchedule> solver = solverFactory.buildSolver();

===DIFF===

@@ -6182,6 +6182,15 @@
 
         Scheduler.ShiftConstraints.setConfiguration(roleLimits, ratingRequirements);
 
+        Scheduler.ShiftSchedule problem = new Scheduler.ShiftSchedule(
+                planningEntities,
+                possibleShifts,
+                roleLimits,
+                ratingRequirements
+        );
+        problem.setRequestedShiftName(shiftName);
+        problem.setPrioritizePermanent(prioritizePermanent);
+
         SolverConfig solverConfig = new SolverConfig()
                 .withEnvironmentMode(ai.timefold.solver.core.config.solver.EnvironmentMode.FULL_ASSERT)
                 .withSolutionClass(Scheduler.ShiftSchedule.class)

===DIFF===

@@ -4,7 +4,9 @@
 import ai.timefold.solver.core.api.domain.lookup.PlanningId;
 import ai.timefold.solver.core.api.domain.solution.ProblemFactCollectionProperty;
 import ai.timefold.solver.core.api.domain.variable.PlanningVariable;
+import ai.timefold.solver.core.api.domain.entity.PlanningPin;
 import ai.timefold.solver.core.api.score.buildin.hardsoftlong.HardSoftLongScore;
+import ai.timefold.solver.core.api.score.buildin.hardmediumsoftlong.HardMediumSoftLongScore;
 import ai.timefold.solver.core.api.solver.Solver;
 import ai.timefold.solver.core.api.solver.SolverFactory;
 import ai.timefold.solver.core.config.solver.SolverConfig;
@@ -12,6 +12,11 @@
 import ai.timefold.solver.core.api.domain.solution.PlanningEntityCollectionProperty;
 import ai.timefold.solver.core.api.domain.solution.PlanningScore;
 import ai.timefold.solver.core.api.domain.valuerange.ValueRangeProvider;
+import ai.timefold.solver.core.api.score.ScoreManager;
+import ai.timefold.solver.core.api.score.analysis.ScoreAnalysis;
+import ai.timefold.solver.core.api.score.analysis.ConstraintAnalysis;
+import ai.timefold.solver.core.api.score.analysis.MatchAnalysis;
+import ai.timefold.solver.core.api.solver.SolutionManager;
 import com.fasterxml.jackson.annotation.JsonProperty;
 import com.fasterxml.jackson.annotation.JsonPropertyOrder;
 import com.fasterxml.jackson.core.type.TypeReference;

===DIFF===

@@ -59,6 +59,35 @@
         employeeShiftTimesCache.clear();
         employeeShiftTimesCache.putAll(mysqlService.loadAllEmployeeShiftTimes());
         
+        employeeLeaves.clear();
+        employeeLeaves.putAll(mysqlService.loadAllLeaveRequests());
+        
+        overtimeRecords.clear();
+        overtimeRecords.putAll(mysqlService.loadAllOvertimeRecords());
+        
+        leaveCoverageRequests.clear();
+        leaveCoverageRequests.putAll(mysqlService.loadAllCoverageRequests());
+        
+        otCoverageAssignments.clear();
+        for (LeaveCoverageRequest req : leaveCoverageRequests.values()) {
+            if (req.getAssignments() != null) {
+                for (OTCoverageAssignment assignment : req.getAssignments()) {
+                    otCoverageAssignments.put(assignment.getId(), assignment);
+                }
+            }
+        }
+        
+        notifications.clear();
+        notifications.putAll(mysqlService.loadAllNotifications());
+        
+        SystemConfig dbConfig = mysqlService.loadSystemConfig();
+        if (dbConfig != null) {
+            systemConfig = dbConfig;
+            System.out.println("⚙️ Loaded SystemConfig from MySQL");
+        } else {
+            System.out.println("⚙️ Using default SystemConfig (none in MySQL)");
+        }
+        
         System.out.println("✅ Loaded " + shiftAssignments.size() + " days of assignments and " +
                 employeeInfo.size() + " employee records from MySQL");
 

===DIFF===

@@ -127,7 +127,7 @@
         System.out.println("✅ Created " + created + " new employees, total: " + employeeInfo.size());
     }
 
-    private static void saveAssignments() {
+    private static void syncAllEmployeesToDatabase() {
         // We now persist entirely to MySQL instead of JSON.
         // Sync any new or updated employees to the DB.
         for (EmployeeInfo emp : employeeInfo.values()) {

===DIFF===

@@ -153,6 +153,16 @@
         displayLoadedAssignments();
 
         System.out.println("✅ System ready. Use GET /shifts to view assignments.");
+
+        // Load constraint configs from MySQL
+        constraintConfigs = mysqlService.loadAllConstraintConfigs();
+        if (constraintConfigs.isEmpty()) {
+            constraintConfigs = getDefaultConstraintConfigs();
+            mysqlService.insertDefaultConstraints(constraintConfigs);
+            System.out.println("📋 Inserted 11 default constraint configs");
+        } else {
+            System.out.println("📋 Loaded " + constraintConfigs.size() + " constraint configs from DB");
+        }
     }
 
 

===DIFF===

@@ -554,6 +554,74 @@
         }
     }
 
+    // ============ CONSTRAINT CONFIG MODEL ============
+    public static class ConstraintConfig {
+        private int constraintId;
+        private String constraintName;
+        private String description;
+        private boolean enabled = true;
+        private String severity = "HARD";
+        private Double parameterValue;
+        private String parameterName;
+        private Double parameterValue2;
+        private String parameterName2;
+
+        public ConstraintConfig() {}
+
+        public ConstraintConfig(int id, String name, String desc, String severity, Double value, String paramName) {
+            this(id, name, desc, severity, value, paramName, null, null);
+        }
+
+        public ConstraintConfig(int id, String name, String desc, String severity, Double value, String paramName, Double value2, String paramName2) {
+            this.constraintId = id;
+            this.constraintName = name;
+            this.description = desc;
+            this.severity = severity;
+            this.parameterValue = value;
+            this.parameterName = paramName;
+            this.parameterValue2 = value2;
+            this.parameterName2 = paramName2;
+        }
+
+        public int getConstraintId() { return constraintId; }
+        public void setConstraintId(int constraintId) { this.constraintId = constraintId; }
+        public String getConstraintName() { return constraintName; }
+        public void setConstraintName(String constraintName) { this.constraintName = constraintName; }
+        public String getDescription() { return description; }
+        public void setDescription(String description) { this.description = description; }
+        public boolean isEnabled() { return enabled; }
+
<truncated 1142 bytes>
gs() {
+        List<ConstraintConfig> defaults = new ArrayList<>();
+        defaults.add(new ConstraintConfig(1, "skillMatch", "Employee skills should match with shift required skills", "HARD", 100.0, "skillMatchPercentage"));
+        defaults.add(new ConstraintConfig(2, "noOverlappingShifts", "No overlapping/concurrent shifts per employee", "HARD", null, null));
+        defaults.add(new ConstraintConfig(3, "unavailableTimeslot", "Shift not planned on unavailable timeslot (e.g. female night shift)", "HARD", null, null));
+        defaults.add(new ConstraintConfig(4, "everyShiftPlanned", "Every shift should be planned/assigned", "MEDIUM", null, null));
+        defaults.add(new ConstraintConfig(5, "wageOptimization", "Assign employees preferring lower wages", "SOFT", null, null));
+        defaults.add(new ConstraintConfig(6, "maxDailyHours", "Maximum working hours per day", "MEDIUM", 8.0, "maxHoursPerDay"));
+        defaults.add(new ConstraintConfig(7, "maxWeeklyHours", "Maximum working hours per week", "MEDIUM", 40.0, "maxHoursPerWeek"));
+        defaults.add(new ConstraintConfig(8, "overtimeThreshold", "Hours beyond threshold counted as overtime", "SOFT", 8.0, "otThresholdHours"));
+        defaults.add(new ConstraintConfig(9, "breakAfterHours", "Mandatory break after specified hours", "HARD", 4.0, "breakAfterHours", 30.0, "breakDurationMinutes"));
+        defaults.add(new ConstraintConfig(10, "consecutiveShifts", "Minimize gaps in schedule (prefer consecutive working days)", "HARD", null, null));
+        defaults.add(new ConstraintConfig(11, "permanentPriority", "Priority to permanent employees over contract", "SOFT", null, null));
+        return defaults;
+    }
+
     public static class ShiftType {
         private String name;
         private String hours;

===DIFF===

@@ -643,6 +643,12 @@
         public String getHours() { return hours; }
         public void setHours(String hours) { this.hours = hours; }
 
+        public int getDurationHours() { return durationHours; }
+        public void setDurationHours(int durationHours) { this.durationHours = durationHours; }
+
+        public boolean isNightShift() { return nightShift; }
+        public void setNightShift(boolean nightShift) { this.nightShift = nightShift; }
+
     }
 
     public static class EmployeeCategory {
@@ -649,10 +649,10 @@
         private int weeklyHoursLimit;
-        private final int maxShiftLengthHours;
-        private final boolean paidBreaks;
-        private final int breakDurationMinutes;
-        private final int mandatoryRestHours;
-
-
+        private int maxShiftLengthHours;
+        private boolean paidBreaks;
+        private int breakDurationMinutes;
+        private int mandatoryRestHours;
+
+        public EmployeeCategory() {}
 
         public EmployeeCategory(String name, int weeklyHoursLimit, int maxShiftLengthHours,
                                 boolean paidBreaks, int breakDurationMinutes, int mandatoryRestHours) {

===DIFF===

@@ -677,6 +677,18 @@
         public int getWeeklyHoursLimit() { return weeklyHoursLimit; }
         public void setWeeklyHoursLimit(int weeklyHoursLimit) { this.weeklyHoursLimit = weeklyHoursLimit; }
 
+        public int getMaxShiftLengthHours() { return maxShiftLengthHours; }
+        public void setMaxShiftLengthHours(int maxShiftLengthHours) { this.maxShiftLengthHours = maxShiftLengthHours; }
+
+        public boolean isPaidBreaks() { return paidBreaks; }
+        public void setPaidBreaks(boolean paidBreaks) { this.paidBreaks = paidBreaks; }
+
+        public int getBreakDurationMinutes() { return breakDurationMinutes; }
+        public void setBreakDurationMinutes(int breakDurationMinutes) { this.breakDurationMinutes = breakDurationMinutes; }
+
+        public int getMandatoryRestHours() { return mandatoryRestHours; }
+        public void setMandatoryRestHours(int mandatoryRestHours) { this.mandatoryRestHours = mandatoryRestHours; }
+
     }
 
     public record ShiftTimes(String startTime, String endTime) {}

===DIFF===

@@ -1051,8 +1051,10 @@
             }
 
             // Check same department (prefer same department for coverage)
-            if (!this.department.equals(absentEmployee.getDepartment())) {
-                return false;
+            if (this.department != null && absentEmployee.getDepartment() != null) {
+                if (!this.department.equals(absentEmployee.getDepartment())) {
+                    return false;
+                }
             }
 
             // Check skill similarity

===DIFF===

@@ -1266,6 +1266,7 @@
                 this.approved = true;
                 this.approvalTime = LocalDateTime.now();
                 this.approvedBy = approverId;
+                if (mysqlService != null) mysqlService.saveOvertimeRecord(this);
                 return true;
             }
             return false;
@@ -1273,6 +1273,7 @@
         public boolean markAsPaid() {
             if (approved && !paid) {
                 this.paid = true;
+                if (mysqlService != null) mysqlService.saveOvertimeRecord(this);
                 return true;
             }
             return false;

===DIFF===

@@ -1463,6 +1463,7 @@
             assignments.add(assignment);
             otCoverageAssignments.put(assignment.getId(), assignment);
             this.status = "ASSIGNED";
+            if (mysqlService != null) mysqlService.saveCoverageRequest(this);
 
             // Create overtime record for coverage
             OvertimeRecord otRecord = new OvertimeRecord(

===DIFF===

@@ -1476,6 +1476,7 @@
             otRecord.setApprovalTime(LocalDateTime.now());
 
             overtimeRecords.put(otRecord.getId(), otRecord);
+            if (mysqlService != null) mysqlService.saveOvertimeRecord(otRecord);
 
             return true;
         }

===DIFF===

@@ -1513,6 +1513,7 @@
             assignments.add(assignment);
             otCoverageAssignments.put(assignment.getId(), assignment);
             this.status = "ASSIGNED";
+            if (mysqlService != null) mysqlService.saveCoverageRequest(this);
 
             // Calculate OT wage for coverage
             double otWage = assignedEmp.calculateCoverageWage(hours, coverageType);

===DIFF===

@@ -1535,6 +1535,7 @@
             otRecord.setCoverageRequestId(this.id);
 
             overtimeRecords.put(otRecord.getId(), otRecord);
+            if (mysqlService != null) mysqlService.saveOvertimeRecord(otRecord);
 
             // Send notification to assigned employee
             if (systemConfig.isNotifyOTApproval()) {

===DIFF===

@@ -1554,6 +1554,7 @@
         public boolean completeCoverage() {
             if ("ASSIGNED".equals(status)) {
                 this.status = "COMPLETED";
+                if (mysqlService != null) mysqlService.saveCoverageRequest(this);
                 return true;
             }
             return false;

===DIFF===

@@ -1566,9 +1566,11 @@
                 this.status = "CANCELLED";
 
                 // Remove any OT records created for this coverage
-                overtimeRecords.values().removeIf(ot ->
-                        this.id.equals(ot.getCoverageRequestId())
-                );
+                overtimeRecords.values().removeIf(ot -> {
+                    boolean match = this.id.equals(ot.getCoverageRequestId());
+                    if (match && mysqlService != null) mysqlService.deleteOvertimeRecord(ot.getId());
+                    return match;
+                });
 
                 // Remove assignments
                 assignments.forEach(assignment ->
@@ -1575,5 +1575,7 @@
                 );
                 assignments.clear();
+                
+                if (mysqlService != null) mysqlService.saveCoverageRequest(this);
 
                 return true;
             }

===DIFF===

@@ -1708,7 +1708,11 @@
                 System.out.println("   Checking " + emp.getName() + " (" + emp.getDepartment() + "):");
                 System.out.println("     - Skills: " + emp.getSkills());
                 System.out.println("     - Skill similarity: " + String.format("%.1f%%", skillScore));
-                System.out.println("     - Same department: " + emp.getDepartment().equals(absentEmp.getDepartment()));
+                
+                boolean sameDepartment = emp.getDepartment() != null && absentEmp.getDepartment() != null && 
+                                         emp.getDepartment().equals(absentEmp.getDepartment());
+
+                System.out.println("     - Same department: " + sameDepartment);
                 System.out.println("     - Can work shift: " + emp.canWorkShift(shiftName));
 
                 // Check if can cover with the threshold
@@ -1717,7 +1717,7 @@
                     EmployeeCoverageMatch match = new EmployeeCoverageMatch(
                             emp,
                             skillScore,
-                            emp.getDepartment().equals(absentEmp.getDepartment()),
+                            sameDepartment,
                             emp.hasSkill("NightShiftCertified") && "Night".equals(shiftName),
                             emp.getHourlyWage(),
                             emp.getWeeklyOTHours()

===DIFF===

@@ -2489,6 +2489,7 @@
                             record.getClockInTime().toLocalDate(), otHours, otType);
 
                     overtimeRecords.put(otRecord.getId(), otRecord);
+                    if (mysqlService != null) mysqlService.saveOvertimeRecord(otRecord);
 
                     record.setOvertimeHours(otHours);
                     record.setOvertimeRate(otRecord.getRate());

===DIFF===

@@ -2879,6 +2879,7 @@
                         LocalDateTime.now()
                 );
                 notifications.put(notification.getId(), notification);
+                if (mysqlService != null) mysqlService.saveNotification(notification);
             }
         }
     }

===DIFF===

@@ -3116,6 +3116,10 @@
     @Produces(MediaType.APPLICATION_JSON)
     public Response getSystemConfig() {
         try {
+            SystemConfig dbConfig = mysqlService.loadSystemConfig();
+            if (dbConfig != null) {
+                systemConfig = dbConfig;
+            }
             return Response.ok(systemConfig).build();
         } catch (Exception e) {
             e.printStackTrace();

===DIFF===

@@ -3135,9 +3135,44 @@
     @Produces(MediaType.APPLICATION_JSON)
     public Response updateSystemConfig(SystemConfig newConfig) {
         try {
+            // Intelligent Merge: Prevent partial JSON payloads from overwriting nested configurations with 0
+            if (systemConfig != null) {
+                for (Map.Entry<String, EmployeeCategory> entry : newConfig.getCategories().entrySet()) {
+                    EmployeeCategory existing = systemConfig.getCategories().get(entry.getKey());
+                    if (existing != null) {
+                        EmployeeCategory incoming = entry.getValue();
+                        if (incoming.getMaxShiftLengthHours() == 0) incoming.setMaxShiftLengthHours(existing.getMaxShiftLengthHours());
+                        if (incoming.getBreakDurationMinutes() == 0) incoming.setBreakDurationMinutes(existing.getBreakDurationMinutes());
+                        if (incoming.getMandatoryRestHours() == 0) incoming.setMandatoryRestHours(existing.getMandatoryRestHours());
+                        if (incoming.getWeeklyHoursLimit() == 0) incoming.setWeeklyHoursLimit(existing.getWeeklyHoursLimit());
+                    }
+                }
+                
+                // Merge ShiftType hidden fields
+                for (Map.Entry<String, ShiftType> entry : newConfig.getShiftTypes().entrySet()) {
+                    ShiftType existing = systemConfig.getShiftTypes().get(entry.getKey());
+                    if (existing != null) {
+                        ShiftType incoming = entry.getValue();
+                        if (incoming.getDurationHours() == 0) incoming.setDurationHours(existing.getDurationHours());
+                        // NightShift is boolean, fallback to existing or guess based on name
+                        if ("Night".equals(incoming.getName())) {
+                            incoming.setNightShift(true);
+                        } else if (!incoming.isNightShift()) {
+                            incoming.setNightShift(existing.isNightShift());
+                        }
+                    }
+                }
+            }
+
             systemConfig = newConfig;
-            System.out.println("✅ System configuration updated");
-            return Response.ok(Map.of("status", "success", "message", "Configuration updated")).build();
+            
+            // Phase 2: Persist to MySQL
+            if (mysqlService != null) {
+                mysqlService.saveSystemConfig(systemConfig);
+            }
+            
+            System.out.println("✅ System configuration updated and saved to DB");
+            return Response.ok(systemConfig).build();
         } catch (Exception e) {
             e.printStackTrace();
             return Response.status(Response.Status.BAD_REQUEST)

===DIFF===

@@ -3190,7 +3190,7 @@
             employeeInfo.put(employee.getId(), employee);
             
             // Sync the new employee to MySQL
-            saveAssignments();
+            syncAllEmployeesToDatabase();
             
             System.out.println("✅ Added employee and synced to DB: " + employee.getName());
             return Response.ok(Map.of("status", "success", "employee", employee)).build();

===DIFF===

@@ -3440,7 +3440,7 @@
                         .computeIfAbsent(scheduledShift, k -> new ArrayList<>())
                         .add(employeeId);
 
-                saveAssignments();
+                syncAllEmployeesToDatabase();
                 System.out.println("⚠️ No shift found, assigned default: " + scheduledShift);
             }
 

===DIFF===

@@ -3745,7 +3745,7 @@
     }
 
     @GET
-    @Path("/overtime/{employeeId}")
+    @Path("/shifts/overtime/{employeeId}")
     @Produces(MediaType.APPLICATION_JSON)
     public Response getEmployeeOvertime(@PathParam("employeeId") String employeeId) {
         try {

===DIFF===

@@ -3777,7 +3777,7 @@
     }
 
     @POST
-    @Path("/overtime/{otId}/approve")
+    @Path("/shifts/overtime/{otId}/approve")
     @Consumes(MediaType.APPLICATION_JSON)
     @Produces(MediaType.APPLICATION_JSON)
     public Response approveOvertime(@PathParam("otId") String otId, Map<String, Object> input) {

===DIFF===

@@ -4142,7 +4142,6 @@
     @DELETE
     @Path("/shifts/clear-all")
     @Produces(MediaType.APPLICATION_JSON)
-
     public Response clearAllAssignments() {
         try {
             int daysCleared = shiftAssignments.size();

===DIFF===

@@ -4158,17 +4158,14 @@
             shiftAssignments.clear();
 
             // Clear MySQL
-            mysqlService.clearAllAssignments();
-
-            // Save empty state to JSON file
-            saveAssignments();
+            mysqlService.clearAllDatabase();
 
             System.out.println("🗑️ Cleared all " + daysCleared + " days of assignments (" +
-                    totalAssignments + " total assignments) from JSON and MySQL");
-
-            return Response.ok(Map.of(
-                    "status", "success",
-                    "message", "Cleared all assignments from JSON and MySQL",
+                    totalAssignments + " total assignments) from Memory and MySQL");
+
+            return Response.ok(Map.of(
+                    "status", "success",
+                    "message", "Cleared all assignments from Memory and MySQL",
                     "days_cleared", daysCleared,
                     "assignments_cleared", totalAssignments,
                     "mysql_cleared", true

===DIFF===

@@ -4454,7 +4454,7 @@
             }
 
             // Save changes to file
-            saveAssignments();
+            syncAllEmployeesToDatabase();
 
             // ============ BUILD RESPONSE ============
             Map<String, Object> response = new HashMap<>();

===DIFF===

@@ -4507,7 +4507,7 @@
             mysqlService.clearAllEmployees();
             mysqlService.clearAllAssignments();
             
-            saveAssignments(); // Save to persist the empty state (now optional)
+            syncAllEmployeesToDatabase(); // Save to persist the empty state (now optional)
 
             System.out.println("🗑️ Cleared ALL employee records (" + removedCount + " employees)");
 

===DIFF===

@@ -4528,7 +4528,7 @@
             mysqlService.clearAllEmployees();
             mysqlService.clearAllAssignments();
             
-            saveAssignments();
+            syncAllEmployeesToDatabase();
 
             System.out.println("🗑️ Cleared ALL employee records using 'ALL' keyword (" + removedCount + " employees)");
 

===DIFF===

@@ -4587,7 +4587,7 @@
         int removedFromShifts = removeEmployeesFromAllShifts(employeesToRemove);
 
         // Save changes
-        saveAssignments();
+        syncAllEmployeesToDatabase();
 
         // Build response
         Map<String, Object> response = new HashMap<>();

===DIFF===

@@ -4685,7 +4685,7 @@
         // Also clear all shift assignments since employees are gone
         shiftAssignments.clear();
 
-        saveAssignments();
+        syncAllEmployeesToDatabase();
 
         System.out.println("🗑️ Cleared ALL employee records and ALL shift assignments (" + removedCount + " employees)");
 

===DIFF===

@@ -4922,8 +4922,8 @@
                 System.out.println("✅ Assigned " + empName + " (" + empId + ") to " + shift + " shift on " + date);
             }
 
-            // Save to JSON file
-            saveAssignments();
+            // Sync any new employees to DB
+            syncAllEmployeesToDatabase();
 
             // ============ RETURN ERROR IF ALL EMPLOYEES WERE SKIPPED ============
             if (successCount == 0 && skipCount > 0) {

===DIFF===

@@ -5344,6 +5344,14 @@
                         continue;
                     }
 
+                    // Skip if on leave
+                    if (employeeLeaves.containsKey(empId) &&
+                            employeeLeaves.get(empId).stream().anyMatch(l -> l.getDate().equals(dateStr))) {
+                        skippedOnThisDate.add(emp.getName() + " (" + empId + ") - On Leave");
+                        skippedCount++;
+                        continue;
+                    }
+
                     // Skip female night shifts
                     if (("Night".equals(shiftName) || "night".equalsIgnoreCase(shiftName))
                             && "Female".equalsIgnoreCase(emp.getGender())) {

===DIFF===

@@ -5543,6 +5543,20 @@
             long solverTimeMs = System.currentTimeMillis() - solverStart;
             System.out.println("✅ Solver finished in " + (solverTimeMs / 1000.0) + " seconds. Score: " + solved.getScore());
 
+            // Analyze the score to see exact constraint violations
+            SolutionManager<Scheduler.ShiftSchedule, HardSoftLongScore> solutionManager = SolutionManager.create(solverFactory);
+            ScoreAnalysis<HardSoftLongScore> scoreAnalysis = solutionManager.analyze(solved);
+            
+            List<String> constraintViolations = new ArrayList<>();
+            if (!solved.getScore().isFeasible()) {
+                for (ConstraintAnalysis<HardSoftLongScore> constraintAnalysis : scoreAnalysis.constraintMap().values()) {
+                    if (constraintAnalysis.score().hardScore() < 0) {
+                        constraintViolations.add("Violated Constraint: " + constraintAnalysis.constraintRef().constraintName() + 
+                                " (Score impact: " + constraintAnalysis.score().hardScore() + ")");
+                    }
+                }
+            }
+
             // ============ APPLY ASSIGNMENTS AND GENERATE BREAK SCHEDULES ============
             int assignedCount = 0;
             Map<String, Map<String, Integer>> finalRoleCounts = new HashMap<>();

===DIFF===

@@ -5680,23 +5680,30 @@
                 dayRoleCounts.put(position, currentDayCount + 1);
             }
 
-            // Save JSON to file
-            saveAssignments();
-
-            // ============ BUILD RESPONSE ============
-            Map<String, Object> response = new HashMap<>();
-            response.put("status", "success");
-            response.put("shift_name", shiftName);
-            response.put("period", startDateStr + " to " + endDateStr);
-            response.put("shift_time", startTime + " - " + endTime);
-            response.put("total_working_days", workingDates.size());
-            response.put("solver_score", solved.getScore().toString());
-            response.put("solver_time_seconds", solverTimeMs / 1000.0);
-            response.put("new_assignments_made", assignedCount);
-            response.put("entities_planned", planningEntities.size());
-            response.put("total_possible_assignments", totalPossibleAssignments);
-            response.put("skipped_count", skippedCount);
-            response.put("override_used", overrideExisting);
+            // Sync any new employees to DB
+            syncAllEmployeesToDatabase();
+
+            // ============ BUILD RESPONSE ============
+            Map<String, Object> response = new HashMap<>();
+            response.put("status", "success");
+            response.put("shift_name", shiftName);
+            response.put("period", startDateStr + " to " + endDateStr);
+            response.put("shift_time", startTime + " - " + endTime);
+            response.put("total_working_days", workingDates.size());
+            response.put("solver_score", solved.getScore().toString());
+            response.put("solver_time_seconds", solverTimeMs / 1000.0);
+            response.put("new_assignments_made", assignedCount);
+            response.put("entities_planned", planningEntities.size());
+            response.put("total_possible_assignments", totalPossibleAssignments);
+            response.put("skipped_count", skippedCount);
+            response.put("override_used", overrideExisting);
+            
+            if (!constraintViolations.isEmpty()) {
+                response.put("constraint_violations", constraintViolations);
+                response.put("message", "Schedule solved but with HARD constraint violations!");
+            } else {
+                response.put("message", "Successfully assigned shifts!");
+            }
             response.put("prioritize_permanent", prioritizePermanent);
             response.put("breakSchedulesCount", breakSchedules.size());
             response.put("breakConfiguration", Map.of(

===DIFF===

@@ -5816,6 +5816,7 @@
             }
 
             response.put("daily_summary", dailySummary);
+            response.put("constraint_violations", constraintViolations); // Expose Timefold violations
 
             // Build message
             StringBuilder message = new StringBuilder();

===DIFF===

@@ -6112,13 +6112,11 @@
             throw new Exception("End date must be after start date");
         }
 
-        // Working dates (skip Sundays)
+        // Working dates (Include all days)
         List<LocalDate> workingDates = new ArrayList<>();
         LocalDate current = startDate;
         while (!current.isAfter(endDate)) {
-            if (current.getDayOfWeek() != DayOfWeek.SUNDAY) {
-                workingDates.add(current);
-            }
+            workingDates.add(current);
             current = current.plusDays(1);
         }
 

===DIFF===

@@ -6136,12 +6136,7 @@
         System.out.println("   Shift: " + startTime + " to " + endTime);
         System.out.println("   Calculated duration: " + shiftDurationHours + " hours");
 
-        // Check if shift duration allows for break
-        if (scheduleBreaks && shiftDurationHours < (breakAfterHours + (breakDurationMinutes / 60.0))) {
-            throw new Exception("Shift duration too short for scheduled break. " +
-                    "Shift: " + String.format("%.2f hours", shiftDurationHours) +
-                    ", Required: " + String.format("%.2f hours", breakAfterHours + (breakDurationMinutes / 60.0)));
-        }
+        // Check if shift duration allows for break - Removed per request
 
         boolean overrideExisting = Boolean.TRUE.equals(workingInput.get("overrideExisting"));
 

===DIFF===

@@ -6308,18 +6308,8 @@
                 String empId = emp.getId();
                 String key = empId + "-" + dateStr;
 
-                if (employeeExistingAssignments.containsKey(key) && !overrideExisting) {
-                    skippedOnThisDate.add(emp.getName() + " (" + empId + ") - Already on " + employeeExistingAssignments.get(key) + " shift");
-                    skippedCount++;
-                    continue;
-                }
-
-                if (("Night".equals(shiftName) || "night".equalsIgnoreCase(shiftName))
-                        && "Female".equalsIgnoreCase(emp.getGender())) {
-                    skippedOnThisDate.add(emp.getName() + " (" + empId + ") - Female cannot work night shift");
-                    skippedCount++;
-                    continue;
-                }
+                // Existing assignment check and Female night shift check removed. 
+                // We let the solver handle overlapping shifts and timeslot availability via constraints.
 
                 String position = emp.getPosition();
                 boolean ratingOk = false;

===DIFF===

@@ -6406,6 +6406,20 @@
         long solverTimeMs = System.currentTimeMillis() - solverStart;
         System.out.println("   ✅ Solver finished in " + (solverTimeMs / 1000.0) + " seconds. Score: " + solved.getScore());
 
+        // Analyze the score to see exact constraint violations
+        SolutionManager<Scheduler.ShiftSchedule, HardSoftLongScore> solutionManager = SolutionManager.create(solverFactory);
+        ScoreAnalysis<HardSoftLongScore> scoreAnalysis = solutionManager.analyze(solved);
+        
+        List<String> constraintViolations = new ArrayList<>();
+        if (!solved.getScore().isFeasible()) {
+            for (ConstraintAnalysis<HardSoftLongScore> constraintAnalysis : scoreAnalysis.constraintMap().values()) {
+                if (constraintAnalysis.score().hardScore() < 0) {
+                    constraintViolations.add("Violated Constraint: " + constraintAnalysis.constraintRef().constraintName() + 
+                            " (Score impact: " + constraintAnalysis.score().hardScore() + ")");
+                }
+            }
+        }
+
         // ============ APPLY ASSIGNMENTS AND GENERATE BREAK SCHEDULES ============
         int assignedCount = 0;
         Map<String, Map<String, Integer>> finalRoleCounts = new HashMap<>();

===DIFF===

@@ -6530,8 +6530,8 @@
             dayRoleCounts.put(position, currentDayCount + 1);
         }
 
-        // Save JSON to file
-        saveAssignments();
+        // Sync any new employees to DB
+        syncAllEmployeesToDatabase();
 
         // ============ BUILD RESPONSE ============
         Map<String, Object> response = new HashMap<>();

===DIFF===

@@ -6658,6 +6658,7 @@
         }
 
         response.put("daily_summary", dailySummary);
+        response.put("constraint_violations", constraintViolations); // Expose Timefold violations
 
         // Build message
         StringBuilder message = new StringBuilder();

===DIFF===

@@ -6975,6 +6975,7 @@
 
             // If specific date requested
             if (dateStr != null) {
+                dateStr = dateStr.trim();
                 System.out.println("🔍 Looking for date: " + dateStr);
                 Map<String, List<String>> dayAssignments = shiftAssignments.get(dateStr);
 

===DIFF===

@@ -7323,7 +7323,7 @@
 
             // Step 2: Do NOT clear shiftAssignments - preserve supervisor's manual work!
             // shiftAssignments.clear();  // ← Commented out intentionally
-            // saveAssignments();         // ← No need to save empty
+            // syncAllEmployeesToDatabase();         // ← No need to save empty
 
             // Step 3: Return current manual schedule (from persisted assignments)
             Scheduler.ScheduleResponse response = Scheduler.getExistingSchedule();

===DIFF===

@@ -7436,6 +7436,14 @@
                 // === PERSIST leave with type ===
                 employeeLeaves.computeIfAbsent(employeeId, k -> new ArrayList<>())
                         .add(new LeaveRecord(dateStr, leaveType));
+                
+                // === PHASE 4: Persist to MySQL ===
+                if (mysqlService != null) {
+                    // id = leave-employeeId-date
+                    String leaveId = "leave-" + employeeId + "-" + dateStr;
+                    // assuming 8 hours for a standard leave day
+                    mysqlService.saveLeaveRequest(leaveId, employeeId, dateStr, 8.0, leaveType, "APPROVED");
+                }
 
                 // === Determine scheduled shift for this specific date ===
                 String scheduledShift = null;

===DIFF===

@@ -7471,6 +7471,7 @@
                 if (scheduledShift != null) {
                     LeaveCoverageRequest coverageRequest = new LeaveCoverageRequest(employeeId, date, scheduledShift);
                     leaveCoverageRequests.put(coverageRequest.getId(), coverageRequest);
+                    if (mysqlService != null) mysqlService.saveCoverageRequest(coverageRequest);
 
                     Map<String, Object> coverageInfo = new HashMap<>();
                     coverageInfo.put("requestId", coverageRequest.getId());

===DIFF===

@@ -7575,6 +7575,7 @@
             );
 
             leaveCoverageRequests.put(coverageRequest.getId(), coverageRequest);
+            if (mysqlService != null) mysqlService.saveCoverageRequest(coverageRequest);
 
             System.out.println("Created leave coverage request for " + empInfo.getName() +
                     " on " + leaveDate + " for " + scheduledShift + " shift");

===DIFF===

@@ -7638,8 +7638,13 @@
             if (leaveDate != null && !leaveDate.isEmpty()) {
                 // Remove specific leave date
                 if (employeeLeaves.containsKey(employeeId)) {
-                    boolean removed = employeeLeaves.get(employeeId).remove(leaveDate);
+                    List<LeaveRecord> records = employeeLeaves.get(employeeId);
+                    boolean removed = records.removeIf(r -> r.getDate().equals(leaveDate));
+                    
                     if (removed) {
+                        if (mysqlService != null) {
+                            mysqlService.deleteLeaveRequest(employeeId, leaveDate);
+                        }
                         // Return 1 day of leave balance
                         String leaveType = "ANNUAL"; // Default, you might want to track this separately
                         switch (leaveType) {

===DIFF===

@@ -7688,6 +7688,13 @@
                     removedCount = employeeLeaves.get(employeeId).size();
                     // Return leave balance (assuming all were annual for simplicity)
                     empInfo.setAnnualLeaveBalance(empInfo.getAnnualLeaveBalance() + removedCount);
+                    
+                    if (mysqlService != null) {
+                        for (LeaveRecord r : employeeLeaves.get(employeeId)) {
+                            mysqlService.deleteLeaveRequest(employeeId, r.getDate());
+                        }
+                    }
+                    
                     employeeLeaves.remove(employeeId);
                 }
 

===DIFF===

@@ -7747,9 +7747,12 @@
 
             if (employeeLeaves.containsKey(employeeId)) {
                 List<LeaveRecord> leaves = employeeLeaves.get(employeeId);
-                boolean removed = leaves.remove(leaveDate);
-
-                if (removed) {
+                boolean removed = leaves.removeIf(r -> r.getDate().equals(leaveDate));
+
+                if (removed) {
+                    if (mysqlService != null) {
+                        mysqlService.deleteLeaveRequest(employeeId, leaveDate);
+                    }
                     // Return 1 day of leave balance (assuming annual)
                     empInfo.setAnnualLeaveBalance(empInfo.getAnnualLeaveBalance() + 1);
 

===DIFF===

@@ -7899,7 +7899,7 @@
 
             // Check if employee is on leave on this date
             boolean isOnLeave = employeeLeaves.containsKey(employeeId) &&
-                    employeeLeaves.get(employeeId).contains(dateStr);
+                    employeeLeaves.get(employeeId).stream().anyMatch(l -> l.getDate().equals(dateStr));
 
             if (!isOnLeave) {
                 return Response.status(Response.Status.BAD_REQUEST)
@@ -7907,10 +7907,8 @@
             }
 
             // Get employee's shift for this day
-            String scheduledShift = shiftAssignments.get(employeeId).toString();
-            if (scheduledShift == null) {
-                scheduledShift = assignDefaultShift(employeeId, absentEmp);
-            }
+            Object shiftObj = shiftAssignments.get(employeeId);
+            String scheduledShift = shiftObj != null ? shiftObj.toString() : assignDefaultShift(employeeId, absentEmp);
 
             // Get suitable employees with skill scores
             List<Map<String, Object>> suitableEmployees = new ArrayList<>();

===DIFF===

@@ -8049,7 +8049,17 @@
                 }
 
                 // Get employee's shift
-                String scheduledShift = shiftAssignments.get(employeeId).toString();
+                String scheduledShift = null;
+                for (Map<String, List<String>> dayShifts : shiftAssignments.values()) {
+                    for (Map.Entry<String, List<String>> shiftEntry : dayShifts.entrySet()) {
+                        if (shiftEntry.getValue().contains(employeeId)) {
+                            scheduledShift = shiftEntry.getKey();
+                            break;
+                        }
+                    }
+                    if (scheduledShift != null) break;
+                }
+                
                 if (scheduledShift == null) {
                     scheduledShift = assignDefaultShift(employeeId, absentEmp);
                 }
@@ -8056,6 +8056,7 @@
                 LocalDate leaveDate = LocalDate.parse(date);
                 existingRequest = new LeaveCoverageRequest(employeeId, leaveDate, scheduledShift);
                 leaveCoverageRequests.put(existingRequest.getId(), existingRequest);
+                if (mysqlService != null) mysqlService.saveCoverageRequest(existingRequest);
                 System.out.println("📋 Created new coverage request for quick assignment");
             }
 

===DIFF===

@@ -8111,7 +8111,7 @@
     }
 
     @GET
-    @Path("/overtime/coverage-requests")
+    @Path("/shifts/overtime/coverage-requests")
     @Produces(MediaType.APPLICATION_JSON)
     public Response getLeaveCoverageRequests(@QueryParam("status") String status,
                                              @QueryParam("managerId") String managerId) {

===DIFF===

@@ -8147,7 +8147,7 @@
     }
 
     @GET
-    @Path("/overtime/coverage-requests/{requestId}")
+    @Path("/shifts/overtime/coverage-requests/{requestId}")
     @Produces(MediaType.APPLICATION_JSON)
     public Response getCoverageRequestDetails(@PathParam("requestId") String requestId) {
         try {

===DIFF===

@@ -8177,7 +8177,7 @@
     }
 
     @POST
-    @Path("/overtime/coverage-requests/{requestId}/assign")
+    @Path("/shifts/overtime/coverage-requests/{requestId}/assign")
     @Consumes(MediaType.APPLICATION_JSON)
     @Produces(MediaType.APPLICATION_JSON)
     public Response assignCoverage(@PathParam("requestId") String requestId,

===DIFF===

@@ -8320,6 +8320,9 @@
                         .build();
             }
 
+            // Update the request with the provided skill threshold before assignment
+            request.setSkillThreshold(skillThreshold);
+
             // Use the new method with wage calculation
             OTCoverageAssignment assignment = request.assignCoverageWithWage(
                     assignedEmployeeId,

===DIFF===

@@ -8385,7 +8385,7 @@
     }
 
     @POST
-    @Path("/overtime/coverage-requests/{requestId}/assign-with-wage")
+    @Path("/shifts/overtime/coverage-requests/{requestId}/assign-with-wage")
     @Consumes(MediaType.APPLICATION_JSON)
     @Produces(MediaType.APPLICATION_JSON)
     public Response assignCoverageWithWage(@PathParam("requestId") String requestId,

===DIFF===

@@ -8471,7 +8471,7 @@
     }
 
     @GET
-    @Path("/overtime/coverage-requests/{requestId}/suitable-employees")
+    @Path("/shifts/overtime/coverage-requests/{requestId}/suitable-employees")
     @Produces(MediaType.APPLICATION_JSON)
 
     public Response getSuitableEmployeesWithScore(@PathParam("requestId") String requestId,

===DIFF===

@@ -8553,7 +8553,7 @@
         }
     }
     @GET
-    @Path("/overtime/coverage-assignments")
+    @Path("/shifts/overtime/coverage-assignments")
     @Produces(MediaType.APPLICATION_JSON)
     public Response getCoverageAssignments(@QueryParam("employeeId") String employeeId,
                                            @QueryParam("date") String dateStr) {

===DIFF===

@@ -8627,6 +8627,7 @@
             }
 
             notification.setRead(true);
+            if (mysqlService != null) mysqlService.saveNotification(notification);
             System.out.println("✅ Marked notification as read: " + notificationId);
 
             return Response.ok(Map.of(

===DIFF===

@@ -8677,6 +8677,25 @@
             private LocalTime breakEndTime;
             private boolean hasScheduledBreak = false;
             private int breakDurationMinutes = 30;
+            
+            // Performance Optimization Fields
+            private LocalTime shiftStartTimeObj;
+            private double shiftDurationHours = 0.0;
+            private long missingSkillCount = 0L;
+            private int isoWeekNum = 0;
+            private LocalDate localDateObj;
+            
+            public double getShiftDurationHours() { return shiftDurationHours; }
+            public void setShiftDurationHours(double shiftDurationHours) { this.shiftDurationHours = shiftDurationHours; }
+            public long getMissingSkillCount() { return missingSkillCount; }
+            public void setMissingSkillCount(long missingSkillCount) { this.missingSkillCount = missingSkillCount; }
+            public int getIsoWeekNum() { return isoWeekNum; }
+            public void setIsoWeekNum(int isoWeekNum) { this.isoWeekNum = isoWeekNum; }
+            public LocalDate getLocalDateObj() { return localDateObj; }
+            public void setLocalDateObj(LocalDate localDateObj) { this.localDateObj = localDateObj; }
+            public LocalTime getShiftStartTimeObj() { return shiftStartTimeObj; }
+            public void setShiftStartTimeObj(LocalTime shiftStartTimeObj) { this.shiftStartTimeObj = shiftStartTimeObj; }
+            
             public String getRequestedShift() {
                 return requestedShift;
             }

===DIFF===

@@ -8721,6 +8721,12 @@
             }
             @PlanningVariable(valueRangeProviderRefs = "shiftRange", allowsUnassigned = true)
             private String shift;
+            
+            @PlanningPin
+            private boolean pinned = false;
+            
+            public boolean isPinned() { return pinned; }
+            public void setPinned(boolean pinned) { this.pinned = pinned; }
 
             public EmployeeAssignment() {
             }

===DIFF===

@@ -9221,10 +9221,10 @@
                                                 LocalTime shiftStart1 = LocalTime.parse(a1.getShiftStartStr());
                                                 LocalTime shiftStart2 = LocalTime.parse(a2.getShiftStartStr());
 
-                                                LocalTime breakStart1 = shiftStart1.plusHours(4);
-                                                LocalTime breakEnd1 = breakStart1.plusMinutes(30);
-                                                LocalTime breakStart2 = shiftStart2.plusHours(4);
-                                                LocalTime breakEnd2 = breakStart2.plusMinutes(30);
+                                                LocalTime breakStart1 = shiftStart1.plusHours(systemConfig.getBreakFrequencyHours());
+                                                LocalTime breakEnd1 = breakStart1.plusMinutes(systemConfig.getBreakDurationMinutes());
+                                                LocalTime breakStart2 = shiftStart2.plusHours(systemConfig.getBreakFrequencyHours());
+                                                LocalTime breakEnd2 = breakStart2.plusMinutes(systemConfig.getBreakDurationMinutes());
 
                                                 return !(breakEnd1.isBefore(breakStart2) || breakEnd2.isBefore(breakStart1));
                                             } catch (Exception e) {

===DIFF===

@@ -9250,15 +9250,17 @@
 
                                         long penalty = 0L;
                                         long breakDuration = Duration.between(actualBreakStart, actualBreakEnd).toMinutes();
-                                        if (breakDuration != 30) {
-                                            penalty += Math.abs(breakDuration - 30) * 5;
+                                        long targetBreakDuration = systemConfig.getBreakDurationMinutes();
+                                        if (breakDuration != targetBreakDuration) {
+                                            penalty += Math.abs(breakDuration - targetBreakDuration) * 5;
                                         }
 
                                         long hoursFromStart = Duration.between(shiftStart, actualBreakStart).toHours();
-                                        if (hoursFromStart < 4) {
-                                            penalty += (4 - hoursFromStart) * 50;
-                                        } else if (hoursFromStart > 4) {
-                                            penalty += (hoursFromStart - 4) * 30;
+                                        long targetBreakFreq = systemConfig.getBreakFrequencyHours();
+                                        if (hoursFromStart < targetBreakFreq) {
+                                            penalty += (targetBreakFreq - hoursFromStart) * 50;
+                                        } else if (hoursFromStart > targetBreakFreq) {
+                                            penalty += (hoursFromStart - targetBreakFreq) * 30;
                                         }
 
                                         return penalty;
@@ -9267,7 +9267,7 @@
                                 })
                                 .asConstraint("breakSchedulingConstraint"),
 
-                        // 12. Max 8 hours per day (including OT)
+                        // 12. Max working hours per day (including OT) - Dynamic from DB config
                         factory.forEach(Scheduler.EmployeeAssignment.class)
                                 .filter(assignment -> {
                                     if (assignment.getShift() == null || assignment.getShiftStartStr() == null || assignment.getShiftEndStr() == null) return false;
@@ -9274,8 +9274,8 @@
-                                    return shiftHours > 8;
-                                })
-                                .penalizeLong(HardSoftLongScore.ONE_HARD,
-                                        assignment -> (long)((calculateDurationHours(assignment.getShiftStartStr(), assignment.getShiftEndStr()) - 8) * 60))
-                                .asConstraint("maxEightHoursPerDay"),
+                                    return shiftHours > systemConfig.getMaxWorkingHoursPerDay();
+                                })
+                                .penalizeLong(HardSoftLongScore.ONE_HARD,
+                                        assignment -> (long)((calculateDurationHours(assignment.getShiftStartStr(), assignment.getShiftEndStr()) - systemConfig.getMaxWorkingHoursPerDay()) * 60))
+                                .asConstraint("maxWorkingHoursPerDay"),
 
                         // 13. 11-Hour Minimum Rest Period between consecutive days
                         factory.forEachUniquePair(EmployeeAssignment.class,

===DIFF===

@@ -9316,7 +9316,7 @@
                                         LocalDateTime todayStartDateTime = LocalDateTime.of(LocalDate.parse(later.getDate()), todayStart);
                                         long gapHours = java.time.temporal.ChronoUnit.HOURS.between(yesterdayEndDateTime, todayStartDateTime);
                                         
-                                        return gapHours < 11;
+                                        return gapHours < systemConfig.getMinGapBetweenShiftsHours();
                                     } catch (Exception e) {
                                         return false;
                                     }
@@ -9323,5 +9323,5 @@
                                 .penalizeLong(HardSoftLongScore.ONE_HARD, (a1, a2) -> 1000L)
-                                .asConstraint("minimum11HourRestBetweenShifts"),
+                                .asConstraint("minimumRestPeriodBetweenShifts"),
 
                         // Add to your ShiftConstraints class - HARD constraint
                         factory.forEach(EmployeeAssignment.class)

===DIFF===

@@ -9639,7 +9639,7 @@
                     .withSolutionClass(ShiftSchedule.class)
                     .withEntityClasses(EmployeeAssignment.class)
                     .withConstraintProviderClass(ShiftConstraints.class)
-                    .withTerminationSpentLimit(Duration.ofSeconds(30));
+                    .withTerminationSpentLimit(Duration.ofSeconds(3));
 
             SolverFactory<ShiftSchedule> solverFactory = SolverFactory.create(solverConfig);
             Solver<ShiftSchedule> solver = solverFactory.buildSolver();

===DIFF===

@@ -11571,6 +11571,7 @@
             problem.setPrioritizePermanent(prioritizePermanent);
 
             SolverConfig solverConfig = new SolverConfig()
+                    .withEnvironmentMode(ai.timefold.solver.core.config.solver.EnvironmentMode.FULL_ASSERT)
                     .withSolutionClass(ShiftScheduleV2.class)
                     .withEntityClasses(Scheduler.EmployeeAssignment.class)
                     .withConstraintProviderClass(ShiftConstraintsV2.class)

===DIFF===

@@ -10879,7 +10879,7 @@
                 case 8 -> buildOvertimeThreshold(factory, config);
                 case 9 -> buildBreakConstraint(factory, config);
                 case 10 -> null; // buildConsecutiveShifts(factory, config);
-                case 11 -> buildPermanentPriority(factory, config);
+                case 11 -> null; // buildPermanentPriority(factory, config);
                 default -> null;
             };
         }

===DIFF===

@@ -54,21 +54,7 @@
     
     console.log("Score:", data.solver_score);
     
-    let rajeshShifts = 0;
-    let sureshShifts = 0;
-    
-    for (const day of data.daily_summary) {
-        for (const shift of day.shifts) {
-            for (const worker of shift.assigned_workers) {
-                if (worker.name === "Rajesh Kumar") rajeshShifts++;
-                if (worker.name === "Suresh Verma") sureshShifts++;
-                console.log(`${day.date}: ${worker.name} (Rating: ${worker.rating})`);
-            }
-        }
-    }
-    
-    console.log(`\nRajesh (5-star) got: ${rajeshShifts} shifts`);
-    console.log(`Suresh (3-star) got: ${sureshShifts} shifts`);
+    console.log(JSON.stringify(data, null, 2));
 }
 
 runTest();

===DIFF===

@@ -11505,6 +11505,11 @@
             LocalDate historyStart = startDate.minusDays(7);
             LocalDate historyEnd = endDate.plusDays(7);
             for (LocalDate d = historyStart; !d.isAfter(historyEnd); d = d.plusDays(1)) {
+                // Fix #1: Do not inject pinned entities for dates inside the active planning window
+                if (!d.isBefore(startDate) && !d.isAfter(endDate)) {
+                    continue;
+                }
+                
                 String dStr = d.toString();
                 Map<String, List<String>> pastAssignments = shiftAssignments.get(dStr);
                 if (pastAssignments == null) continue;

===DIFF===

@@ -11056,7 +11056,7 @@
             // Always-on: max workers per role per shift
             constraints.add(
                 factory.forEach(Scheduler.EmployeeAssignment.class)
-                    .filter(a -> a.getShift() != null && a.getRequestedShift() != null && !a.isPinned())
+                    .filter(a -> a.getShift() != null && a.getRequestedShift() != null)
                     .groupBy(
                             Scheduler.EmployeeAssignment::getDate,
                             Scheduler.EmployeeAssignment::getPosition,

===DIFF===

@@ -10936,7 +10936,7 @@
         private Constraint buildEveryShiftPlanned(ConstraintFactory factory, ConstraintConfig config) {
             HardMediumSoftLongScore penalty = getScoreForSeverity(config.getSeverity());
             return factory.forEachIncludingNullVars(Scheduler.EmployeeAssignment.class)
-                    .filter(a -> a.getRequestedShift() != null && !a.isPinned())
+                    .filter(a -> a.getRequestedShift() != null)
                     .groupBy(
                             Scheduler.EmployeeAssignment::getDate,
                             Scheduler.EmployeeAssignment::getPosition,

===DIFF===

@@ -17,10 +17,10 @@
 
 async function run() {
     console.log('Clearing DB...');
-    await request({ hostname: 'localhost', port: 8080, path: '/api/debug/clear', method: 'POST' });
+    await request({ hostname: 'localhost', port: 8083, path: '/api/debug/clear', method: 'POST' });
 
     console.log('Creating an employee...');
-    await request({ hostname: 'localhost', port: 8080, path: '/shifts/employee-profile', method: 'POST', headers: { 'Content-Type': 'application/json' } }, {
+    await request({ hostname: 'localhost', port: 8083, path: '/shifts/employee-profile', method: 'POST', headers: { 'Content-Type': 'application/json' } }, {
         employee_id: '101', name: 'Manual Pin Worker', role: 'Cook', hourly_wage: 15, rating: 5, category: 'Contract',
         max_weekly_hours: 40, max_daily_hours: 8
     });
@@ -27,5 +27,5 @@
     console.log('Manually assigning employee to 2026-03-01 Morning...');
-    await request({ hostname: 'localhost', port: 8080, path: '/shifts/manual-assign', method: 'POST', headers: { 'Content-Type': 'application/json' } }, {
+    await request({ hostname: 'localhost', port: 8083, path: '/shifts/manual-assign', method: 'POST', headers: { 'Content-Type': 'application/json' } }, {
         date: '2026-03-01', shift: 'Morning',
         employees: [{ employee_id: '101', name: 'Manual Pin Worker' }]
     });
@@ -35,7 +35,7 @@
         shifts: [{ shift_name: 'Morning', start_time: '08:00', end_time: '16:00', duration_hours: 8,
             requirements: [{ role: 'Cook', required_rating: 3, workers_needed: 1 }] }]
     };
-    const res = await request({ hostname: 'localhost', port: 8080, path: '/shifts/assign-v2', method: 'POST', headers: { 'Content-Type': 'application/json' } }, solveReq);
+    const res = await request({ hostname: 'localhost', port: 8083, path: '/shifts/assign-v2', method: 'POST', headers: { 'Content-Type': 'application/json' } }, solveReq);
     
     console.log('\nSolver Result (Should have 0 assignments in array because the shift is already filled by a pinned worker, and pinned workers are omitted from JSON response):');
     console.log(JSON.stringify(res.assignments_by_date, null, 2));

===DIFF===

@@ -40,7 +40,7 @@
     const res = await request({ hostname: 'localhost', port: 8083, path: '/shifts/assign-v2', method: 'POST', headers: { 'Content-Type': 'application/json' } }, solveReq);
     
     console.log('\nSolver Result (Should have 0 assignments in array because the shift is already filled by a pinned worker, and pinned workers are omitted from JSON response):');
-    console.log(JSON.stringify(res.assignments_by_date, null, 2));
+    console.log(JSON.stringify(res, null, 2));
 }
 
 run();

===DIFF===

@@ -33,9 +33,13 @@
 
     console.log('Running auto-solver for 2026-03-01...');
     const solveReq = {
-        startDate: '2026-03-01', endDate: '2026-03-01', prioritizePermanent: false,
-        shifts: [{ shift_name: 'Morning', start_time: '08:00', end_time: '16:00', duration_hours: 8,
-            requirements: [{ role: 'Cook', required_rating: 3, workers_needed: 1 }] }]
+        shift_name: 'Morning',
+        date: '2026-03-01',
+        time: '08:00 - 16:00',
+        duration: '8 Hours',
+        prioritizePermanent: false,
+        schedule_breaks: false,
+        requirements: [{ role: 'Cook', rating: 3, count: 1 }]
     };
     const res = await request({ hostname: 'localhost', port: 8083, path: '/shifts/assign-v2', method: 'POST', headers: { 'Content-Type': 'application/json' } }, solveReq);
     

===DIFF===

@@ -34,12 +34,17 @@
     console.log('Running auto-solver for 2026-03-01...');
     const solveReq = {
         shift_name: 'Morning',
-        date: '2026-03-01',
-        time: '08:00 - 16:00',
-        duration: '8 Hours',
+        start_date: '2026-03-01',
+        end_date: '2026-03-01',
+        start_time: '08:00',
+        end_time: '16:00',
         prioritizePermanent: false,
         schedule_breaks: false,
-        requirements: [{ role: 'Cook', rating: 3, count: 1 }]
+        roles: [{ role_name: 'Cook', rating: 3, max_workers: 1 }],
+        existing_users: [{
+            employee_id: '101', name: 'Manual Pin Worker', rate: 15.0, unit: 'hour',
+            rating: 5, role: 'Cook', gender: 'Male', employeeType: 'Contract'
+        }]
     };
     const res = await request({ hostname: 'localhost', port: 8083, path: '/shifts/assign-v2', method: 'POST', headers: { 'Content-Type': 'application/json' } }, solveReq);
     

===DIFF===

@@ -696,6 +696,7 @@
     // Store employee shifts and leaves - make them static so they persist
 //    private static final Map<String, String> employeeShifts = new HashMap<>();
     private static final Map<String, Map<String, List<String>>> shiftAssignments = new HashMap<>();  // day -> shift -> list of employee IDs
+    private static final Map<String, Map<String, List<String>>> manualAssignments = new HashMap<>(); // NEW: tracks ONLY manual assignments
     private static Map<String, Map<String, ShiftTimes>> employeeShiftTimesCache = new ConcurrentHashMap<>();
     private static final Map<String, List<LeaveRecord>> employeeLeaves = new HashMap<>();
     private static final Map<String, EmployeeInfo> employeeInfo = new HashMap<>();

===DIFF===



===DIFF===

@@ -4879,6 +4879,12 @@
                         .computeIfAbsent(date, k -> new HashMap<>())
                         .computeIfAbsent(shift, k -> new ArrayList<>())
                         .add(empId);
+                        
+                // NEW: Also explicitly track as a manual assignment
+                manualAssignments
+                        .computeIfAbsent(date, k -> new HashMap<>())
+                        .computeIfAbsent(shift, k -> new ArrayList<>())
+                        .add(empId);
 
                 String startTime = null;
                 String endTime = null;

===DIFF===

@@ -11512,13 +11512,18 @@
             LocalDate historyStart = startDate.minusDays(7);
             LocalDate historyEnd = endDate.plusDays(7);
             for (LocalDate d = historyStart; !d.isAfter(historyEnd); d = d.plusDays(1)) {
-                // Fix #1: Do not inject pinned entities for dates inside the active planning window
-                if (!d.isBefore(startDate) && !d.isAfter(endDate)) {
-                    continue;
-                }
-                
+                // Fix #1b: Differentiate manual pins from auto-solver history.
+                // For dates inside the active planning window, ONLY inject manual assignments.
+                // For dates outside, inject all assignments (auto + manual) to provide historical context.
+                boolean inActiveWindow = !d.isBefore(startDate) && !d.isAfter(endDate);
                 String dStr = d.toString();
-                Map<String, List<String>> pastAssignments = shiftAssignments.get(dStr);
+                
+                Map<String, List<String>> pastAssignments;
+                if (inActiveWindow) {
+                    pastAssignments = manualAssignments.get(dStr);
+                } else {
+                    pastAssignments = shiftAssignments.get(dStr);
+                }
                 if (pastAssignments == null) continue;
                 
                 for (Map.Entry<String, List<String>> shiftEntry : pastAssignments.entrySet()) {

===DIFF===

@@ -17,7 +17,7 @@
 
 async function run() {
     console.log('Clearing DB...');
-    await request({ hostname: 'localhost', port: 8083, path: '/api/debug/clear', method: 'POST' });
+    await request({ hostname: 'localhost', port: 8083, path: '/employees/all', method: 'DELETE' });
 
     console.log('Creating an employee...');
     await request({ hostname: 'localhost', port: 8083, path: '/shifts/employee-profile', method: 'POST', headers: { 'Content-Type': 'application/json' } }, {

===DIFF===

@@ -122,11 +122,7 @@
     
     
     console.log("\n--- Starting Benchmarks ---");
-    results.push(await runBenchmark(25, 50));
-    results.push(await runBenchmark(50, 100));
     results.push(await runBenchmark(100, 200));
-    results.push(await runBenchmark(250, 500));
-    results.push(await runBenchmark(500, 1000));
     
     console.log(`\n===============================================`);
     console.log(`BENCHMARK SUMMARY`);

===DIFF===

@@ -11587,6 +11587,9 @@
             problem.setRequestedShiftName(shiftName);
             problem.setPrioritizePermanent(prioritizePermanent);
 
+            long timeLimit = input.containsKey("time_limit_seconds") ? ((Number) input.get("time_limit_seconds")).longValue() : 60L;
+            long unimprovedLimit = input.containsKey("unimproved_time_limit_seconds") ? ((Number) input.get("unimproved_time_limit_seconds")).longValue() : 30L;
+
             SolverConfig solverConfig = new SolverConfig()
                     .withEnvironmentMode(ai.timefold.solver.core.config.solver.EnvironmentMode.FULL_ASSERT)
                     .withSolutionClass(ShiftScheduleV2.class)
@@ -11593,7 +11593,7 @@
                     .withConstraintProviderClass(ShiftConstraintsV2.class)
                     .withTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
-                            .withSpentLimit(Duration.ofSeconds(60))
-                            .withUnimprovedSpentLimit(Duration.ofSeconds(30)));
+                            .withSpentLimit(Duration.ofSeconds(timeLimit))
+                            .withUnimprovedSpentLimit(Duration.ofSeconds(unimprovedLimit)));
 
             SolverFactory<ShiftScheduleV2> solverFactory = SolverFactory.create(solverConfig);
             Solver<ShiftScheduleV2> solver = solverFactory.buildSolver();

===DIFF===

@@ -11082,7 +11082,7 @@
                 factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .rewardLong(HardMediumSoftLongScore.ONE_SOFT, 
-                            a -> (long) (a.getPerformanceRating() * a.getShiftDurationHours() * 5))
+                            a -> (long) (a.getPerformanceRating() * a.getShiftDurationHours() * 2))
                     .asConstraint("maximizeRating")
             );
 

===DIFF===

@@ -10886,7 +10886,7 @@
                 case 8 -> buildOvertimeThreshold(factory, config);
                 case 9 -> buildBreakConstraint(factory, config);
                 case 10 -> null; // buildConsecutiveShifts(factory, config);
-                case 11 -> null; // buildPermanentPriority(factory, config);
+                case 11 -> buildPermanentPriority(factory, config);
                 default -> null;
             };
         }

===DIFF===

@@ -10885,7 +10885,7 @@
                 case 7 -> buildMaxWeeklyHours(factory, config);
                 case 8 -> buildOvertimeThreshold(factory, config);
                 case 9 -> buildBreakConstraint(factory, config);
-                case 10 -> null; // buildConsecutiveShifts(factory, config);
+                case 10 -> buildConsecutiveShifts(factory, config);
                 case 11 -> buildPermanentPriority(factory, config);
                 default -> null;
             };

===DIFF===

@@ -11249,15 +11249,24 @@
             
             // Allow dynamic overrides from JSON payload for testing
             @SuppressWarnings("unchecked")
-            List<Map<String, Object>> overrideConfigs = (List<Map<String, Object>>) input.get("config");
-            if (overrideConfigs != null) {
-                for (Map<String, Object> override : overrideConfigs) {
-                    int cId = (Integer) override.get("constraintId");
+            List<Map<String, Object>> activeOverrides = (List<Map<String, Object>>) input.get("active_constraints");
+            if (activeOverrides != null) {
+                // If the user provided active_constraints, disable all constraints by default
+                constraintConfigs.forEach(c -> c.setEnabled(false));
+                
+                for (Map<String, Object> override : activeOverrides) {
+                    String name = (String) override.get("name");
                     String severity = (String) override.get("severity");
+                    Number value = (Number) override.get("value");
+                    
                     constraintConfigs.stream()
-                        .filter(c -> c.getConstraintId() == cId)
-                        .findFirst()
-                        .ifPresent(c -> c.setSeverity(severity));
+                        .filter(c -> c.getConstraintName().equals(name))
+                        .findFirst()
+                        .ifPresent(c -> {
+                            c.setEnabled(true);
+                            if (severity != null) c.setSeverity(severity);
+                            if (value != null) c.setParameterValue(value.doubleValue());
+                        });
                 }
             }
             

===DIFF===

@@ -53,7 +53,7 @@
         { name: "maxWeeklyHours", severity: "HARD", value: 40.0 },
         { name: "overtimeThreshold", severity: "SOFT", value: 8.0 },
         { name: "breakAfterHours", severity: "HARD", value: 4.0 },
-        { name: "consecutiveShifts", severity: "HARD" },
+        { name: "consecutiveShifts", severity: "SOFT" },
         { name: "permanentPriority", severity: "SOFT" }
     ],
     time_limit_seconds: 300,

===DIFF===

@@ -56,8 +56,8 @@
         { name: "consecutiveShifts", severity: "SOFT" },
         { name: "permanentPriority", severity: "SOFT" }
     ],
-    time_limit_seconds: 300,
-    unimproved_time_limit_seconds: 60
+    time_limit_seconds: 900,
+    unimproved_time_limit_seconds: 120
 };
 
 console.log(`===============================================`);
@@ -64,4 +64,4 @@
-console.log(`- 500 Employees (${permanentCount} Perm, ${contractCount} Contract), 2800 Slots, 5 Min Limit`);
+console.log(`- 500 Employees (${permanentCount} Perm, ${contractCount} Contract), 2800 Slots, 15 Min Limit`);
 console.log(`- ALL 11 Constraints Enabled`);
 console.log(`- permanentPriority explicitly set to SOFT`);
 console.log(`- Single ISO Week Boundary (Monday to Sunday)`);

===DIFF===

@@ -1,7 +1,7 @@
 target/
-!.mvn/wrapper/maven-wrapper.jar
-!**/src/main/**/target/
-!**/src/test/**/target/
+# !.mvn/wrapper/maven-wrapper.jar
+# !**/src/main/**/target/
+# !**/src/test/**/target/
 
 # target/*
 # !target/quarkus-app/
@@ -8,4 +8,5 @@
 
+
 ### IntelliJ IDEA ###
 .idea/modules.xml
 .idea/jarRepositories.xml

===DIFF===

@@ -7,11 +7,6 @@
 > [!IMPORTANT]
 > Since benchmarks can take 15+ minutes and write HTML reports to the disk, I plan to expose this as a background job or a simple blocking API endpoint that returns the path to the HTML report. For this implementation, a **blocking API** (`POST /shifts/benchmark`) is easiest—you just send the request, wait, and get the folder path back. Is that acceptable, or do you want an asynchronous setup?
 
-## Open Questions
-
-> [!NOTE]
-> Do you want the benchmark reports to be served over HTTP so you can view them in the browser, or is simply generating them in the `target/benchmarks` folder locally sufficient for now?
-
 ## Proposed Changes
 
 ### Dependencies
@@ -20,6 +20,26 @@
 
 ### Java Source Code
 
+#### [NEW] [BenchmarkRunner.java](file:///c:/Users/user/Downloads/timefold/timefold/src/main/java/com/scheduler/BenchmarkRunner.java)
+- Create a standalone Java main class (`BenchmarkRunner`) specifically for developer benchmarking.
+- Extract the JSON payload parsing logic from `ShiftApp.java` into a reusable helper method to load `ShiftScheduleV2` problem instances from real JSON files on disk (e.g., `payload1.json`, `test_payload_user.json`, `request_500_employees_400_shifts.json`).
+- Programmatically construct a `PlannerBenchmarkConfig` outputting to `target/benchmarks`.
+- Add 3 different Solver Configurations for Phase 1:
+  1. **Current V2**: The standard V2 solver.
+  2. **Late Acceptance**: Current V2 + Late Acceptance algorithm.
+  3. **Simulated Annealing**: Current V2 + Simulated Annealing algorithm.
+- Provide a `main` method to execute the benchmark across all payloads and generate the HTML report.
+
+## Verification Plan
+
+### Automated Execution
+- Run `java -cp ... com.scheduler.BenchmarkRunner` locally to execute the benchmark against the test suites.
+
+### Manual Verification
+- Verify that `target/benchmarks/index.html` accurately reports Hard/Medium/Soft scores, Solve time, and assignment counts across all 3 search strategies.
+
+### Java Source Code
+
 #### [NEW] [BenchmarkApp.java](file:///c:/Users/user/Downloads/timefold/timefold/src/main/java/com/scheduler/BenchmarkApp.java)
 - Create a new Quarkus endpoint `POST /shifts/benchmark`.
 - Reuse the dynamic payload parsing and constraint initialization from `ShiftApp.java` (V2).

===DIFF===

@@ -38,10 +38,10 @@
         // Strategy 1: Current V2 (Default Timefold behavior)
         SolverBenchmarkConfig currentV2 = new SolverBenchmarkConfig()
                 .withName("Current_V2")
-                .withSolverConfig(baseSolverConfig.copy());
+                .withSolverConfig(new SolverConfig(baseSolverConfig));
 
         // Strategy 2: Late Acceptance
-        SolverConfig laConfig = baseSolverConfig.copy();
+        SolverConfig laConfig = new SolverConfig(baseSolverConfig);
         laConfig.withPhaseList(Arrays.asList(
                 new ConstructionHeuristicPhaseConfig(),
                 new LocalSearchPhaseConfig().withAcceptorConfig(
@@ -52,7 +52,7 @@
                 .withSolverConfig(laConfig);
 
         // Strategy 3: Simulated Annealing
-        SolverConfig saConfig = baseSolverConfig.copy();
+        SolverConfig saConfig = new SolverConfig(baseSolverConfig);
         saConfig.withPhaseList(Arrays.asList(
                 new ConstructionHeuristicPhaseConfig(),
                 new LocalSearchPhaseConfig().withAcceptorConfig(

===DIFF===

@@ -68,8 +68,8 @@
 
         // 3. Load Datasets
         List<ShiftApp.ShiftScheduleV2> datasets = new ArrayList<>();
-        datasets.add(loadProblem("test_payload_user.json", "Real_User_Payload"));
-        datasets.add(loadProblem("request_500_employees_400_shifts.json", "Stress_Test_500_Employees"));
+        datasets.add(loadProblem("test_payload.json", "Small_Dataset"));
+        datasets.add(loadProblem("dataset_500_convergence.json", "Stress_Test_500_Employees"));
 
         datasets.removeIf(Objects::isNull);
 

===DIFF===

@@ -80,6 +80,17 @@
 
         System.out.println("✅ Loaded " + datasets.size() + " datasets. Building benchmark...");
 
+        // Evaluate initial score
+        try {
+            ai.timefold.solver.core.api.score.ScoreManager<ShiftApp.ShiftScheduleV2, ai.timefold.solver.core.api.score.buildin.hardmediumsoftlong.HardMediumSoftLongScore> scoreManager = 
+                ai.timefold.solver.core.api.score.ScoreManager.create(ai.timefold.solver.core.api.solver.SolverFactory.create(baseSolverConfig));
+            for (ShiftApp.ShiftScheduleV2 prob : datasets) {
+                System.out.println("Initial score for " + prob.getRequestedShiftName() + ": " + scoreManager.updateScore(prob));
+            }
+        } catch (Exception e) {
+            System.err.println("Failed to calculate initial score: " + e.getMessage());
+        }
+
         // 4. Run Benchmark
         PlannerBenchmarkFactory benchmarkFactory = PlannerBenchmarkFactory.create(benchmarkConfig);
         PlannerBenchmark benchmark = benchmarkFactory.buildPlannerBenchmark(datasets.toArray(new ShiftApp.ShiftScheduleV2[0]));
@@ -86,5 +86,5 @@
         System.out.println("⏳ Running benchmark. This will take a while...");
-        benchmark.benchmark(); // Don't show report in browser if not running in UI
+        // benchmark.benchmark(); // Don't show report in browser if not running in UI
         System.out.println("🎉 Benchmark complete! Report generated in target/benchmarks/index.html");
     }
 

===DIFF===

@@ -257,12 +257,17 @@
         List<String> possibleShifts = Arrays.asList(shiftName);
 
         List<ShiftApp.ConstraintConfig> constraintConfigs = new ArrayList<>();
-        constraintConfigs.add(new ShiftApp.ConstraintConfig(1, "maxWeeklyHours", "desc", "HARD", 40.0, "hours"));
-        constraintConfigs.add(new ShiftApp.ConstraintConfig(2, "consecutiveShifts", "desc", "HARD", 5.0, "days"));
-        constraintConfigs.add(new ShiftApp.ConstraintConfig(6, "everyShiftPlanned", "desc", "MEDIUM", 1.0, "penalty"));
-        constraintConfigs.add(new ShiftApp.ConstraintConfig(7, "wageOptimization", "desc", "SOFT", 1.0, "weight"));
-        constraintConfigs.add(new ShiftApp.ConstraintConfig(8, "maximizeRating", "desc", "SOFT", 1.0, "weight"));
-        constraintConfigs.add(new ShiftApp.ConstraintConfig(10, "permanentPriority", "desc", prioritizePermanent ? "SOFT" : "DISABLED", 1.0, "weight"));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(1, "skillMatch", "Employee skills should match with shift required skills", "HARD", 100.0, "skillMatchPercentage"));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(2, "noOverlappingShifts", "No overlapping/concurrent shifts per employee", "HARD", null, null));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(3, "unavailableTimeslot", "Shift not planned on unavailable timeslot", "HARD", null, null));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(4, "everyShiftPlanned", "Every shift should be planned/assigned", "MEDIUM", null, null));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(5, "wageOptimization", "Assign employees preferring lower wages", "SOFT", null, null));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(6, "maxDailyHours", "Maximum working hours per day", "MEDIUM", 8.0, "maxHoursPerDay"));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(7, "maxWeeklyHours", "Maximum working hours per week", "MEDIUM", 40.0, "maxHoursPerWeek"));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(8, "overtimeThreshold", "Hours beyond threshold counted as overtime", "SOFT", 8.0, "otThresholdHours"));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(9, "breakAfterHours", "Mandatory break after specified hours", "HARD", 4.0, "breakAfterHours"));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(10, "consecutiveShifts", "Minimize gaps in schedule", "HARD", null, null));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(11, "permanentPriority", "Priority to permanent employees over contract", prioritizePermanent ? "SOFT" : "DISABLED", null, null));
 
         @SuppressWarnings("unchecked")
         List<Map<String, Object>> activeOverrides = (List<Map<String, Object>>) input.get("active_constraints");

===DIFF===

@@ -96,7 +96,7 @@
         PlannerBenchmark benchmark = benchmarkFactory.buildPlannerBenchmark(datasets.toArray(new ShiftApp.ShiftScheduleV2[0]));
 
         System.out.println("⏳ Running benchmark. This will take a while...");
-        // benchmark.benchmark(); // Don't show report in browser if not running in UI
+        benchmark.benchmark(); // Don't show report in browser if not running in UI
         System.out.println("🎉 Benchmark complete! Report generated in target/benchmarks/index.html");
     }
 

===DIFF===

@@ -10973,7 +10973,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> a.getShiftDurationHours() > maxHours)
-                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - maxHours) * 60 * 100L))
+                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - maxHours) * 60))
                     .asConstraint("maxDailyHours");
         }
 
@@ -10984,7 +10984,7 @@
                     .filter(a -> a.getShift() != null)
                     .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, Scheduler.EmployeeAssignment::getIsoWeekNum, ConstraintCollectors.sumLong(a -> (long)(a.getShiftDurationHours() * 60)))
                     .filter((empId, weekNum, totalMinutes) -> totalMinutes > maxWeeklyHours * 60)
-                    .penalizeLong(penalty, (empId, weekNum, totalMinutes) -> (long) ((totalMinutes - (maxWeeklyHours * 60)) * 100L))
+                    .penalizeLong(penalty, (empId, weekNum, totalMinutes) -> (long) (totalMinutes - (maxWeeklyHours * 60)))
                     .asConstraint("maxWeeklyHours");
         }
 

===DIFF===

@@ -10973,7 +10973,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> a.getShiftDurationHours() > maxHours)
-                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - maxHours) * 60))
+                    .penalizeLong(penalty, a -> (long) ((a.getShiftDurationHours() - maxHours) * 60 * 100L))
                     .asConstraint("maxDailyHours");
         }
 
@@ -10984,7 +10984,7 @@
                     .filter(a -> a.getShift() != null)
                     .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, Scheduler.EmployeeAssignment::getIsoWeekNum, ConstraintCollectors.sumLong(a -> (long)(a.getShiftDurationHours() * 60)))
                     .filter((empId, weekNum, totalMinutes) -> totalMinutes > maxWeeklyHours * 60)
-                    .penalizeLong(penalty, (empId, weekNum, totalMinutes) -> (long) (totalMinutes - (maxWeeklyHours * 60)))
+                    .penalizeLong(penalty, (empId, weekNum, totalMinutes) -> (long) ((totalMinutes - (maxWeeklyHours * 60)) * 100L))
                     .asConstraint("maxWeeklyHours");
         }
 

===DIFF===

@@ -1,9 +1,9 @@
-# Constraint Severities and Penalties
-
-This table outlines every constraint in the Timefold engine, its default severity, and its underlying mathematical penalty weight (multiplier).
-
-## Dynamic Constraints
-*These constraints are configurable by the admin via the database.*
+# V2 Engine Constraint Severities and Penalties
+
+This table outlines every constraint active in the **V2 Solver Engine**, its default severity, and its underlying mathematical penalty weight (multiplier).
+
+## Dynamic Constraints (V2)
+*These constraints are evaluated dynamically based on the `default_constraints` database table.*
 
 | Constraint Name | Default Severity | Penalty Mathematical Formula |
 | :--- | :--- | :--- |
@@ -20,19 +20,13 @@
 
 ---
 
-## System / Hardcoded Constraints
-*These constraints are locked into the Java code and cannot be changed by the admin.*
-
-| Constraint Name | Locked Severity | Penalty Mathematical Formula |
-| :--- | :--- | :--- |
-| `oneShiftPerEmployeePerDay` | **HARD** | `1L` (per violation) |
-| `femaleCannotWorkNightShift`| **HARD** | `1L` (per violation) |
-| `employeeRatingMatchesRole` | **HARD** | `1L` (per violation) |
-| `assignAsManyAsPossible`    | **HARD** | `1L` (Helper for Construction Heuristic) |
-| `minimumRestPeriod`         | **HARD** | `1000L` (per violation) |
-| `assignHighestRatingToTough`| **SOFT** | Variable based on rating difference |
-| `avoidOverlappingBreaks`    | **SOFT** | `1L` (per overlap) |
-| `minimizeLateNightShifts`   | **SOFT** | `(nightShiftCount)²` |
+## System / Hardcoded Constraints (V2)
+*These constraints are locked into the Java code for the V2 engine and cannot be changed by the admin. They ensure basic logical rules remain intact.*
+
+| Constraint Name | Locked Severity | Penalty / Reward Mathematical Formula |
+| :--- | :--- | :--- |
+| `maxWorkersPerRolePerShift` | **HARD** | Penalty: `(count - maxWorkers) * 10000L` |
+| `maximizeRating`            | **SOFT** | Reward: `rating * shiftDurationHours * 2` |
 
 ---
 
@@ -39,4 +39,3 @@
 
-As discussed regarding your manager's strict policy:
 Because `maxWeeklyHours` multiplies every single minute of overtime by **100**, an 8-hour overtime shift incurs a penalty of **48,000**. This completely dwarfs the **3,000** penalty for `everyShiftPlanned`. Therefore, the solver will heavily favor leaving shifts empty to avoid overtime, regardless of whether they are placed in the same severity tier.
 

===DIFF===

@@ -1,4 +1,4 @@
-# Constraint Severity Refactor — Final Implementation Plan (v2)
+# Constraint Severity Refactor — Final Implementation Plan (v3)
 
 ## Goal
 
@@ -231,7 +231,10 @@
 After:
 ```java
 .penalizeLong(resolveScore("maxDailyHours", config.getSeverity()),
-        a -> (long) Math.ceil((a.getShiftDurationHours() - maxHours) * 60 / 60.0))
+        a -> {
+            long overMinutes = (long) ((a.getShiftDurationHours() - maxHours) * 60);
+            return overMinutes <= 0 ? 0L : (long) Math.ceil(overMinutes / 60.0);
+        })
 ```
 
 ---
@@ -245,7 +245,10 @@
 After:
 ```java
 .penalizeLong(resolveScore("maxWeeklyHours", config.getSeverity()),
-        (empId, weekNum, totalMinutes) -> (long) Math.ceil((totalMinutes - maxWeeklyHours * 60) / 60.0))
+        (empId, weekNum, totalMinutes) -> {
+            long overMinutes = (long) (totalMinutes - maxWeeklyHours * 60);
+            return overMinutes <= 0 ? 0L : (long) Math.ceil(overMinutes / 60.0);
+        })
 ```
 
 ---
@@ -259,7 +259,10 @@
 After:
 ```java
 .penalizeLong(resolveScore("overtimeThreshold", config.getSeverity()),
-        a -> (long) Math.ceil((a.getShiftDurationHours() - threshold) * 60 / 60.0))
+        a -> {
+            long overMinutes = (long) ((a.getShiftDurationHours() - threshold) * 60);
+            return overMinutes <= 0 ? 0L : (long) Math.ceil(overMinutes / 60.0);
+        })
 ```
 
 ---
@@ -279,7 +279,8 @@
         a -> {
             long workBeforeBreak = java.time.Duration.between(
                     a.getShiftStartTimeObj(), a.getBreakStartTime()).toMinutes();
-            return (long) Math.ceil((workBeforeBreak - maxHours * 60) / 60.0);
+            long overMinutes = (long) (workBeforeBreak - maxHours * 60);
+            return overMinutes <= 0 ? 0L : (long) Math.ceil(overMinutes / 60.0);
         })
 ```
 
@@ -361,6 +361,7 @@
 
 | Variant | Match Weigher | 5-star reward |
 | :--- | :--- | :--- |
+| Baseline | `rating * 0` | 0 (no rating reward) |
 | A | `rating * 50` | 250 |
 | B | `rating * 100` *(starting point)* | 500 |
 | C | `rating * 200` | 1000 |
@@ -367,7 +367,13 @@
 Run the standard 500-employee test with each variant and compare:
-- Total wage cost
-- Average employee rating assigned
-- Coverage (assignments made)
+
+| Multiplier | Coverage | Avg Rating | Total Wage |
+| :--- | :--- | :--- | :--- |
+| 0 | ? | ? | ? |
+| 50 | ? | ? | ? |
+| 100 | ? | ? | ? |
+| 200 | ? | ? | ? |
+
+This immediately shows the real business impact of the rating reward.
 
 Pick the variant that balances rating quality vs wage cost most fairly.
 

===DIFF===

@@ -26,7 +26,7 @@
 | `consecutiveShifts` | **100** | `totalGapDays` | 100 × 2 = **200** |
 | `overtimeThreshold` | **10** | `ceil(minutesOver / 60)` | 10 × 2 = **20** |
 | `permanentPriority` | **10** | `1` (conditional) | 10 × 1 = **10** |
-| `wageOptimization` | **1** | `(wage/avgWage) × 100` | 1 × 150 = **150** |
+| `wageOptimization` | **10** | `(wage/avgWage) × 100` | 10 × 150 = **1500** |
 | `maximizeRating` | **1** | `rating × 100` *(benchmark 50/100/200 later)* | 1 × 500 = **500** (reward) |
 
 ---
@@ -110,7 +110,7 @@
         case "consecutiveShifts"   -> 100L;
         case "overtimeThreshold"   -> 10L;
         case "permanentPriority"   -> 10L;
-        case "wageOptimization"    -> 1L;
+        case "wageOptimization"    -> 10L;
         case "maximizeRating"      -> 1L;
         default                    -> 1L;
     };

===DIFF===

@@ -619,6 +619,7 @@
         defaults.add(new ConstraintConfig(9, "breakAfterHours", "Mandatory break after specified hours", "HARD", 4.0, "breakAfterHours", 30.0, "breakDurationMinutes"));
         defaults.add(new ConstraintConfig(10, "consecutiveShifts", "Minimize gaps in schedule (prefer consecutive working days)", "HARD", null, null));
         defaults.add(new ConstraintConfig(11, "permanentPriority", "Priority to permanent employees over contract", "SOFT", null, null));
+        defaults.add(new ConstraintConfig(12, "maximizeRating", "Reward higher-rated employees", "SOFT", null, null));
         return defaults;
     }
 

===DIFF===

@@ -10839,6 +10839,7 @@
         private static Map<String, List<Integer>> requiredRatingsPerRole = new HashMap<>();
         private static Map<String, List<String>> requiredSkillsPerRole = new HashMap<>();
         private static List<ConstraintConfig> activeConstraintConfigs = new ArrayList<>();
+        private static double averageWage = 1.0;
 
         public ShiftConstraintsV2() {}
 
@@ -10845,7 +10845,8 @@
                 List<Scheduler.ShiftSchedule.RoleLimit> roleLimits,
                 List<Scheduler.ShiftSchedule.RatingRequirement> ratingRequirements,
                 Map<String, List<String>> skillsMap,
-                List<ConstraintConfig> configs) {
+                List<ConstraintConfig> configs,
+                double avgWage) {
 
             maxWorkersPerRole.clear();
             requiredRatingsPerRole.clear();
@@ -10852,4 +10852,5 @@
             activeConstraintConfigs = configs != null ? new ArrayList<>(configs) : new ArrayList<>();
+            averageWage = avgWage > 0 ? avgWage : 1.0;
 
             for (Scheduler.ShiftSchedule.RoleLimit l : roleLimits) {
                 maxWorkersPerRole.put(l.getRoleName(), l.getMaxWorkers());
@@ -10863,12 +10863,28 @@
                     activeConstraintConfigs.size() + " constraint configs");
         }
 
-        private HardMediumSoftLongScore getScoreForSeverity(String severity) {
+        private HardMediumSoftLongScore resolveScore(String constraintName, String severity) {
+            long weight = switch (constraintName) {
+                case "skillMatch"          -> 100L;
+                case "noOverlappingShifts" -> 1000L;
+                case "unavailableTimeslot" -> 1L;
+                case "everyShiftPlanned"   -> 100L;
+                case "maxDailyHours"       -> 10L;
+                case "maxWeeklyHours"      -> 10L;
+                case "breakAfterHours"     -> 10L;
+                case "consecutiveShifts"   -> 100L;
+                case "overtimeThreshold"   -> 10L;
+                case "permanentPriority"   -> 10L;
+                case "wageOptimization"    -> 10L;
+                case "maximizeRating"      -> 1L;
+                default                    -> 1L;
+            };
+
             return switch (severity.toUpperCase()) {
-                case "HARD" -> HardMediumSoftLongScore.ONE_HARD;
-                case "MEDIUM" -> HardMediumSoftLongScore.ONE_MEDIUM;
-                case "SOFT" -> HardMediumSoftLongScore.ONE_SOFT;
-                default -> HardMediumSoftLongScore.ONE_HARD;
+                case "HARD"   -> HardMediumSoftLongScore.ofHard(weight);
+                case "MEDIUM" -> HardMediumSoftLongScore.ofMedium(weight);
+                case "SOFT"   -> HardMediumSoftLongScore.ofSoft(weight);
+                default       -> HardMediumSoftLongScore.ofSoft(weight);
             };
         }
 
@@ -10884,6 +10884,7 @@
                 case 9 -> buildBreakConstraint(factory, config);
                 case 10 -> buildConsecutiveShifts(factory, config);
                 case 11 -> buildPermanentPriority(factory, config);
+                case 12 -> buildMaximizeRating(factory, config);
                 default -> null;
             };
         }

===DIFF===

@@ -10915,7 +10915,6 @@
         // Constraint #1: Skill Match (percentage-based)
         private Constraint buildSkillMatch(ConstraintFactory factory, ConstraintConfig config) {
             double matchPercentage = config.getParameterValue() != null ? config.getParameterValue() : 100.0;
-            HardMediumSoftLongScore penalty = getScoreForSeverity(config.getSeverity());
 
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
@@ -10925,7 +10925,8 @@
                         double actualPct = (matchedCount * 100.0) / required.size();
                         return actualPct < matchPercentage;
                     })
-                    .penalizeLong(penalty, a -> a.getMissingSkillCount() * 2000L)
+                    .penalizeLong(resolveScore("skillMatch", config.getSeverity()),
+                            a -> (long) a.getMissingSkillCount())
                     .asConstraint("skillMatch");
         }
 
@@ -10932,5 +10932,4 @@
         private Constraint buildNoOverlap(ConstraintFactory factory, ConstraintConfig config) {
-            HardMediumSoftLongScore penalty = getScoreForSeverity(config.getSeverity());
             return factory.forEachUniquePair(Scheduler.EmployeeAssignment.class,
                             Joiners.equal(Scheduler.EmployeeAssignment::getEmployeeId),
                             Joiners.equal(Scheduler.EmployeeAssignment::getDate))
@@ -10937,4 +10937,5 @@
-                    .penalizeLong(penalty, (a1, a2) -> 3000L)
+                    .penalizeLong(resolveScore("noOverlappingShifts", config.getSeverity()),
+                   
<truncated 11532 bytes>
           .filter(a -> a.getShift() != null)
+                    .filter(a -> !a.isPermanentEmployee() && a.isPrioritizePermanent())
+                    .penalizeLong(resolveScore("permanentPriority", config.getSeverity()),
+                            a -> 1L)
                     .asConstraint("permanentPriority");
         }
 
+        // Constraint #12: Maximize Rating (dynamic from DB)
+        private Constraint buildMaximizeRating(ConstraintFactory factory, ConstraintConfig config) {
+            return factory.forEach(Scheduler.EmployeeAssignment.class)
+                    .filter(a -> a.getShift() != null)
+                    .rewardLong(resolveScore("maximizeRating", config.getSeverity()),
+                            a -> (long) (a.getPerformanceRating() * 100))
+                    .asConstraint("maximizeRating");
+        }
+
         // Also add max workers per role and rating mismatch as always-on constraints
         @Override
         public Constraint[] defineConstraints(ConstraintFactory factory) {
@@ -11081,15 +11081,6 @@
                     .asConstraint("maxWorkersPerRolePerShift")
             );
 
-            // Always-on: Maximize Rating (Balanced Approach)
-            constraints.add(
-                factory.forEach(Scheduler.EmployeeAssignment.class)
-                    .filter(a -> a.getShift() != null)
-                    .rewardLong(HardMediumSoftLongScore.ONE_SOFT, 
-                            a -> (long) (a.getPerformanceRating() * a.getShiftDurationHours() * 2))
-                    .asConstraint("maximizeRating")
-            );
-
             // Dynamic constraints from DB
             for (ConstraintConfig config : activeConstraintConfigs) {
                 if (!config.isEnabled()) continue;

===DIFF===

@@ -11622,7 +11622,13 @@
             // constraintConfigs already loaded at the top of the method
 
 
-            ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, constraintConfigs);
+            // Calculate average wage for normalization
+            double averageWage = employeeWages.values().stream()
+                    .mapToDouble(Double::doubleValue)
+                    .average()
+                    .orElse(1.0);
+
+            ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, constraintConfigs, averageWage);
 
             ShiftScheduleV2 problem = new ShiftScheduleV2(planningEntities, possibleShifts, roleLimits, ratingRequirements);
             problem.setRequestedShiftName(shiftName);

===DIFF===

@@ -268,6 +268,7 @@
         constraintConfigs.add(new ShiftApp.ConstraintConfig(9, "breakAfterHours", "Mandatory break after specified hours", "HARD", 4.0, "breakAfterHours"));
         constraintConfigs.add(new ShiftApp.ConstraintConfig(10, "consecutiveShifts", "Minimize gaps in schedule", "HARD", null, null));
         constraintConfigs.add(new ShiftApp.ConstraintConfig(11, "permanentPriority", "Priority to permanent employees over contract", prioritizePermanent ? "SOFT" : "DISABLED", null, null));
+        constraintConfigs.add(new ShiftApp.ConstraintConfig(12, "maximizeRating", "Reward higher-rated employees", "SOFT", null, null));
 
         @SuppressWarnings("unchecked")
         List<Map<String, Object>> activeOverrides = (List<Map<String, Object>>) input.get("active_constraints");
@@ -284,7 +284,12 @@
             }
         }
 
-        ShiftApp.ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, constraintConfigs);
+        double averageWage = allEmployees.values().stream()
+                .mapToDouble(ShiftApp.EmployeeInfo::getHourlyWage)
+                .average()
+                .orElse(1.0);
+
+        ShiftApp.ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, constraintConfigs, averageWage);
 
         ShiftApp.ShiftScheduleV2 problem = new ShiftApp.ShiftScheduleV2(planningEntities, possibleShifts, roleLimits, ratingRequirements);
         problem.setRequestedShiftName(shiftName);

===DIFF===

@@ -1,23 +1,23 @@
 # Constraint Severity Refactor — Task Checklist
 
-- [ ] Change 1: Add `maximizeRating` to default constraint configs (ID 12)
-- [ ] Change 2: Add `averageWage` static field + update `setConfiguration()` signature
-- [ ] Change 3: Replace `getScoreForSeverity()` with `resolveScore()`
-- [ ] Change 4: Add `case 12` to `buildConstraint()`
-- [ ] Change 5: Update `buildSkillMatch`
-- [ ] Change 6: Update `buildNoOverlap`
-- [ ] Change 7: Update `buildUnavailableTimeslot`
-- [ ] Change 8: Update `buildEveryShiftPlanned`
-- [ ] Change 9: Update `buildWageOptimization` (with averageWage normalization)
-- [ ] Change 10: Update `buildMaxDailyHours` (with negative guard)
-- [ ] Change 11: Update `buildMaxWeeklyHours` (with negative guard)
-- [ ] Change 12: Update `buildOvertimeThreshold` (with negative guard)
-- [ ] Change 13: Update `buildBreakConstraint` (with negative guard)
-- [ ] Change 14: Update `buildConsecutiveShifts` (gap magnitude)
-- [ ] Change 15: Update `buildPermanentPriority` (conditional filter)
-- [ ] Change 16: Make `maximizeRating` dynamic — new `buildMaximizeRating()` method + remove hardcoded block
-- [ ] Change 17: Calculate `averageWage` in `assignShiftsV2` and pass to `setConfiguration()`
-- [ ] Compile and verify no errors
+- [x] Change 1: Add `maximizeRating` to default constraint configs (ID 12)
+- [x] Change 2: Add `averageWage` static field + update `setConfiguration()` signature
+- [x] Change 3: Replace `getScoreForSeverity()` with `resolveScore()`
+- [x] Change 4: Add `case 12` to `buildConstraint()`
+- [x] Change 5: Update `buildSkillMatch`
+- [x] Change 6: Update `buildNoOverlap`
+- [x] Change 7: Update `buildUnavailableTimeslot`
+- [x] Change 8: Update `buildEveryShiftPlanned`
+- [x] Change 9: Update `buildWageOptimization` (with averageWage normalization)
+- [x] Change 10: Update `buildMaxDailyHours` (with negative guard)
+- [x] Change 11: Update `buildMaxWeeklyHours` (with negative guard)
+- [x] Change 12: Update `buildOvertimeThreshold` (with negative guard)
+- [x] Change 13: Update `buildBreakConstraint` (with negative guard)
+- [x] Change 14: Update `buildConsecutiveShifts` (gap magnitude)
+- [x] Change 15: Update `buildPermanentPriority` (conditional filter)
+- [x] Change 16: Make `maximizeRating` dynamic — new `buildMaximizeRating()` method + remove hardcoded block
+- [x] Change 17: Calculate `averageWage` in `assignShiftsV2` and pass to `setConfiguration()`
+- [x] Compile and verify no errors
 - [ ] Test 1: `maxWeeklyHours = HARD` → 0 overtime
 - [ ] Test 2: `maxWeeklyHours = MEDIUM` → some overtime, higher coverage
 - [ ] Test 3: `maxWeeklyHours = SOFT` → max coverage

===DIFF===

@@ -2,7 +2,7 @@
 
 const options = {
     hostname: 'localhost',
-    port: 8080,
+    port: 8083,
     path: '/api/scheduler/v2/assign-shifts',
     method: 'POST',
     headers: {

===DIFF===

@@ -29,7 +29,20 @@
             let data = '';
             res.on('data', (chunk) => { data += chunk; });
             res.on('end', () => {
-                const result = JSON.parse(data);
+                if (res.statusCode >= 400) {
+                    console.error(`Error: HTTP ${res.statusCode} - ${data.substring(0, 200)}...`);
+                    resolve();
+                    return;
+                }
+                
+                let result;
+                try {
+                    result = JSON.parse(data);
+                } catch (e) {
+                    console.error('Failed to parse JSON:', data.substring(0, 200));
+                    resolve();
+                    return;
+                }
                 
                 let totalOvertime = 0;
                 let employeesWithOvertime = 0;

===DIFF===

@@ -3,7 +3,7 @@
 const options = {
     hostname: 'localhost',
     port: 8083,
-    path: '/api/scheduler/v2/assign-shifts',
+    path: '/shifts/assign-v2',
     method: 'POST',
     headers: {
         'Content-Type': 'application/json',

===DIFF===

@@ -16,7 +16,7 @@
     return new Promise((resolve) => {
         const payload = JSON.parse(JSON.stringify(basePayload));
         
-        // Find maxWeeklyHours constraint and set severity
+        // Set competing constraints
         payload.active_constraints = [
             {
                 name: "maxWeeklyHours",
@@ -23,4 +23,12 @@
                 value: 40.0
+            },
+            {
+                name: "everyShiftPlanned",
+                severity: "MEDIUM"
+            },
+            {
+                name: "wageOptimization",
+                severity: "SOFT"
             }
         ];
 

===DIFF===

@@ -21,7 +21,7 @@
             {
                 name: "maxWeeklyHours",
                 severity: severity,
-                value: 40.0
+                value: 4.0
             },
             {
                 name: "everyShiftPlanned",

===DIFF===

@@ -52,20 +52,23 @@
                     return;
                 }
                 
-                let totalOvertime = 0;
+                let totalAssignments = 0;
                 let employeesWithOvertime = 0;
-                let totalAssignments = result.assignments ? result.assignments.length : 0;
+                let totalOvertime = 0;
                 
                 const hoursByEmployee = {};
                 
-                if (result.assignments) {
-                    result.assignments.forEach(a => {
-                        hoursByEmployee[a.employee_id] = (hoursByEmployee[a.employee_id] || 0) + a.shift_duration_hours;
-                    });
+                if (result.assignments_by_date) {
+                    for (const [date, dateAssignments] of Object.entries(result.assignments_by_date)) {
+                        totalAssignments += dateAssignments.length;
+                        dateAssignments.forEach(a => {
+                            hoursByEmployee[a.employee_id] = (hoursByEmployee[a.employee_id] || 0) + a.shift_duration_hours;
+                        });
+                    }
                     
                     for (const [empId, hours] of Object.entries(hoursByEmployee)) {
-                        if (hours > 40) {
-                            totalOvertime += (hours - 40);
+                        if (hours > 4.0) { // Using 4.0 because that's our threshold for the test
+                            totalOvertime += (hours - 4.0);
                             employeesWithOvertime++;
                         }
                     }

===DIFF===

@@ -619,7 +619,7 @@
         defaults.add(new ConstraintConfig(9, "breakAfterHours", "Mandatory break after specified hours", "HARD", 4.0, "breakAfterHours", 30.0, "breakDurationMinutes"));
         defaults.add(new ConstraintConfig(10, "consecutiveShifts", "Minimize gaps in schedule (prefer consecutive working days)", "HARD", null, null));
         defaults.add(new ConstraintConfig(11, "permanentPriority", "Priority to permanent employees over contract", "SOFT", null, null));
-        defaults.add(new ConstraintConfig(12, "maximizeRating", "Reward higher-rated employees", "SOFT", null, null));
+        defaults.add(new ConstraintConfig(12, "maximizeRating", "Reward higher-rated employees", "SOFT", 100.0, "ratingMultiplier"));
         return defaults;
     }
 
@@ -11088,10 +11088,11 @@
 
         // Constraint #12: Maximize Rating (dynamic from DB)
         private Constraint buildMaximizeRating(ConstraintFactory factory, ConstraintConfig config) {
+            long multiplier = config.getParameterValue() != null ? config.getParameterValue().longValue() : 100L;
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .rewardLong(resolveScore("maximizeRating", config.getSeverity()),
-                            a -> (long) (a.getPerformanceRating() * 100))
+                            a -> (long) (a.getPerformanceRating() * multiplier))
                     .asConstraint("maximizeRating");
         }
 

===DIFF===

@@ -11051,23 +11051,17 @@
 
         // Constraint #10: Consecutive Shifts
         private Constraint buildConsecutiveShifts(ConstraintFactory factory, ConstraintConfig config) {
-            // The goal is to penalize schedules that are NOT consecutive.
-            // If they work Monday and Wednesday (gap on Tuesday), we penalize.
-            return factory.forEach(Scheduler.EmployeeAssignment.class)
-                    .filter(a -> a.getShift() != null)
-                    .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, ConstraintCollectors.toList())
+            return factory.forEach(Scheduler.EmployeeAssignment.class)
+                    .filter(a -> a.getShift() != null)
+                    .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, ConstraintCollectors.mapping(Scheduler.EmployeeAssignment::getLocalDateObj, ConstraintCollectors.toList()))
                     .penalizeLong(resolveScore("consecutiveShifts", config.getSeverity()),
-                            (employeeId, list) -> {
-                                if (list.size() <= 1) return 0L;
-                        
-                                // Sort by date
-                                list.sort(Comparator.comparing(Scheduler.EmployeeAssignment::getLocalDateObj));
-                        
+                            (employeeId, dates) -> {
+                                if (dates.size() < 2) return 0L;
+                                List<LocalDate> sortedDates = new ArrayList<>(dates);
+                                Collections.sort(sortedDates);
                                 long totalGaps = 0;
-                                for (int i = 0; i < list.size() - 1; i++) {
-                                    LocalDate current = list.get(i).getLocalDateObj();
-                                    LocalDate next = list.get(i+1).getLocalDateObj();
-                                    long daysBetween = java.time.temporal.ChronoUnit.DAYS.between(current, next);
+                                for (int i = 1; i < sortedDates.size(); i++) {
+                                    long daysBetween = java.time.temporal.ChronoUnit.DAYS.between(sortedDates.get(i - 1), sortedDates.get(i));
                                     if (daysBetween > 1) {
                                         totalGaps += (daysBetween - 1);
                                     }

===DIFF===

@@ -11053,7 +11053,8 @@
         private Constraint buildConsecutiveShifts(ConstraintFactory factory, ConstraintConfig config) {
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
-                    .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, ConstraintCollectors.mapping(Scheduler.EmployeeAssignment::getLocalDateObj, ConstraintCollectors.toList()))
+                    .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, 
+                             ConstraintCollectors.toList(a -> LocalDate.parse(a.getShift().getStartDate())))
                     .penalizeLong(resolveScore("consecutiveShifts", config.getSeverity()),
                             (employeeId, dates) -> {
                                 if (dates.size() < 2) return 0L;

===DIFF===

@@ -57,8 +57,9 @@
                     for (const [date, dateAssignments] of Object.entries(result.assignments_by_date)) {
                         totalAssignments += dateAssignments.length;
                         dateAssignments.forEach(a => {
-                            totalRating += a.performance_rating;
-                            totalWage += (a.hourly_wage * a.shift_duration_hours);
+                            totalRating += a.rating;
+                            let shift_hours = a.regular_hours + (a.ot_hours || 0);
+                            totalWage += (a.wage * shift_hours);
                         });
                     }
                 }

===DIFF===

@@ -11053,16 +11053,17 @@
         private Constraint buildConsecutiveShifts(ConstraintFactory factory, ConstraintConfig config) {
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
-                    .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, 
-                             ConstraintCollectors.toList(a -> LocalDate.parse(a.getShift().getStartDate())))
+                    .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, ConstraintCollectors.toList())
                     .penalizeLong(resolveScore("consecutiveShifts", config.getSeverity()),
-                            (employeeId, dates) -> {
-                                if (dates.size() < 2) return 0L;
-                                List<LocalDate> sortedDates = new ArrayList<>(dates);
-                                Collections.sort(sortedDates);
+                            (employeeId, list) -> {
+                                if (list.size() < 2) return 0L;
+                                List<Scheduler.EmployeeAssignment> sortedList = new ArrayList<>(list);
+                                sortedList.sort(Comparator.comparing(Scheduler.EmployeeAssignment::getLocalDateObj));
                                 long totalGaps = 0;
-                                for (int i = 1; i < sortedDates.size(); i++) {
-                                    long daysBetween = java.time.temporal.ChronoUnit.DAYS.between(sortedDates.get(i - 1), sortedDates.get(i));
+                                for (int i = 1; i < sortedList.size(); i++) {
+                                    long daysBetween = java.time.temporal.ChronoUnit.DAYS.between(
+                                            sortedList.get(i - 1).getLocalDateObj(), 
+                                            sortedList.get(i).getLocalDateObj());
                                     if (daysBetween > 1) {
                                         totalGaps += (daysBetween - 1);
                                     }

===DIFF===

@@ -11148,6 +11148,7 @@
     @Path("/constraints")
     @Produces(MediaType.APPLICATION_JSON)
     public Response getConstraints() {
+        constraintConfigs = mysqlService.loadAllConstraintConfigs();
         return Response.ok(Map.of("constraints", constraintConfigs)).build();
     }
 

===DIFF===

@@ -159,9 +159,18 @@
         if (constraintConfigs.isEmpty()) {
             constraintConfigs = getDefaultConstraintConfigs();
             mysqlService.insertDefaultConstraints(constraintConfigs);
-            System.out.println("📋 Inserted 11 default constraint configs");
+            System.out.println("📋 Inserted " + constraintConfigs.size() + " default constraint configs");
         } else {
             System.out.println("📋 Loaded " + constraintConfigs.size() + " constraint configs from DB");
+            
+            // Check if constraint 12 is missing and add it
+            boolean hasMaximizeRating = constraintConfigs.stream().anyMatch(c -> c.getConstraintId() == 12);
+            if (!hasMaximizeRating) {
+                ConstraintConfig maximizeRating = new ConstraintConfig(12, "maximizeRating", "Reward higher-rated employees", "SOFT", 100.0, "ratingMultiplier");
+                mysqlService.insertDefaultConstraints(List.of(maximizeRating));
+                constraintConfigs.add(maximizeRating);
+                System.out.println("📋 Inserted missing constraint 12 (maximizeRating) into DB");
+            }
         }
     }
 

===DIFF===

@@ -10992,7 +10992,7 @@
                     .penalizeLong(resolveScore("wageOptimization", config.getSeverity()),
                             a -> {
                                 double wageRatio = a.getHourlyWage() / averageWage;
-                                return (long) (wageRatio * 100);
+                                return (long) (wageRatio * 100 * 10); // 10x weight as requested
                             })
                     .asConstraint("wageOptimization");
         }

===DIFF===

@@ -65,7 +65,8 @@
         "role_limits": roleLimits,
         "active_constraints": activeConstraints,
         // Increase time limits for larger sizes
-        "solver_timeout_seconds": numEmployees >= 300 ? 120 : (numEmployees >= 100 ? 60 : 30)
+        "time_limit_seconds": numEmployees >= 300 ? 120 : (numEmployees >= 100 ? 60 : 30),
+        "unimproved_time_limit_seconds": 20
     };
 }
 

===DIFF===

@@ -11645,7 +11645,6 @@
             long unimprovedLimit = input.containsKey("unimproved_time_limit_seconds") ? ((Number) input.get("unimproved_time_limit_seconds")).longValue() : 30L;
 
             SolverConfig solverConfig = new SolverConfig()
-                    .withEnvironmentMode(ai.timefold.solver.core.config.solver.EnvironmentMode.FULL_ASSERT)
                     .withSolutionClass(ShiftScheduleV2.class)
                     .withEntityClasses(Scheduler.EmployeeAssignment.class)
                     .withConstraintProviderClass(ShiftConstraintsV2.class)

===DIFF===

@@ -51,9 +51,16 @@
     // Role limits per shift: enough to employ about 80% of the workforce
     // 80% of numEmployees / 2 shifts per day = workers per shift
     const workersPerShift = Math.max(1, Math.floor((numEmployees * 0.8) / 2));
-    const roleLimits = {};
+    // assignShiftsV2 expects "name", "severity", "value"
+    const mappedConstraints = activeConstraints.map(c => ({
+        "name": c.constraintName,
+        "severity": c.severity,
+        "value": c.parameterValue
+    }));
+
+    const rolesArr = [];
     roles.forEach(r => {
-        roleLimits[r] = Math.ceil(workersPerShift / roles.length);
+        rolesArr.push({ "role": r, "max_workers": Math.ceil(workersPerShift / roles.length) });
     });
 
     return {
@@ -60,10 +60,10 @@
         "start_date": "2026-06-01",
         "end_date": "2026-06-07",
-        "shifts": shifts,
-        "employees": employees,
-        "role_limits": roleLimits,
-        "active_constraints": activeConstraints,
-        // Increase time limits for larger sizes
+        "start_time": "08:00:00",
+        "end_time": "16:00:00",
+        "roles": rolesArr,
+        "existing_users": employees,
+        "active_constraints": mappedConstraints,
         "time_limit_seconds": numEmployees >= 300 ? 120 : (numEmployees >= 100 ? 60 : 30),
         "unimproved_time_limit_seconds": 20
     };

===DIFF===

@@ -83,7 +83,7 @@
         const postData = JSON.stringify(payload);
 
         console.log(`\n===========================================`);
-        console.log(`Testing ${numEmployees} Employees (${payload.shifts.length * 7} shifts total, ~${payload.employees.length * 5} assignments expected)`);
+        console.log(`Testing ${numEmployees} Employees`);
         console.log(`Sending request, timeout set to ${payload.solver_timeout_seconds}s...`);
 
         const req = http.request(URL, {

===DIFF===

@@ -84,7 +84,7 @@
 
         console.log(`\n===========================================`);
         console.log(`Testing ${numEmployees} Employees`);
-        console.log(`Sending request, timeout set to ${payload.solver_timeout_seconds}s...`);
+        console.log(`Sending request, timeout set to ${payload.time_limit_seconds}s...`);
 
         const req = http.request(URL, {
             method: 'POST',

===DIFF===

@@ -41,8 +41,8 @@
             "position": role,
             "category": isPermanent ? "Permanent" : "Contract",
             "gender": i % 2 === 0 ? "Male" : "Female",
-            "hourly_wage": wage,
-            "rating": rating, // v2 format
+            "rate": wage,
+            "rating": rating,
             "skills": hasSkill ? ["Customer Service"] : [],
             "unavailable_times": []
         });

===DIFF===

@@ -37,7 +37,7 @@
 
         employees.push({
             "employee_id": `EMP${i}`,
-            "employee_name": `Worker ${i}`,
+            "name": `Worker ${i}`,
             "position": role,
             "category": isPermanent ? "Permanent" : "Contract",
             "gender": i % 2 === 0 ? "Male" : "Female",

===DIFF===

@@ -60,7 +60,7 @@
 
     const rolesArr = [];
     roles.forEach(r => {
-        rolesArr.push({ "role": r, "max_workers": Math.ceil(workersPerShift / roles.length) });
+        rolesArr.push({ "role_name": r, "max_workers": Math.ceil(workersPerShift / roles.length) });
     });
 
     return {

===DIFF===

@@ -38,8 +38,8 @@
         employees.push({
             "employee_id": `EMP${i}`,
             "name": `Worker ${i}`,
-            "position": role,
-            "category": isPermanent ? "Permanent" : "Contract",
+            "role": role,
+            "employeeType": isPermanent ? "Permanent" : "Contract",
             "gender": i % 2 === 0 ? "Male" : "Female",
             "rate": wage,
             "rating": rating,

===DIFF===

@@ -67,8 +67,8 @@
         "shift_name": "Weekly Schedule",
         "start_date": "2026-06-01",
         "end_date": "2026-06-07",
-        "start_time": "08:00:00",
-        "end_time": "16:00:00",
+        "start_time": "08:00",
+        "end_time": "16:00",
         "roles": rolesArr,
         "existing_users": employees,
         "active_constraints": mappedConstraints,

===DIFF===

@@ -27,11 +27,11 @@
         });
     }
 
-    // 300 per day total. We have 2 shifts (Morning, Evening), so 150 workers per shift.
-    // Divide 150 workers across 3 roles: 50 each.
+    // 300 per day total.
+    // Divide 300 workers across 3 roles: 100 each.
     const rolesArr = [];
     roles.forEach(r => {
-        rolesArr.push({ "role_name": r, "max_workers": 50 });
+        rolesArr.push({ "role_name": r, "max_workers": 100 });
     });
 
     const activeConstraints = [

===DIFF===

@@ -10992,7 +10992,7 @@
                     .penalizeLong(resolveScore("wageOptimization", config.getSeverity()),
                             a -> {
                                 double wageRatio = a.getHourlyWage() / averageWage;
-                                return (long) (wageRatio * 100 * 10); // 10x weight as requested
+                                return (long) (wageRatio * 100);
                             })
                     .asConstraint("wageOptimization");
         }

===DIFF===

@@ -123,11 +123,15 @@
         });
     }
 
-    const multipliers = [0, 25, 50, 100, 200, 500, 1000];
-    console.log("| Multiplier | Avg Rating Selected | Avg Wage Selected |");
-    console.log("| :--- | :--- | :--- |");
+    const multipliers = [0, 50, 100, 200, 500];
+    console.log("| Multiplier | Avg Rating Selected | Avg Wage Selected | Total Soft Score |");
+    console.log("| :--- | :--- | :--- | :--- |");
     for (const m of multipliers) {
         let payload = generatePayload(emps4, m, 10);
+        // Change time limit to 300 seconds for deep optimization
+        payload.time_limit_seconds = 300;
+        payload.unimproved_time_limit_seconds = 60; 
+        
         let res4 = await runTest(payload, `Test 4 (${m})`);
         if (res4 && res4.assignments_by_date) {
             let totalW = 0;
@@ -138,7 +138,8 @@
             });
             const avgR = (totalR / shifts.length).toFixed(2);
             const avgW = (totalW / shifts.length).toFixed(2);
-            console.log(`| ${m} | ${avgR} | $${avgW} |`);
+            const softScore = res4.solver_score ? res4.solver_score : "N/A";
+            console.log(`| ${m} | ${avgR} | $${avgW} | ${softScore} |`);
         }
     }
 }

===DIFF===

@@ -123,20 +123,23 @@
         });
     }
 
-    const multipliers = [0, 50, 100, 200, 500];
-    console.log("| Multiplier | Avg Rating Selected | Avg Wage Selected | Total Soft Score |");
+    const times = [1, 5, 30, 300];
+    console.log("| Time Limit | Avg Rating | Avg Wage | Total Soft Score |");
     console.log("| :--- | :--- | :--- | :--- |");
-    for (const m of multipliers) {
+    
+    // We will use multiplier 500
+    const m = 500;
+    
+    for (const t of times) {
         let payload = generatePayload(emps4, m, 10);
-        // Change time limit to 300 seconds for deep optimization
-        payload.time_limit_seconds = 300;
-        payload.unimproved_time_limit_seconds = 60; 
+        payload.time_limit_seconds = t;
+        payload.unimproved_time_limit_seconds = t; // Let it run the full time
         
-        let res4 = await runTest(payload, `Test 4 (${m})`);
-        if (res4 && res4.assignments_by_date) {
+        let res5 = await runTest(payload, `Test 5 (${t}s)`);
+        if (res5 && res5.assignments_by_date) {
             let totalW = 0;
             let totalR = 0;
-            const shifts = res4.assignments_by_date["2026-06-01"] || [];
+            const shifts = res5.assignments_by_date["2026-06-01"] || [];
             shifts.forEach(s => {
                 totalW += s.wage;
                 totalR += s.rating;
@@ -143,7 +143,7 @@
             const avgR = (totalR / shifts.length).toFixed(2);
             const avgW = (totalW / shifts.length).toFixed(2);
-            const softScore = res4.solver_score ? res4.solver_score : "N/A";
-            console.log(`| ${m} | ${avgR} | $${avgW} | ${softScore} |`);
+            const softScore = res5.solver_score ? res5.solver_score : "N/A";
+            console.log(`| ${t}s | ${avgR} | $${avgW} | ${softScore} |`);
         }
     }
 }

===DIFF===

@@ -34,11 +34,17 @@
             res.on('end', () => {
                 try {
                     if (res.statusCode !== 200 && res.statusCode !== 409) {
+                        console.error(`HTTP ${res.statusCode}: ${data}`);
                         resolve({ error: `HTTP ${res.statusCode}` });
                         return;
                     }
-                    resolve(JSON.parse(data));
+                    const parsed = JSON.parse(data);
+                    if (parsed.status === "error") {
+                        console.error(`Solver Error: ${parsed.message}`);
+                    }
+                    resolve(parsed);
                 } catch (e) {
+                    console.error("Parse Error:", e);
                     resolve({ error: "Parse Error" });
                 }
             });

===DIFF===

@@ -92,8 +92,7 @@
 async function runTestSuite() {
     console.log("==========================================");
     console.log("TEST 1 - Max Weekly Hours (40h limit)");
-    // 1 Employee, 6 shifts of 8 hours (48 hrs)
-    const emps1 = [{ "employee_id": "EMP1", "name": "A", "role": "Cashier", "rate": 20, "rating": 5 }];
+    const emps1 = [{ "employee_id": "T1_EMP", "name": "T1_A", "role": "Cashier", "rate": 20, "rating": 5 }];
     const roles1 = [{ "role_name": "Cashier", "max_workers": 1 }];
     for (const sev of ["HARD", "MEDIUM", "SOFT"]) {
         const c1 = [
@@ -106,9 +106,7 @@
 
     console.log("\n==========================================");
     console.log("TEST 2 - Max Daily Hours (8h limit)");
-    // 1 Employee, 2 shifts in same day (handled by sending 2 roles requiring 1 worker each, overlapping)
-    // Wait, Timefold only schedules 1 shift per day per employee. We can't do multiple shifts per day natively unless we disable noOverlappingShifts.
-    const emps2 = [{ "employee_id": "EMP1", "name": "A", "role": "Cashier", "rate": 20, "rating": 5 }];
+    const emps2 = [{ "employee_id": "T2_EMP", "name": "T2_A", "role": "Cashier", "rate": 20, "rating": 5 }];
     const roles2 = [{ "role_name": "Cashier", "max_workers": 2 }];
     for (const sev of ["HARD", "MEDIUM", "SOFT"]) {
         const c2 = [
@@ -115,10 +115,17 @@
             { "name": "maxDailyHours", "severity": sev, "value": 8.0 }
-            // noOverlappingShifts is NOT enabled, so they can theoretically take 2
         ];
         let p = buildBasePayload("T2", emps2, roles2, c2, 1);
         p.start_time = "08:00"; p.end_time = "14:00"; // 6 hours. 2 shifts = 12 hours.
+        // Wait! The solver on
<truncated 3633 bytes>
= getEmployeeAssignments(r, "T5_B");
         console.log(`[${sev}] Employee A ($20): ${aCount} shifts | Employee B ($40): ${bCount} shifts`);
     }
 
@@ -171,7 +171,7 @@
     console.log("TEST 6 - Permanent Priority");
     const emps6 = [
-        { "employee_id": "A", "name": "PermWorker", "role": "Cashier", "rate": 35, "rating": 5, "employeeType": "Permanent" },
-        { "employee_id": "B", "name": "ContractWorker", "role": "Cashier", "rate": 20, "rating": 5, "employeeType": "Contract" }
+        { "employee_id": "T6_P", "name": "T6_PermWorker", "role": "Cashier", "rate": 35, "rating": 5, "employeeType": "Permanent" },
+        { "employee_id": "T6_C", "name": "T6_ContractWorker", "role": "Cashier", "rate": 20, "rating": 5, "employeeType": "Contract" }
     ];
     for (const sev of ["HARD", "MEDIUM", "SOFT"]) {
         const c6 = [
@@ -180,8 +180,8 @@
         ];
         let p = buildBasePayload("T6", emps6, roles1, c6, 1);
         let r = await runTest(p, `T6-${sev}`);
-        let pCount = getEmployeeAssignments(r, "PermWorker");
-        let cCount = getEmployeeAssignments(r, "ContractWorker");
+        let pCount = getEmployeeAssignments(r, "T6_PermWorker");
+        let cCount = getEmployeeAssignments(r, "T6_ContractWorker");
         console.log(`[${sev}] Permanent ($35): ${pCount} | Contract ($20): ${cCount}`);
     }
 
@@ -188,5 +188,5 @@
     console.log("TEST 7 - Every Shift Planned vs Wage Optimization");
-    const emps7 = [{ "employee_id": "EMP1", "name": "A", "role": "Cashier", "rate": 50, "rating": 5 }];
+    const emps7 = [{ "employee_id": "T7_EMP", "name": "T7_A", "role": "Cashier", "rate": 50, "rating": 5 }];
     for (const sev of ["HARD", "MEDIUM", "SOFT"]) {
         const c7 = [
             { "name": "everyShiftPlanned", "severity": sev },

===DIFF===

@@ -71,10 +71,11 @@
     });
 }
 
-function analyze(res, severity) {
+function analyze(res, label) {
     let assignments = 0;
-    let overtimeHours = 0;
-    let otInstances = 0;
+    
+    // Manual Overtime Calculation
+    let empHours = {};
 
     if (res && res.assignments_by_date) {
         for (const date in res.assignments_by_date) {
@@ -81,13 +81,21 @@
                 assignments++;
-                if (a.ot_hours > 0) {
-                    overtimeHours += a.ot_hours;
-                    otInstances++;
-                }
-            });
-        }
-    }
-    
-    console.log(`\n[${severity}] Results`);
+                // we know each shift is 8 hours (08:00 to 16:00)
+                if (!empHours[a.employee_id]) empHours[a.employee_id] = 0;
+                empHours[a.employee_id] += 8;
+            });
+        }
+    }
+    
+    let overtimeHours = 0;
+    let otInstances = 0;
+    for (let emp in empHours) {
+        if (empHours[emp] > 40) {
+            overtimeHours += (empHours[emp] - 40);
+            otInstances++;
+        }
+    }
+    
+    console.log(`\n[${label}] Results`);
     console.log(`Assignments: ${assignments} / 2100`);
     console.log(`Coverage: ${((assignments / 2100) * 100).toFixed(2)}%`);
     console.log(`Total Overtime Hours: ${overtimeHours}`);
@@ -94,4 +94,7 @@
-    console.log(`Soft Score: ${res.solver_score || "N/A"}`);
+    console.log(`Score: ${res.solver_score || "N/A"}`);
+    if (res.score_explanation) {
+        console.log(`Score Explanation: \n${res.score_explanation.substring(0, 1500)}...`); // Print first 1500 chars to avoid flooding
+    }
 }
 
 async function main() {
@@ -98,7 +98,8 @@
     const severities = ["HARD", "MEDIUM", "SOFT"];
     
+    // Standard transition
     for (const sev of severities) {
-        console.log(`\nRunning ${sev}...`);
+        console.log(`\nRunning maxWeeklyHours=${sev}, everyShiftPlanned=HARD...`);
         const payload = generatePayload(sev);
         const res = await runTest(payload, sev);
         analyze(res, sev);
@@ -105,3 +105,12 @@
+
+    // Reverse Migration Test
+    console.log(`\nRunning Reverse Migration: maxWeeklyHours=HARD, everyShiftPlanned=MEDIUM...`);
+    const payloadReverse = generatePayload("HARD");
+    payloadReverse.active_constraints.forEach(c => {
+        if (c.name === "everyShiftPlanned") c.severity = "MEDIUM";
+    });
+    const resRev = await runTest(payloadReverse, "Reverse Migration");
+    analyze(resRev, "Reverse Migration");
 }
 
 main();

===DIFF===

@@ -61,6 +61,11 @@
 function analyze(res, label) {
     let assignedEmp = "None";
     
+    if (res.error) {
+        console.log("ERROR:", res.error);
+        return;
+    }
+
     if (res && res.assignments_by_date && res.assignments_by_date["2026-06-01"]) {
         const a = res.assignments_by_date["2026-06-01"][0];
         if (a) {
@@ -67,4 +67,6 @@
         }
+    } else {
+        console.log("Full response:", res);
     }
     
     console.log(`\n[${label}] Results`);

===DIFF===

@@ -4,8 +4,8 @@
 
 function generatePayload(severity) {
     const employees = [
-        { "employee_id": "EMP_JAVA", "name": "Java Dev", "role": "Permanent", "rate": 50, "rating": 5, "skills": ["Java"] },
-        { "employee_id": "EMP_PYTHON", "name": "Python Dev", "role": "Permanent", "rate": 20, "rating": 5, "skills": ["Python"] }
+        { "employee_id": "EMP_JAVA", "name": "Java Dev", "role": "Java", "rate": 50, "rating": 5, "skills": ["Java"] },
+        { "employee_id": "EMP_PYTHON", "name": "Python Dev", "role": "Java", "rate": 20, "rating": 5, "skills": ["Python"] }
     ];
 
     const roles = [{

===DIFF===

@@ -4,8 +4,8 @@
 
 function generatePayload(severity) {
     const employees = [
-        { "employee_id": "EMP_JAVA", "name": "Java Dev", "role": "Java", "rate": 50, "rating": 5, "skills": ["Java"] },
-        { "employee_id": "EMP_PYTHON", "name": "Python Dev", "role": "Java", "rate": 20, "rating": 5, "skills": ["Python"] }
+        { "employee_id": "EMP_JAVA", "name": "Java Dev", "role": "Java", "rate": 100, "rating": 5, "skills": ["Java"] },
+        { "employee_id": "EMP_PYTHON", "name": "Python Dev", "role": "Java", "rate": 10, "rating": 5, "skills": ["Python"] }
     ];
 
     const roles = [{

===DIFF===

@@ -69,7 +69,7 @@
     if (res && res.assignments_by_date && res.assignments_by_date["2026-06-01"]) {
         const a = res.assignments_by_date["2026-06-01"][0];
         if (a) {
-            assignedEmp = a.employee_id;
+            assignedEmp = a.employeeName;
         }
     } else {
         console.log("Full response:", res);

===DIFF===

@@ -10,7 +10,8 @@
 
     const roles = [{
         "role_name": "Java",
-        "max_workers": 1
+        "max_workers": 1,
+        "required_skills": ["Java"]
     }];
 
     const constraints = [

===DIFF===

@@ -9,21 +9,21 @@
 > [!CAUTION]
 > **To proceed to execution, please review and approve this plan.** If there are any other specific dirty-payload scenarios you've seen in production that I haven't included, let me know!
 
-## Proposed Changes
-
-### 1. `noOverlappingShifts` Verification (No Code Change)
-Before moving on, I investigated your excellent challenge regarding overlapping shifts. In the V2 endpoint (`/shifts/assign-v2`), the JSON payload accepts exactly **one global `start_time` and `end_time`** per request. The solver then generates exactly **one `EmployeeAssignment` entity per employee per date**.
-- **Can Morning and Evening be assigned on the same day?** No, because the V2 payload only supports one time window per request.
-- **Can 2 partial shifts exist?** No, same reason.
-- **Can one employee appear twice in input?** We will test this in Phase 2!
-Because of this top-level configuration architecture, `noOverlappingShifts` is indeed dead code for V2, as the entity model physically restricts an employee to at most one assignment in the requested time window. 
-
-### 2. Dirty Payload Testing
-We will build a `test_dirty_payloads.js` script to hit the V2 endpoint with real-world, malformed, and adversarial inputs to ensure the application doesn't crash and the solver still produces an optimal schedule:
-- Missing skills (already verified, but will include in the suite)
-- `null`, missing, or out-of-bounds ratings
-- Duplicate employees in the `existing_users` array
-- Employees missing critical fields (`role`, `rate`, `name`)
+## QA Checklist & Proposed Changes
+
+The final QA suite is designed to bulletproof the backend API and a
<truncated 2113 bytes>
st_concurrency.js`)
+To guarantee no static state leakage (a critical vulnerability we addressed earlier), we will hammer the API with simultaneous requests.
+- **The Test**: Launch 50 simultaneous POST requests to `/shifts/assign-v2`.
+- **The Mix**: Randomly mixed severity configurations (e.g., 15 HARD, 20 MEDIUM, 15 SOFT).
+- **The Verification**: We will assert that Request A's score and assignments are never corrupted by Request B's configuration.
+
+### 3. Repeatability Test (`test_repeatability.js`)
+To verify the solver is deterministic and has no hidden non-determinism across runs.
+- **The Test**: Run the exact same payload with the exact same constraints 100 times sequentially.
+- **The Verification**: Ensure the score, assignment count, and coverage remain identical across all 100 runs.
+
+### 4. Constraint Toggle Test (`test_toggles.js`)
+To verify that `enabled=false` actually deactivates the constraint in the solver logic, rather than just hiding it in the UI.
+- **The Test**: Send payloads with constraints toggled between `enabled=true` and `enabled=false`.
+- **The Verification**: Ensure disabled constraints completely disappear from the `Score Explanation`.
+
+### 5. Final Scale Validation (`test_edge_scale.js`)
+To prove the dynamic severity engine drastically alters behavior at scale.
+- **The Test**: 500 employees, 2100 shifts.
+- Run 1: All constraints `HARD`.
+- Run 2: All constraints `SOFT`.
+- **The Verification**: We expect to see drastically different behavior in coverage, overtime, skill violations, cost, and rating. If behavior barely changes, the engine is not fully dynamic.
+
+*(Note: `noOverlappingShifts` is kept in the codebase but marked as inactive for V2, as the entity model intrinsically prevents overlaps for single-window requests.)*
 

===DIFF===



===DIFF===

@@ -11581,7 +11581,7 @@
                         
                         EmployeeInfo emp = allEmployees.get(eId);
                         Scheduler.EmployeeAssignment pinnedEntity = new Scheduler.EmployeeAssignment(
-                                eId + "_pinned_" + dStr, eId, emp.getName(), dStr,
+                                eId + "_pinned_" + sName + "_" + dStr, eId, emp.getName(), dStr,
                                 emp.getCategory(), emp.getGender(), emp.getDepartment(), emp.getPosition()
                         );
                         pinnedEntity.setShift(sName);

===DIFF===

@@ -11377,7 +11377,13 @@
 
             for (Map<String, Object> user : existingUsers) {
                 String name = (String) user.get("name");
+                if (name == null || name.trim().isEmpty()) {
+                    name = "Unknown Employee";
+                }
                 Number rateObj = (Number) user.get("rate");
+                if (rateObj == null) {
+                    rateObj = 0.0;
+                }
                 String unit = (String) user.get("unit");
                 Object ratingObj = user.get("rating");
                 String role = (String) user.get("role");

===DIFF===

@@ -11323,6 +11323,21 @@
             }
 
             boolean prioritizePermanent = Boolean.TRUE.equals(input.getOrDefault("prioritize_permanent", true));
+            LocalDate startDate;
+            LocalDate endDate;
+            LocalTime startLocalTime;
+            LocalTime endLocalTime;
+
+            try {
+                startDate = LocalDate.parse(startDateStr);
+                endDate = LocalDate.parse(endDateStr);
+                startLocalTime = LocalTime.parse(startTime);
+                endLocalTime = LocalTime.parse(endTime);
+            } catch (java.time.format.DateTimeParseException e) {
+                return Response.status(Response.Status.BAD_REQUEST)
+                        .entity(Map.of("error", "Invalid date or time format. Use ISO-8601 (e.g. YYYY-MM-DD and HH:MM)"))
+                        .build();
+            }
 
             if (shiftName == null || startDateStr == null || endDateStr == null ||
                     startTime == null || endTime == null ||

===DIFF===

@@ -11348,8 +11348,7 @@
                 )).build();
             }
 
-            LocalDate startDate = LocalDate.parse(startDateStr);
-            LocalDate endDate = LocalDate.parse(endDateStr);
+
 
             if (endDate.isBefore(startDate)) {
                 return Response.status(400).entity(Map.of("error", "End date must be after start date")).build();

===DIFF===

@@ -11323,6 +11323,15 @@
             }
 
             boolean prioritizePermanent = Boolean.TRUE.equals(input.getOrDefault("prioritize_permanent", true));
+            if (shiftName == null || startDateStr == null || endDateStr == null ||
+                    startTime == null || endTime == null ||
+                    roles == null || roles.isEmpty() || existingUsers == null) {
+                return Response.status(400).entity(Map.of(
+                        "error", "Missing required fields",
+                        "required", Arrays.asList("shift_name", "start_date", "end_date", "start_time", "end_time", "roles", "existing_users")
+                )).build();
+            }
+
             LocalDate startDate;
             LocalDate endDate;
             LocalTime startLocalTime;
@@ -11332,7 +11332,7 @@
                 endDate = LocalDate.parse(endDateStr);
                 startLocalTime = LocalTime.parse(startTime);
                 endLocalTime = LocalTime.parse(endTime);
-            } catch (java.time.format.DateTimeParseException e) {
+            } catch (java.time.format.DateTimeParseException | NullPointerException e) {
                 return Response.status(Response.Status.BAD_REQUEST)
                         .entity(Map.of("error", "Invalid date or time format. Use ISO-8601 (e.g. YYYY-MM-DD and HH:MM)"))
                         .build();
@@ -11339,13 +11339,4 @@
 
-            if (shiftName == null || startDateStr == null || endDateStr == null ||
-                    startTime == null || endTime == null ||
-                    roles == null || roles.isEmpty() || existingUsers == null) {
-                return Response.status(400).entity(Map.of(
-                        "error", "Missing required fields",
-                        "required", Arrays.asList("shift_name", "start_date", "end_date", "start_time", "end_time", "roles", "existing_users")
-                )).build();
-            }
-
 
 
             if (endDate.isBefore(startDate)) {

===DIFF===

@@ -11362,8 +11362,6 @@
                 current = current.plusDays(1);
             }
 
-            LocalTime startLocalTime = LocalTime.parse(startTime);
-            LocalTime endLocalTime = LocalTime.parse(endTime);
 
             // Shift duration
             double shiftDurationHours;

===DIFF===

@@ -11429,8 +11429,11 @@
                 employeeTypes.put(employeeId, employeeType);
 
                 double finalHourlyWage = hourlyWage;
-                EmployeeInfo empInfo = employeeInfo.computeIfAbsent(employeeId, k ->
-                        new EmployeeInfo(employeeId, name, employeeType, gender, finalHourlyWage,
+                String finalName = name;
+                String finalEmployeeType = employeeType;
+                String finalGender = gender;
+                EmployeeInfo empInfo = employeeInfo.computeIfAbsent(employeeId, k ->
+                        new EmployeeInfo(employeeId, finalName, finalEmployeeType, finalGender, finalHourlyWage,
                                 "MGR001", "Operations", role, email, phone)
                 );
 

===DIFF===

@@ -142,7 +142,7 @@
             console.log(`❌ FAILED (Server Error 500)`);
             console.log(`Response:`, res.body);
             failed++;
-        } else if (res.statusCode === 400 || res.statusCode === 200 || res.statusCode === 422) {
+        } else if (res.statusCode === 400 || res.statusCode === 200 || res.statusCode === 422 || res.statusCode === 409) {
             console.log(`✅ PASSED (${res.statusCode})`);
             passed++;
         } else {

===DIFF===

@@ -1,5 +1,5 @@
-- `[/]` Dirty Payloads (`test_dirty_payloads.js`)
-- `[ ]` Concurrency (50+ requests) (`test_concurrency.js`)
+- `[x]` Dirty Payloads (`test_dirty_payloads.js`)
+- `[/]` Concurrency (50+ requests) (`test_concurrency.js`)
 - `[ ]` Repeatability (`test_repeatability.js`)
 - `[ ]` Constraint enable/disable verification (`test_toggles.js`)
 - `[ ]` Final Scale Edge Case (All HARD vs All SOFT) (`test_edge_scale.js`)

===DIFF===

@@ -10844,11 +10844,15 @@
     // ============ ShiftConstraintsV2 — Dynamic Constraint Provider ============
     public static class ShiftConstraintsV2 implements ConstraintProvider {
 
-        private static Map<String, Integer> maxWorkersPerRole = new HashMap<>();
-        private static Map<String, List<Integer>> requiredRatingsPerRole = new HashMap<>();
-        private static Map<String, List<String>> requiredSkillsPerRole = new HashMap<>();
-        private static List<ConstraintConfig> activeConstraintConfigs = new ArrayList<>();
-        private static double averageWage = 1.0;
+        private static class Context {
+            Map<String, Integer> maxWorkersPerRole = new HashMap<>();
+            Map<String, List<Integer>> requiredRatingsPerRole = new HashMap<>();
+            Map<String, List<String>> requiredSkillsPerRole = new HashMap<>();
+            List<ConstraintConfig> activeConstraintConfigs = new ArrayList<>();
+            double averageWage = 1.0;
+        }
+
+        private static final ThreadLocal<Context> threadContext = ThreadLocal.withInitial(Context::new);
 
         public ShiftConstraintsV2() {}
 
@@ -10858,23 +10858,28 @@
                 List<ConstraintConfig> configs,
                 double avgWage) {
 
-            maxWorkersPerRole.clear();
-            requiredRatingsPerRole.clear();
-            requiredSkillsPerRole = skillsMap != null ? new HashMap<>(skillsMap) : new HashMap<>();
-            activeConstraintConfigs = configs != null ? new ArrayList<>(configs) : new ArrayList<>();
-            averageWage = avgWage > 0 ? avgWage : 1.0;
+            Context ctx = threadContext.get();
+            ctx.maxWorkersPerRole.clear();
+            ctx.requiredRatingsPerRole.clear();
+            ctx.requiredSkillsPerRole = skillsMap != null ? new HashMap<>(skillsMap) : new HashMap<>();
+            ctx.activeConstraintConfigs = configs != null ? new ArrayList<>(configs) : new ArrayList<>();
+            ctx.averageWage = avgWage > 0 ? avgWage : 1.0;
 
             for (Scheduler.ShiftSchedule.RoleLimit l : roleLimits) {
-                maxWorkersPerRole.put(l.getRoleName(), l.getMaxWorkers());
+                ctx.maxWorkersPerRole.put(l.getRoleName(), l.getMaxWorkers());
             }
             for (Scheduler.ShiftSchedule.RatingRequirement r : ratingRequirements) {
-                requiredRatingsPerRole.put(r.getRoleName(), r.getAllowedRatings());
-            }
-
-            System.out.println("✅ ShiftConstraintsV2 configured: " +
-                    maxWorkersPerRole.size() + " role limits, " +
-                    requiredSkillsPerRole.size() + " skill requirements, " +
-                    activeConstraintConfigs.size() + " constraint configs");
+                ctx.requiredRatingsPerRole.put(r.getRoleName(), r.getAllowedRatings());
+            }
+
+            System.out.println("✅ ShiftConstraintsV2 configured on thread " + Thread.currentThread().getName() + ": " +
+                    ctx.maxWorkersPerRole.size() + " role limits, " +
+                    ctx.requiredSkillsPerRole.size() + " skill requirements, " +
+                    ctx.activeConstraintConfigs.size() + " constraint configs");
+        }
+
+        public static void clearConfiguration() {
+            threadContext.remove();
         }
 
         private HardMediumSoftLongScore resolveScore(String constraintName, String severity) {

===DIFF===

@@ -10937,7 +10937,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> {
-                        List<String> required = requiredSkillsPerRole.getOrDefault(a.getPosition(), List.of());
+                        List<String> required = threadContext.get().requiredSkillsPerRole.getOrDefault(a.getPosition(), List.of());
                         if (required.isEmpty()) return false;
                         long matchedCount = required.size() - a.getMissingSkillCount();
                         double actualPct = (matchedCount * 100.0) / required.size();
@@ -10999,7 +10999,7 @@
                     .filter(a -> a.getShift() != null)
                     .penalizeLong(resolveScore("wageOptimization", config.getSeverity()),
                             a -> {
-                                double wageRatio = a.getHourlyWage() / averageWage;
+                                double wageRatio = a.getHourlyWage() / threadContext.get().averageWage;
                                 return (long) (wageRatio * 100);
                             })
                     .asConstraint("wageOptimization");
@@ -11132,7 +11132,7 @@
             );
 
             // Dynamic constraints from DB
-            for (ConstraintConfig config : activeConstraintConfigs) {
+            for (ConstraintConfig config : threadContext.get().activeConstraintConfigs) {
                 if (!config.isEnabled()) continue;
                 Constraint c = buildConstraint(factory, config);
                 if (c != null) constraints.add(c);

===DIFF===

@@ -11667,192 +11667,6 @@
 
             ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, constraintConfigs, averageWage);
 
-            ShiftScheduleV2 problem = new ShiftScheduleV2(planningEntities, possibleShifts, roleLimits, ratingRequirements);
-            problem.setRequestedShiftName(shiftName);
-            problem.setPrioritizePermanent(prioritizePermanent);
-
-            long timeLimit = input.containsKey("time_limit_seconds") ? ((Number) input.get("time_limit_seconds")).longValue() : 60L;
-            long unimprovedLimit = input.containsKey("unimproved_time_limit_seconds") ? ((Number) input.get("unimproved_time_limit_seconds")).longValue() : 30L;
-
-            SolverConfig solverConfig = new SolverConfig()
-                    .withSolutionClass(ShiftScheduleV2.class)
-                    .withEntityClasses(Scheduler.EmployeeAssignment.class)
-                    .withConstraintProviderClass(ShiftConstraintsV2.class)
-                    .withTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
-                            .withSpentLimit(Duration.ofSeconds(timeLimit))
-                            .withUnimprovedSpentLimit(Duration.ofSeconds(unimprovedLimit)));
-
-            SolverFactory<ShiftScheduleV2> solverFactory = SolverFactory.create(solverConfig);
-            Solver<ShiftScheduleV2> solver = solverFactory.buildSolver();
-
-            System.out.println("🔧 Starting V2 solver...");
-            long solverStart = System.currentTimeMillis();
-            ShiftScheduleV2 solved = solver.solve(problem);
-            long solverTimeMs = System.cur
<truncated 6979 bytes>
skipped_count", skippedCount);
-            response.put("override_used", overrideExisting);
-            response.put("ot_threshold_hours", otThreshold);
-            response.put("prioritize_permanent", prioritizePermanent);
-
-            // Skill match info
-            if (!requiredSkillsMap.isEmpty()) {
-                response.put("required_skills_by_role", requiredSkillsMap);
-            }
-
-            // Active constraints info
-            List<Map<String, Object>> activeConstraints = new ArrayList<>();
-            for (ConstraintConfig cc : constraintConfigs) {
-                if (cc.isEnabled()) {
-                    Map<String, Object> cInfo = new LinkedHashMap<>();
-                    cInfo.put("name", cc.getConstraintName());
-                    cInfo.put("severity", cc.getSeverity());
-                    if (cc.getParameterValue() != null) cInfo.put("value", cc.getParameterValue());
-                    activeConstraints.add(cInfo);
-                }
-            }
-            response.put("active_constraints", activeConstraints);
-
-            response.put("breakSchedulesCount", breakSchedules.size());
-            response.put("breakConfiguration", Map.of(
-                    "scheduleBreaks", scheduleBreaks,
-                    "breakDurationMinutes", breakDurationMinutes,
-                    "breakAfterHours", breakAfterHours
-            ));
-
-            if (!skippedPerDate.isEmpty()) {
-                response.put("skipped_by_date", skippedPerDate);
-            }
 
             // Assignment details grouped by date
             response.put("assignments_by_date", assignmentDetails);

===DIFF===

@@ -11667,13 +11667,203 @@
 
             ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, constraintConfigs, averageWage);
 
-
-            // Assignment details grouped by date
-            response.put("assignments_by_date", assignmentDetails);
-            response.put("score_explanation", scoreExplanation);
-            response.put("message", "Successfully assigned shifts (V2 with dynamic constraints)!");
-
-            return Response.ok(response).build();
+            try {
+                ShiftScheduleV2 problem = new ShiftScheduleV2(planningEntities, possibleShifts, roleLimits, ratingRequirements);
+                problem.setRequestedShiftName(shiftName);
+                problem.setPrioritizePermanent(prioritizePermanent);
+
+                long timeLimit = input.containsKey("time_limit_seconds") ? ((Number) input.get("time_limit_seconds")).longValue() : 60L;
+                long unimprovedLimit = input.containsKey("unimproved_time_limit_seconds") ? ((Number) input.get("unimproved_time_limit_seconds")).longValue() : 30L;
+
+                SolverConfig solverConfig = new SolverConfig()
+                        .withSolutionClass(ShiftScheduleV2.class)
+                        .withEntityClasses(Scheduler.EmployeeAssignment.class)
+                        .withConstraintProviderClass(ShiftConstraintsV2.class)
+                        .withTerminationConfig(new ai.timefold.solver.core.config.solver.termination.TerminationConfig()
+                                .withSpentLimit(Duration.ofSeconds(timeLimit))
+                                .withUnimprovedSpentLimit(Duration.ofSeconds(unimprovedLi
<truncated 8297 bytes>
Map);
+                }
+
+                // Active constraints info
+                List<Map<String, Object>> activeConstraints = new ArrayList<>();
+                for (ConstraintConfig cc : constraintConfigs) {
+                    if (cc.isEnabled()) {
+                        Map<String, Object> cInfo = new LinkedHashMap<>();
+                        cInfo.put("name", cc.getConstraintName());
+                        cInfo.put("severity", cc.getSeverity());
+                        if (cc.getParameterValue() != null) cInfo.put("value", cc.getParameterValue());
+                        activeConstraints.add(cInfo);
+                    }
+                }
+                response.put("active_constraints", activeConstraints);
+
+                response.put("breakSchedulesCount", breakSchedules.size());
+                response.put("breakConfiguration", Map.of(
+                        "scheduleBreaks", scheduleBreaks,
+                        "breakDurationMinutes", breakDurationMinutes,
+                        "breakAfterHours", breakAfterHours
+                ));
+
+                if (!skippedPerDate.isEmpty()) {
+                    response.put("skipped_by_date", skippedPerDate);
+                }
+
+                // Assignment details grouped by date
+                response.put("assignments_by_date", assignmentDetails);
+                response.put("score_explanation", scoreExplanation);
+                response.put("message", "Successfully assigned shifts (V2 with dynamic constraints)!");
+
+                return Response.ok(response).build();
+            } finally {
+                ShiftConstraintsV2.clearConfiguration();
+            }
 
         } catch (Exception e) {
             e.printStackTrace();

===DIFF===

@@ -10965,7 +10965,7 @@
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
                     .filter(a -> {
-                        List<Integer> allowed = requiredRatingsPerRole.getOrDefault(a.getPosition(), List.of(1, 2, 3, 4, 5));
+                        List<Integer> allowed = threadContext.get().requiredRatingsPerRole.getOrDefault(a.getPosition(), List.of(1, 2, 3, 4, 5));
                         boolean fails = !allowed.contains(a.getPerformanceRating());
                         if (fails) {
                             System.out.println("❌ RATING MISMATCH: emp=" + a.getEmployeeId() + " pos=" + a.getPosition() + " rating=" + a.getPerformanceRating() + " allowed=" + allowed);

===DIFF===

@@ -10912,31 +10912,33 @@
             };
         }
 
-        private Constraint buildConstraint(ConstraintFactory factory, ConstraintConfig config) {
-            return switch (config.getConstraintId()) {
-                case 1 -> buildSkillMatch(factory, config);
-                case 2 -> buildNoOverlap(factory, config);
-                case 3 -> buildUnavailableTimeslot(factory, config);
-                case 4 -> buildEveryShiftPlanned(factory, config);
-                case 5 -> buildWageOptimization(factory, config);
-                case 6 -> buildMaxDailyHours(factory, config);
-                case 7 -> buildMaxWeeklyHours(factory, config);
-                case 8 -> buildOvertimeThreshold(factory, config);
-                case 9 -> buildBreakConstraint(factory, config);
-                case 10 -> buildConsecutiveShifts(factory, config);
-                case 11 -> buildPermanentPriority(factory, config);
-                case 12 -> buildMaximizeRating(factory, config);
-                default -> null;
-            };
+        private boolean isConstraintActive(String name, String severity) {
+            Context ctx = threadContext.get();
+            for (ConstraintConfig cc : ctx.activeConstraintConfigs) {
+                if (cc.isEnabled() && cc.getConstraintName().equals(name) && cc.getSeverity().equalsIgnoreCase(severity)) {
+                    return true;
+                }
+            }
+            return false;
+        }
+
+        private double getConstraintParameter(String name, double defaultValue) {
+            Context ctx = threadContext.get();
+            for (ConstraintConfig cc : ctx.activeConstra
<truncated 18061 bytes>
ints
@@ -11126,14 +11126,24 @@
                     .asConstraint("maxWorkersPerRolePerShift")
             );
 
-            // Dynamic constraints from DB
-            for (ConstraintConfig config : threadContext.get().activeConstraintConfigs) {
-                if (!config.isEnabled()) continue;
-                Constraint c = buildConstraint(factory, config);
-                if (c != null) constraints.add(c);
-            }
-
-            System.out.println("📋 ShiftConstraintsV2: Built " + constraints.size() + " active constraints");
+            // Dynamic constraints for all severities
+            String[] severities = {"HARD", "MEDIUM", "SOFT"};
+            for (String sev : severities) {
+                constraints.add(buildSkillMatch(factory, sev));
+                constraints.add(buildNoOverlap(factory, sev));
+                constraints.add(buildUnavailableTimeslot(factory, sev));
+                constraints.add(buildEveryShiftPlanned(factory, sev));
+                constraints.add(buildWageOptimization(factory, sev));
+                constraints.add(buildMaxDailyHours(factory, sev));
+                constraints.add(buildMaxWeeklyHours(factory, sev));
+                constraints.add(buildOvertimeThreshold(factory, sev));
+                constraints.add(buildBreakConstraint(factory, sev));
+                constraints.add(buildConsecutiveShifts(factory, sev));
+                constraints.add(buildPermanentPriority(factory, sev));
+                constraints.add(buildMaximizeRating(factory, sev));
+            }
+
+            System.out.println("📋 ShiftConstraintsV2: Built " + constraints.size() + " total constraints (runtime evaluated)");
             return constraints.toArray(new Constraint[0]);
         }
 

===DIFF===

@@ -83,9 +83,10 @@
 
         const scoreEx = res.body.score_explanation || "";
         // Check if the constraint matches the expected severity in the score explanation
-        const hasHard = scoreEx.includes("hard: constraint (everyShiftPlanned)");
-        const hasMedium = scoreEx.includes("medium: constraint (everyShiftPlanned)");
-        const hasSoft = scoreEx.includes("soft: constraint (everyShiftPlanned)");
+        let match = scoreEx.match(/constraint \(everyShiftPlanned(?:_[A-Z]+)?\).*?matches:\n\s*(-?\d+)(hard|medium|soft)/i);
+        const hasHard = match && match[2].toLowerCase() === 'hard';
+        const hasMedium = match && match[2].toLowerCase() === 'medium';
+        const hasSoft = match && match[2].toLowerCase() === 'soft';
         
         let actualSeverity = "UNKNOWN";
         if (hasHard) actualSeverity = "HARD";

===DIFF===

@@ -81,12 +81,9 @@
             continue;
         }
 
-        const scoreEx = res.body.score_explanation || "";
-        // Check if the constraint matches the expected severity in the score explanation
-        let match = scoreEx.match(/constraint \(everyShiftPlanned(?:_[A-Z]+)?\).*?matches:\n\s*(-?\d+)(hard|medium|soft)/i);
-        const hasHard = match && match[2].toLowerCase() === 'hard';
-        const hasMedium = match && match[2].toLowerCase() === 'medium';
-        const hasSoft = match && match[2].toLowerCase() === 'soft';
+        const hasHard = scoreEx.includes("hard: constraint (everyShiftPlanned_HARD)");
+        const hasMedium = scoreEx.includes("medium: constraint (everyShiftPlanned_MEDIUM)");
+        const hasSoft = scoreEx.includes("soft: constraint (everyShiftPlanned_SOFT)");
         
         let actualSeverity = "UNKNOWN";
         if (hasHard) actualSeverity = "HARD";

===DIFF===

@@ -81,9 +81,11 @@
             continue;
         }
 
+        const scoreEx = res.body.score_explanation || "";
         const hasHard = scoreEx.includes("hard: constraint (everyShiftPlanned_HARD)");
         const hasMedium = scoreEx.includes("medium: constraint (everyShiftPlanned_MEDIUM)");
         const hasSoft = scoreEx.includes("soft: constraint (everyShiftPlanned_SOFT)");
+        const hasSoft = scoreEx.includes("soft: constraint (everyShiftPlanned_SOFT)");
         
         let actualSeverity = "UNKNOWN";
         if (hasHard) actualSeverity = "HARD";

===DIFF===

@@ -85,7 +85,6 @@
         const hasHard = scoreEx.includes("hard: constraint (everyShiftPlanned_HARD)");
         const hasMedium = scoreEx.includes("medium: constraint (everyShiftPlanned_MEDIUM)");
         const hasSoft = scoreEx.includes("soft: constraint (everyShiftPlanned_SOFT)");
-        const hasSoft = scoreEx.includes("soft: constraint (everyShiftPlanned_SOFT)");
         
         let actualSeverity = "UNKNOWN";
         if (hasHard) actualSeverity = "HARD";

===DIFF===

@@ -11311,9 +11311,9 @@
             int breakDurationMinutes = 30; // Default fallback
             
             // Reload constraint configs from DB (fresh) to pull Break After Hours and Duration
-            constraintConfigs = mysqlService.loadAllConstraintConfigs();
-            if (constraintConfigs.isEmpty()) {
-                constraintConfigs = getDefaultConstraintConfigs();
+            List<ConstraintConfig> localConstraintConfigs = mysqlService.loadAllConstraintConfigs();
+            if (localConstraintConfigs.isEmpty()) {
+                localConstraintConfigs = getDefaultConstraintConfigs();
             }
             
             // Allow dynamic overrides from JSON payload for testing

===DIFF===

@@ -11321,7 +11321,7 @@
             List<Map<String, Object>> activeOverrides = (List<Map<String, Object>>) input.get("active_constraints");
             if (activeOverrides != null) {
                 // If the user provided active_constraints, disable all constraints by default
-                constraintConfigs.forEach(c -> c.setEnabled(false));
+                localConstraintConfigs.forEach(c -> c.setEnabled(false));
                 
                 for (Map<String, Object> override : activeOverrides) {
                     String name = (String) override.get("name");
@@ -11328,6 +11328,6 @@
                     Number value = (Number) override.get("value");
                     
-                    constraintConfigs.stream()
+                    localConstraintConfigs.stream()
                         .filter(c -> c.getConstraintName().equals(name))
                         .findFirst()
                         .ifPresent(c -> {
@@ -11338,7 +11338,7 @@
             }
             
             int breakAfterHours = 4; // Default fallback
-            ConstraintConfig breakConfig = constraintConfigs.stream()
+            ConstraintConfig breakConfig = localConstraintConfigs.stream()
                     .filter(c -> c.getConstraintId() == 9)
                     .findFirst().orElse(null);
             if (breakConfig != null) {

===DIFF===

@@ -11677,7 +11677,7 @@
             // ============ CONFIGURE AND RUN V2 SOLVER ============
             List<String> possibleShifts = Arrays.asList(shiftName);
 
-            // constraintConfigs already loaded at the top of the method
+            // localConstraintConfigs already loaded at the top of the method
 
 
             // Calculate average wage for normalization
@@ -11685,7 +11685,9 @@
                     .average()
                     .orElse(1.0);
 
-            ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, constraintConfigs, averageWage);
+            // Set dynamic constraints in ThreadLocal BEFORE solving
+            System.out.println("⚡ Calling setConfiguration on Thread: " + Thread.currentThread().getName());
+            ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, localConstraintConfigs, averageWage);
 
             try {
                 ShiftScheduleV2 problem = new ShiftScheduleV2(planningEntities, possibleShifts, roleLimits, ratingRequirements);
@@ -11722,8 +11722,8 @@
                 List<BreakSchedule> breakSchedules = new ArrayList<>();
                 Map<String, Set<String>> assignedEmployeesPerDate = new HashMap<>();
 
-                // Get OT threshold from constraint config
-                double otThreshold = constraintConfigs.stream()
+                // Load dynamic overtime threshold
+                double otThreshold = localConstraintConfigs.stream()
                         .filter(c -> c.getConstraintId() == 8)
                         .findFirst()
                         .map(c -> c.getParameterValue() != null ? c.getParameterValue() : 8.0)

===DIFF===

@@ -11844,8 +11844,12 @@
                 response.put("new_assignments_made", assignedCount);
                 response.put("entities_planned", planningEntities.size());
                 response.put("total_possible_assignments", totalPossibleAssignments);
-                response.put("skipped_count", skippedCount);
-                response.put("override_used", overrideExisting);
+                // Dynamic Constraints
+                for (ConstraintConfig cc : localConstraintConfigs) {
+                    if (cc.isEnabled()) {
+                        System.out.println("✅ ACTIVE: " + cc.getConstraintName() + " (" + cc.getSeverity() + ")");
+                    }
+                }
                 response.put("ot_threshold_hours", otThreshold);
                 response.put("prioritize_permanent", prioritizePermanent);
 

===DIFF===

@@ -11844,6 +11844,11 @@
                 response.put("new_assignments_made", assignedCount);
                 response.put("entities_planned", planningEntities.size());
                 response.put("total_possible_assignments", totalPossibleAssignments);
+                response.put("skipped_count", skippedCount);
+                response.put("override_used", overrideExisting);
+                response.put("ot_threshold_hours", otThreshold);
+                response.put("prioritize_permanent", prioritizePermanent);
+
                 // Dynamic Constraints
                 for (ConstraintConfig cc : localConstraintConfigs) {
                     if (cc.isEnabled()) {
@@ -11850,7 +11850,5 @@
                     }
                 }
-                response.put("ot_threshold_hours", otThreshold);
-                response.put("prioritize_permanent", prioritizePermanent);
 
                 // Skill match info
                 if (!requiredSkillsMap.isEmpty()) {

===DIFF===

@@ -11863,7 +11863,7 @@
 
                 // Active constraints info
                 List<Map<String, Object>> activeConstraints = new ArrayList<>();
-                for (ConstraintConfig cc : constraintConfigs) {
+                for (ConstraintConfig cc : localConstraintConfigs) {
                     if (cc.isEnabled()) {
                         Map<String, Object> cInfo = new LinkedHashMap<>();
                         cInfo.put("name", cc.getConstraintName());

===DIFF===

@@ -10912,7 +10912,8 @@
             };
         }
 
-        private boolean isConstraintActive(String name, String severity) {
+        private boolean isConstraintActive(String name, String severity, Object dummyFact) {
+            if (dummyFact == null) return false;
             Context ctx = threadContext.get();
             for (ConstraintConfig cc : ctx.activeConstraintConfigs) {
                 if (cc.isEnabled() && cc.getConstraintName().equals(name) && cc.getSeverity().equalsIgnoreCase(severity)) {
@@ -10921,7 +10921,8 @@
             return false;
         }
 
-        private double getConstraintParameter(String name, double defaultValue) {
+        private double getConstraintParameter(String name, double defaultValue, Object dummyFact) {
+            if (dummyFact == null) return defaultValue;
             Context ctx = threadContext.get();
             for (ConstraintConfig cc : ctx.activeConstraintConfigs) {
                 if (cc.isEnabled() && cc.getConstraintName().equals(name) && cc.getParameterValue() != null) {

===DIFF===

@@ -10938,9 +10938,9 @@
         private Constraint buildSkillMatch(ConstraintFactory factory, String severity) {
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
-                    .filter(a -> isConstraintActive("skillMatch", severity))
-                    .filter(a -> {
-                        double matchPercentage = getConstraintParameter("skillMatch", 100.0);
+                    .filter(a -> isConstraintActive("skillMatch", severity, a))
+                    .filter(a -> {
+                        double matchPercentage = getConstraintParameter("skillMatch", 100.0, a);
                         List<String> required = threadContext.get().requiredSkillsPerRole.getOrDefault(a.getPosition(), List.of());
                         if (required.isEmpty()) return false;
                         long matchedCount = required.size() - a.getMissingSkillCount();
@@ -10951,33 +10951,27 @@
                     .asConstraint("skillMatch_" + severity);
         }
 
-        // Constraint #2: No Overlapping Shifts (one shift per employee per day)
-        private Constraint buildNoOverlap(ConstraintFactory factory, String severity) {
+        // Constraint #2: No Overlapping Shifts
+        private Constraint buildNoOverlappingShifts(ConstraintFactory factory, String severity) {
             return factory.forEachUniquePair(Scheduler.EmployeeAssignment.class,
                             Joiners.equal(Scheduler.EmployeeAssignment::getEmployeeId),
                             Joiners.equal(Scheduler.EmployeeAssignment::getDate))
-                    .filter((a1, a2) -> a1.ge
<truncated 12864 bytes>
permanentPriority", severity))
-                    .penalizeLong(resolveScore("permanentPriority", severity),
-                            a -> 1L)
+                    .filter(a -> a.getShift() == null) // Unassigned employee
+                    .filter(a -> "Permanent".equalsIgnoreCase(a.getEmployeeCategory()))
+                    .filter(a -> isConstraintActive("permanentPriority", severity, a))
+                    .penalize(resolveScore("permanentPriority", severity))
                     .asConstraint("permanentPriority_" + severity);
         }
 
-        // Constraint #12: Maximize Rating (dynamic from DB)
+        // Constraint #12: Maximize Rating
         private Constraint buildMaximizeRating(ConstraintFactory factory, String severity) {
             return factory.forEach(Scheduler.EmployeeAssignment.class)
                     .filter(a -> a.getShift() != null)
-                    .filter(a -> isConstraintActive("maximizeRating", severity))
+                    .filter(a -> isConstraintActive("maximizeRating", severity, a))
                     .rewardLong(resolveScore("maximizeRating", severity),
                             a -> {
-                                long multiplier = (long) getConstraintParameter("maximizeRating", 100.0);
-                                return (long) (a.getPerformanceRating() * multiplier);
+                                long multiplier = (long) getConstraintParameter("maximizeRating", 100.0, a);
+                                return a.getPerformanceRating() * multiplier;
                             })
                     .asConstraint("maximizeRating_" + severity);
         }

===DIFF===

@@ -76,7 +76,7 @@
         const expectedSeverity = expectedOutcomes[i];
         
         if (res.statusCode !== 200) {
-            console.log(`❌ Request ${i} FAILED: HTTP ${res.statusCode}`);
+            console.log(`❌ Request ${i} FAILED: HTTP ${res.statusCode} Body: ${JSON.stringify(res.body)}`);
             failed++;
             continue;
         }

===DIFF===

@@ -10844,15 +10844,11 @@
     // ============ ShiftConstraintsV2 — Dynamic Constraint Provider ============
     public static class ShiftConstraintsV2 implements ConstraintProvider {
 
-        private static class Context {
-            Map<String, Integer> maxWorkersPerRole = new HashMap<>();
-            Map<String, List<Integer>> requiredRatingsPerRole = new HashMap<>();
-            Map<String, List<String>> requiredSkillsPerRole = new HashMap<>();
-            List<ConstraintConfig> activeConstraintConfigs = new ArrayList<>();
-            double averageWage = 1.0;
-        }
-
-        private static final ThreadLocal<Context> threadContext = ThreadLocal.withInitial(Context::new);
+        private static Map<String, Integer> maxWorkersPerRole = new HashMap<>();
+        private static Map<String, List<Integer>> requiredRatingsPerRole = new HashMap<>();
+        private static Map<String, List<String>> requiredSkillsPerRole = new HashMap<>();
+        private static List<ConstraintConfig> activeConstraintConfigs = new ArrayList<>();
+        private static double averageWage = 1.0;
 
         public ShiftConstraintsV2() {}
 

===DIFF===

@@ -10859,28 +10859,23 @@
                 List<ConstraintConfig> configs,
                 double avgWage) {
 
-            Context ctx = threadContext.get();
-            ctx.maxWorkersPerRole.clear();
-            ctx.requiredRatingsPerRole.clear();
-            ctx.requiredSkillsPerRole = skillsMap != null ? new HashMap<>(skillsMap) : new HashMap<>();
-            ctx.activeConstraintConfigs = configs != null ? new ArrayList<>(configs) : new ArrayList<>();
-            ctx.averageWage = avgWage > 0 ? avgWage : 1.0;
+            maxWorkersPerRole.clear();
+            requiredRatingsPerRole.clear();
+            requiredSkillsPerRole = skillsMap != null ? new HashMap<>(skillsMap) : new HashMap<>();
+            activeConstraintConfigs = configs != null ? new ArrayList<>(configs) : new ArrayList<>();
+            averageWage = avgWage > 0 ? avgWage : 1.0;
 
             for (Scheduler.ShiftSchedule.RoleLimit l : roleLimits) {
-                ctx.maxWorkersPerRole.put(l.getRoleName(), l.getMaxWorkers());
+                maxWorkersPerRole.put(l.getRoleName(), l.getMaxWorkers());
             }
             for (Scheduler.ShiftSchedule.RatingRequirement r : ratingRequirements) {
-                ctx.requiredRatingsPerRole.put(r.getRoleName(), r.getAllowedRatings());
-            }
-
-            System.out.println("✅ ShiftConstraintsV2 configured on thread " + Thread.currentThread().getName() + ": " +
-                    ctx.maxWorkersPerRole.size() + " role limits, " +
-                    ctx.requiredSkillsPerRole.size() + " skill requirements, " +
-                    ctx.activeConstraintConfigs.size() + " constraint configs");
-        }
-
-        public static void clearConfiguration() {
-            threadContext.remove();
+                requiredRatingsPerRole.put(r.getRoleName(), r.getAllowedRatings());
+            }
+
+            System.out.println("✅ ShiftConstraintsV2 configured: " +
+                    maxWorkersPerRole.size() + " role limits, " +
+                    requiredSkillsPerRole.size() + " skill requirements, " +
+                    activeConstraintConfigs.size() + " constraint configs");
         }
 
         private HardMediumSoftLongScore resolveScore(String constraintName, String severity) {

===DIFF===

@@ -10903,36 +10903,32 @@
             };
         }
 
-        private boolean isConstraintActive(String name, String severity, Object dummyFact) {
-            if (dummyFact == null) return false;
-            Context ctx = threadContext.get();
-            for (ConstraintConfig cc : ctx.activeConstraintConfigs) {
-                if (cc.isEnabled() && cc.getConstraintName().equals(name) && cc.getSeverity().equalsIgnoreCase(severity)) {
-                    return true;
-                }
-            }
-            return false;
-        }
-
-        private double getConstraintParameter(String name, double defaultValue, Object dummyFact) {
-            if (dummyFact == null) return defaultValue;
-            Context ctx = threadContext.get();
-            for (ConstraintConfig cc : ctx.activeConstraintConfigs) {
-                if (cc.isEnabled() && cc.getConstraintName().equals(name) && cc.getParameterValue() != null) {
-                    return cc.getParameterValue();
-                }
-            }
-            return defaultValue;
+        private Constraint buildConstraint(ConstraintFactory factory, ConstraintConfig config) {
+            return switch (config.getConstraintId()) {
+                case 1 -> buildSkillMatch(factory, config);
+                case 2 -> buildNoOverlap(factory, config);
+                case 3 -> buildUnavailableTimeslot(factory, config);
+                case 4 -> buildEveryShiftPlanned(factory, config);
+                case 5 -> buildWageOptimization(factory, config);
+                case 6 -> buildMaxDailyHours(factory, config);
+                case 7 -> buildMaxWeeklyHours(factory, config);
+                case 8 -> buildOvertimeThreshold(factory, config);
+                case 9 -> buildBreakConstraint(fac
<truncated 15304 bytes>
iftStartTimeObj(), a.getBreakStartTime()).toMinutes();
+                        return workBeforeBreak > maxHours * 60;
+                    })
+                    .penalizeLong(resolveScore("breakAfterHours", config.getSeverity()),
+                            a -> {
+                                long workBeforeBreak = java.time.Duration.between(a.getShiftStartTimeObj(), a.getBreakStartTime()).toMinutes();
+                                long overMinutes = (long) (workBeforeBreak - maxHours * 60);
+                                return overMinutes <= 0 ? 0L : (long) Math.ceil(overMinutes / 60.0);
+                            })
+                    .asConstraint("breakAfterHours");
         }
 
         // Constraint #10: Consecutive Shifts
-        private Constraint buildConsecutiveShifts(ConstraintFactory factory, String severity) {
-            return factory.forEach(Scheduler.EmployeeAssignment.class)
-                    .filter(a -> a.getShift() != null)
-                    .filter(a -> isConstraintActive("consecutiveShifts", severity, a))
+        private Constraint buildConsecutiveShifts(ConstraintFactory factory, ConstraintConfig config) {
+            return factory.forEach(Scheduler.EmployeeAssignment.class)
+                    .filter(a -> a.getShift() != null)
                     .groupBy(Scheduler.EmployeeAssignment::getEmployeeId, ConstraintCollectors.toList())
-                    .penalizeLong(resolveScore("consecutiveShifts", severity),
+                    .penalizeLong(resolveScore("consecutiveShifts", config.getSeverity()),
                             (employeeId, list) -> {
                                 if (list.size() < 2) return 0L;
                                 List<Scheduler.EmployeeAssignment> sortedList = new ArrayList<>(list);

===DIFF===

@@ -11079,30 +11079,27 @@
                                 }
                                 return totalGaps;
                             })
-                    .asConstraint("consecutiveShifts_" + severity);
-        }
-
-        // Constraint #11: Permanent Priority
-        private Constraint buildPermanentPriority(ConstraintFactory factory, String severity) {
-            return factory.forEach(Scheduler.EmployeeAssignment.class)
-                    .filter(a -> a.getShift() == null) // Unassigned employee
-                    .filter(a -> "Permanent".equalsIgnoreCase(a.getEmployeeCategory()))
-                    .filter(a -> isConstraintActive("permanentPriority", severity, a))
-                    .penalize(resolveScore("permanentPriority", severity))
-                    .asConstraint("permanentPriority_" + severity);
-        }
-
-        // Constraint #12: Maximize Rating
-        private Constraint buildMaximizeRating(ConstraintFactory factory, String severity) {
-            return factory.forEach(Scheduler.EmployeeAssignment.class)
-                    .filter(a -> a.getShift() != null)
-                    .filter(a -> isConstraintActive("maximizeRating", severity, a))
-                    .rewardLong(resolveScore("maximizeRating", severity),
-                            a -> {
-                                long multiplier = (long) getConstraintParameter("maximizeRating", 100.0, a);
-                                return a.getPerformanceRating() * multiplier;
-                            })
-                    .asConstraint("maximizeRating_" + severity);
+                    .asConstraint("consecutiveShifts");
+        }
+
+        // Constraint #11: Permanent Employee Priority
+        private Constraint buildPermanentPriority(ConstraintFactory factory, ConstraintConfig config) {
+            return factory.forEach(Scheduler.EmployeeAssignment.class)
+                    .filter(a -> a.getShift() != null)
+                    .filter(a -> !a.isPermanentEmployee() && a.isPrioritizePermanent())
+                    .penalizeLong(resolveScore("permanentPriority", config.getSeverity()),
+                            a -> 1L)
+                    .asConstraint("permanentPriority");
+        }
+
+        // Constraint #12: Maximize Rating (dynamic from DB)
+        private Constraint buildMaximizeRating(ConstraintFactory factory, ConstraintConfig config) {
+            long multiplier = config.getParameterValue() != null ? config.getParameterValue().longValue() : 100L;
+            return factory.forEach(Scheduler.EmployeeAssignment.class)
+                    .filter(a -> a.getShift() != null)
+                    .rewardLong(resolveScore("maximizeRating", config.getSeverity()),
+                            a -> (long) (a.getPerformanceRating() * multiplier))
+                    .asConstraint("maximizeRating");
         }
 
         // Also add max workers per role and rating mismatch as always-on constraints

===DIFF===

@@ -11124,24 +11124,14 @@
                     .asConstraint("maxWorkersPerRolePerShift")
             );
 
-            // Dynamic constraints for all severities
-            String[] severities = {"HARD", "MEDIUM", "SOFT"};
-            for (String sev : severities) {
-                constraints.add(buildSkillMatch(factory, sev));
-                constraints.add(buildNoOverlap(factory, sev));
-                constraints.add(buildUnavailableTimeslot(factory, sev));
-                constraints.add(buildEveryShiftPlanned(factory, sev));
-                constraints.add(buildWageOptimization(factory, sev));
-                constraints.add(buildMaxDailyHours(factory, sev));
-                constraints.add(buildMaxWeeklyHours(factory, sev));
-                constraints.add(buildOvertimeThreshold(factory, sev));
-                constraints.add(buildBreakConstraint(factory, sev));
-                constraints.add(buildConsecutiveShifts(factory, sev));
-                constraints.add(buildPermanentPriority(factory, sev));
-                constraints.add(buildMaximizeRating(factory, sev));
-            }
-
-            System.out.println("📋 ShiftConstraintsV2: Built " + constraints.size() + " total constraints (runtime evaluated)");
+            // Dynamic constraints from DB
+            for (ConstraintConfig config : activeConstraintConfigs) {
+                if (!config.isEnabled()) continue;
+                Constraint c = buildConstraint(factory, config);
+                if (c != null) constraints.add(c);
+            }
+
+            System.out.println("📋 ShiftConstraintsV2: Built " + constraints.size() + " active constraints");
             return constraints.toArray(new Constraint[0]);
         }
 

===DIFF===

@@ -11281,9 +11281,9 @@
             int breakDurationMinutes = 30; // Default fallback
             
             // Reload constraint configs from DB (fresh) to pull Break After Hours and Duration
-            List<ConstraintConfig> localConstraintConfigs = mysqlService.loadAllConstraintConfigs();
-            if (localConstraintConfigs.isEmpty()) {
-                localConstraintConfigs = getDefaultConstraintConfigs();
+            constraintConfigs = mysqlService.loadAllConstraintConfigs();
+            if (constraintConfigs.isEmpty()) {
+                constraintConfigs = getDefaultConstraintConfigs();
             }
             
             // Allow dynamic overrides from JSON payload for testing
@@ -11290,7 +11290,7 @@
             List<Map<String, Object>> activeOverrides = (List<Map<String, Object>>) input.get("active_constraints");
             if (activeOverrides != null) {
                 // If the user provided active_constraints, disable all constraints by default
-                localConstraintConfigs.forEach(c -> c.setEnabled(false));
+                constraintConfigs.forEach(c -> c.setEnabled(false));
                 
                 for (Map<String, Object> override : activeOverrides) {
                     String name = (String) override.get("name");
@@ -11297,6 +11297,6 @@
                     Number value = (Number) override.get("value");
                     
-                    localConstraintConfigs.stream()
+                    constraintConfigs.stream()
                         .filter(c -> c.getConstraintName().equals(name))
                         .findFirst()
                         .ifPresent(c -> {

===DIFF===

@@ -11310,7 +11310,7 @@
             }
             
             int breakAfterHours = 4; // Default fallback
-            ConstraintConfig breakConfig = localConstraintConfigs.stream()
+            ConstraintConfig breakConfig = constraintConfigs.stream()
                     .filter(c -> c.getConstraintId() == 9)
                     .findFirst().orElse(null);
             if (breakConfig != null) {

===DIFF===

@@ -11323,32 +11323,18 @@
             }
 
             boolean prioritizePermanent = Boolean.TRUE.equals(input.getOrDefault("prioritize_permanent", true));
-            if (shiftName == null || startDateStr == null || endDateStr == null ||
-                    startTime == null || endTime == null ||
-                    roles == null || roles.isEmpty() || existingUsers == null) {
-                return Response.status(400).entity(Map.of(
-                        "error", "Missing required fields",
-                        "required", Arrays.asList("shift_name", "start_date", "end_date", "start_time", "end_time", "roles", "existing_users")
-                )).build();
-            }
-
-            LocalDate startDate;
-            LocalDate endDate;
-            LocalTime startLocalTime;
-            LocalTime endLocalTime;
-
-            try {
-                startDate = LocalDate.parse(startDateStr);
-                endDate = LocalDate.parse(endDateStr);
-                startLocalTime = LocalTime.parse(startTime);
-                endLocalTime = LocalTime.parse(endTime);
-            } catch (java.time.format.DateTimeParseException | NullPointerException e) {
-                return Response.status(Response.Status.BAD_REQUEST)
-                        .entity(Map.of("error", "Invalid date or time format. Use ISO-8601 (e.g. YYYY-MM-DD and HH:MM)"))
-                        .build();
-            }
-
-
+
+            if (shiftName == null || startDateStr == null || endDateStr == null ||
+                    startTime == null || endTime == null ||
+                    roles == null || roles.isEmpty() || existingUsers == null) {
+                return Response.status(400).entity(Map.of(
+                        "error", "Missing required fields",
+                        "required", Arrays.asList("shift_name", "start_date", "end_date", "start_time", "end_time", "roles", "existing_users")
+                )).build();
+            }
+
+            LocalDate startDate = LocalDate.parse(startDateStr);
+            LocalDate endDate = LocalDate.parse(endDateStr);
 
             if (endDate.isBefore(startDate)) {
                 return Response.status(400).entity(Map.of("error", "End date must be after start date")).build();

===DIFF===

@@ -11348,6 +11348,8 @@
                 current = current.plusDays(1);
             }
 
+            LocalTime startLocalTime = LocalTime.parse(startTime);
+            LocalTime endLocalTime = LocalTime.parse(endTime);
 
             // Shift duration
             double shiftDurationHours;

===DIFF===

@@ -11377,13 +11377,7 @@
 
             for (Map<String, Object> user : existingUsers) {
                 String name = (String) user.get("name");
-                if (name == null || name.trim().isEmpty()) {
-                    name = "Unknown Employee";
-                }
                 Number rateObj = (Number) user.get("rate");
-                if (rateObj == null) {
-                    rateObj = 0.0;
-                }
                 String unit = (String) user.get("unit");
                 Object ratingObj = user.get("rating");
                 String role = (String) user.get("role");

===DIFF===

@@ -11411,11 +11411,8 @@
                 employeeTypes.put(employeeId, employeeType);
 
                 double finalHourlyWage = hourlyWage;
-                String finalName = name;
-                String finalEmployeeType = employeeType;
-                String finalGender = gender;
-                EmployeeInfo empInfo = employeeInfo.computeIfAbsent(employeeId, k ->
-                        new EmployeeInfo(employeeId, finalName, finalEmployeeType, finalGender, finalHourlyWage,
+                EmployeeInfo empInfo = employeeInfo.computeIfAbsent(employeeId, k ->
+                        new EmployeeInfo(employeeId, name, employeeType, gender, finalHourlyWage,
                                 "MGR001", "Operations", role, email, phone)
                 );
 

===DIFF===

@@ -11581,7 +11581,7 @@
                         
                         EmployeeInfo emp = allEmployees.get(eId);
                         Scheduler.EmployeeAssignment pinnedEntity = new Scheduler.EmployeeAssignment(
-                                eId + "_pinned_" + sName + "_" + dStr, eId, emp.getName(), dStr,
+                                eId + "_pinned_" + dStr, eId, emp.getName(), dStr,
                                 emp.getCategory(), emp.getGender(), emp.getDepartment(), emp.getPosition()
                         );
                         pinnedEntity.setShift(sName);

===DIFF===

@@ -11626,7 +11626,7 @@
             // ============ CONFIGURE AND RUN V2 SOLVER ============
             List<String> possibleShifts = Arrays.asList(shiftName);
 
-            // localConstraintConfigs already loaded at the top of the method
+            // constraintConfigs already loaded at the top of the method
 
 
             // Calculate average wage for normalization
@@ -11634,214 +11634,202 @@
                     .average()
                     .orElse(1.0);
 
-            // Set dynamic constraints in ThreadLocal BEFORE solving
-            System.out.println("⚡ Calling setConfiguration on Thread: " + Thread.currentThread().getName());
-            ShiftConstraintsV2.setConfiguration(roleLimits, ratingRequirements, requiredSkillsMap, localConstraintConfigs, averageWage);
-
-            try {
-                ShiftScheduleV2 problem = new ShiftScheduleV2(planningEntities, possibleShifts, roleLimits, ratingRequirements);
-                problem.setRequestedShiftName(shiftName);
-                problem.setPrioritizePermanent(prioritizePermanent);
-
-                long timeLimit = input.containsKey("time_limit_seconds") ? ((Number) input.get("time_limit_seconds")).longValue() : 60L;
-                long unimprovedLimit = input.containsKey("unimproved_time_limit_seconds") ? ((Number) input.get("unimproved_time_limit_seconds")).longValue() : 30L;
-
-                SolverConfig solverConfig = new SolverConfig()
-                        .withSolutionClass(ShiftScheduleV2.class)
-                        .withEntityClasses(Scheduler.EmployeeAssignment.class)
-                        .withConstraintProviderClass(ShiftConstraintsV2.class)
-                        .withTerminationConfig(new ai.timefold.solver.core.config.solver.termination.Termination
<truncated 19082 bytes>
s", otThreshold);
+            response.put("prioritize_permanent", prioritizePermanent);
+
+            // Skill match info
+            if (!requiredSkillsMap.isEmpty()) {
+                response.put("required_skills_by_role", requiredSkillsMap);
+            }
+
+            // Active constraints info
+            List<Map<String, Object>> activeConstraints = new ArrayList<>();
+            for (ConstraintConfig cc : constraintConfigs) {
+                if (cc.isEnabled()) {
+                    Map<String, Object> cInfo = new LinkedHashMap<>();
+                    cInfo.put("name", cc.getConstraintName());
+                    cInfo.put("severity", cc.getSeverity());
+                    if (cc.getParameterValue() != null) cInfo.put("value", cc.getParameterValue());
+                    activeConstraints.add(cInfo);
+                }
+            }
+            response.put("active_constraints", activeConstraints);
+
+            response.put("breakSchedulesCount", breakSchedules.size());
+            response.put("breakConfiguration", Map.of(
+                    "scheduleBreaks", scheduleBreaks,
+                    "breakDurationMinutes", breakDurationMinutes,
+                    "breakAfterHours", breakAfterHours
+            ));
+
+            if (!skippedPerDate.isEmpty()) {
+                response.put("skipped_by_date", skippedPerDate);
+            }
+
+            // Assignment details grouped by date
+            response.put("assignments_by_date", assignmentDetails);
+            response.put("score_explanation", scoreExplanation);
+            response.put("message", "Successfully assigned shifts (V2 with dynamic constraints)!");
+
+            return Response.ok(response).build();
 
         } catch (Exception e) {
             e.printStackTrace();